---
phase: 02-correctness-critical-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enpipe/mkv/__init__.py
  - src/enpipe/mkv/ebml.py
  - src/enpipe/encoding/keyframes.py
  - tests/unit/mkv/_ebml_builder.py
  - tests/unit/mkv/test_ebml.py
  - tests/unit/encoding/test_keyframes.py
  - tests/integration/test_ebml_cross_validation.py
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "The Cues/EBML byte-parsing lives in enpipe.mkv.ebml as pure, no-I/O functions; encoding.keyframes.keyframe_table_cues is a thin I/O shell importing them"
    - "Parsing a real synthetic .mkv's Cues via the isolated module produces a keyframe table identical to keyframe_table_ffprobe on the same file"
    - "A byte-fixture corpus exercises the isolated parser with no real media: well-formed, missing-SeekHead, past-EOF (truncated), mid-CuePoint truncation, and no-keyframe-at-frame-0"
    - "Any structural anomaly returns None (never a wrong-but-parseable table, never an uncaught exception), so keyframe_table falls back to ffprobe exactly as before"
    - "The existing fast test tier still collects and passes after the _eid/_ebml_num/_esz symbols move modules"
  artifacts:
    - path: "src/enpipe/mkv/__init__.py"
      provides: "mkv subpackage marker"
    - path: "src/enpipe/mkv/ebml.py"
      provides: "Pure EBML read/parse core: _ebml_num/_eid/_esz, peek_element_header, find_cues_position, parse_cues_body"
      contains: "def parse_cues_body"
    - path: "src/enpipe/encoding/keyframes.py"
      provides: "keyframe_table_cues thin I/O shell importing enpipe.mkv.ebml; dispatcher + ffprobe fallback unchanged"
      contains: "from enpipe.mkv import ebml"
    - path: "tests/unit/mkv/_ebml_builder.py"
      provides: "Legible VINT/element byte-fixture builder"
    - path: "tests/unit/mkv/test_ebml.py"
      provides: "Byte-fixture tests (Cases A-E) + the moved EBML-primitive tests"
      contains: "from enpipe.mkv.ebml import"
    - path: "tests/integration/test_ebml_cross_validation.py"
      provides: "D-08 cross-validation: keyframe_table_cues == keyframe_table_ffprobe on a synthetic ffmpeg .mkv"
  key_links:
    - from: "src/enpipe/encoding/keyframes.py"
      to: "src/enpipe/mkv/ebml.py"
      via: "import + call find_cues_position/peek_element_header/parse_cues_body"
      pattern: "ebml\\.(find_cues_position|peek_element_header|parse_cues_body)"
    - from: "src/enpipe/encoding/keyframes.py"
      to: "keyframe_table_ffprobe"
      via: "keyframe_table dispatcher falls back on None"
      pattern: "if table is not None"
---

<objective>
Isolate the hand-rolled EBML/Cues parser out of `encoding/keyframes.py` into a new `enpipe.mkv.ebml` subpackage using a read/parse split (DEBT-01), and prove correctness with a byte-fixture corpus plus a synthetic-`.mkv` cross-validation test — all with ZERO behavior change (D-06).

Purpose: The 130-line hand-rolled parser has never been testable in isolation; every prior verification was end-to-end against real media. This plan makes the byte-interpretation logic pure and fixture-testable for the first time, while the public API (`keyframe_table_cues`, `keyframe_table`, `keyframe_table_ffprobe`) keeps its exact names, signatures, and module locations.
Output: `src/enpipe/mkv/{__init__,ebml}.py`, a rewritten thin-shell `keyframe_table_cues`, a byte-fixture builder + test corpus, and a cross-validation integration test.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked user decisions (D-01, D-02, D-03, D-07, D-08, D-11) and the VERIFIED
# read/parse split design, exact function signatures, and 5 byte fixtures with
# exact expected outputs — READ THESE, they contain the hex + expected tables:
@.planning/phases/02-correctness-critical-extraction/02-CONTEXT.md
@.planning/phases/02-correctness-critical-extraction/02-RESEARCH.md

# Extraction source (current inline parser) + parity oracle:
@src/enpipe/encoding/keyframes.py
@tests/unit/encoding/test_keyframes.py

<interfaces>
<!-- Contracts the executor implements. Signatures are LOCKED by RESEARCH.md Pattern 1
     (verified against a working prototype). Move bodies verbatim from the current
     keyframes.py lines cited; do not re-derive the algorithm. -->

In src/enpipe/mkv/ebml.py (pure, no I/O):
  _ebml_num(b: bytes, p: int, keep_marker: bool) -> Tuple[int, int]   # verbatim, keyframes.py:26-37
  _eid(b, p)                                                          # verbatim, keyframes.py:40-41
  _esz(b, p)                                                          # verbatim, keyframes.py:44-45
  peek_element_header(buf: bytes, pos: int) -> Tuple[int, int, int]   # (elem_id, size, header_len)
  find_cues_position(head: bytes, total_size: int) -> Optional[Tuple[int, int, int]]  # (cues_pos, scale, vtrack) | None
  parse_cues_body(cues_body: bytes, video_track: int, scale: int, fps: float) -> Optional[List[Tuple[int, float]]]

In src/enpipe/encoding/keyframes.py (unchanged public names/signatures):
  keyframe_table_cues(src: Path, fps: float) -> Optional[List[Tuple[int, float]]]  # now a thin I/O shell
  keyframe_table_ffprobe(src: Path, fps: float) -> List[Tuple[int, float]]         # UNCHANGED (stays here, D-03)
  keyframe_table(src: Path, fps: float) -> List[Tuple[int, float]]                 # UNCHANGED dispatcher (D-03)
  kf_before / fmt_seek                                                             # UNCHANGED (Phase 1, do not touch)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the EBML read/parse core into enpipe.mkv.ebml and rewrite keyframe_table_cues as a thin I/O shell</name>
  <read_first>
    - src/enpipe/encoding/keyframes.py (lines 26-158 are the extraction source; lines 161-197 STAY)
    - .planning/phases/02-correctness-critical-extraction/02-RESEARCH.md "Pattern 1: Read/Parse Split" (verified target code) and "Anti-Patterns to Avoid" (all 3) and "Pitfall 1/2/5"
  </read_first>
  <files>src/enpipe/mkv/__init__.py, src/enpipe/mkv/ebml.py, src/enpipe/encoding/keyframes.py</files>
  <action>
    Create `src/enpipe/mkv/__init__.py` as an empty package marker matching the empty `encoding`/`shared`/`detection` __init__ style (D-01).

    Create `src/enpipe/mkv/ebml.py` per DEBT-01 / D-01 / D-02, implementing the six functions in the `<interfaces>` block using RESEARCH.md Pattern 1 as the verified target. Move `_ebml_num`/`_eid`/`_esz` VERBATIM from keyframes.py:26-45 (D-06 — do not rewrite the algorithm). Split the current single `keyframe_table_cues` body (keyframes.py:48-158) into: `find_cues_position` (the EBML-header → Segment → SeekHead/Info/Tracks walk that resolves the Cues position, timestamp scale, and video track number — currently keyframes.py:55-116, with the `cues_pos >= sz` bound check folded in as the `total_size` parameter per RESEARCH "Where the total_size bound check lives"), `peek_element_header` (an `_eid`+`_esz` pair returning id/size/header_len), and `parse_cues_body` (the Cues-body CuePoint walk currently keyframes.py:129-158, including the `if table[0][0] != 0: return None` frame-0 guard). Each pure function must return `None` on any structural anomaly and must keep its OWN internal `except (IndexError, ValueError): return None` so byte-fixture tests calling them directly observe `None`, never a traceback (Pitfall 2 — deliberate duplication, NOT redundant). Preserve Russian docstrings/comments and the `typing` (List/Optional/Tuple) style verbatim (D-11).

    Rewrite `keyframe_table_cues` in `encoding/keyframes.py` as the thin I/O shell from RESEARCH.md Pattern 1: `import` `enpipe.mkv.ebml`; `src.stat().st_size` + read the 16MB head; call `find_cues_position(head, sz)`; on a located Cues, do the targeted second `seek`/`read` sized via `peek_element_header`; call `parse_cues_body`. The shell keeps `except (IndexError, OSError, ValueError): return None` for the I/O boundary (D-03). Every `None` from either pure function or any caught exception yields the same outcome — `keyframe_table_cues` returns `None` — with NO new logging or branching inside the shell (Pitfall 5). Delete the now-moved `_ebml_num`/`_eid`/`_esz`/inline-parse code from keyframes.py. Leave `keyframe_table_ffprobe`, `keyframe_table`, `kf_before`, `fmt_seek` exactly as they are (D-03/D-06); update the module docstring's "остаётся здесь INLINE ... отложен до фазы 2" note to reflect the completed extraction.
  </action>
  <verify>
    <automated>uv run python -c "from enpipe.mkv.ebml import _eid, _ebml_num, _esz, peek_element_header, find_cues_position, parse_cues_body; from enpipe.encoding.keyframes import keyframe_table, keyframe_table_cues, keyframe_table_ffprobe, kf_before, fmt_seek; import enpipe.encoding.keyframes as k, inspect; assert 'from enpipe.mkv import ebml' in inspect.getsource(k); print('ok')"</automated>
  </verify>
  <done>enpipe.mkv.ebml exposes the six functions; enpipe.encoding.keyframes imports ebml and no longer defines _eid/_ebml_num/_esz; keyframe_table_cues/keyframe_table_ffprobe/keyframe_table/kf_before/fmt_seek remain importable from enpipe.encoding.keyframes with unchanged signatures.</done>
</task>

<task type="auto">
  <name>Task 2: Build the byte-fixture corpus and repair the split test-import surface</name>
  <read_first>
    - .planning/phases/02-correctness-critical-extraction/02-RESEARCH.md "EBML Byte-Fixture Corpus" Cases A-E (exact hex + expected outputs) and "Pitfall 4" (the test_keyframes import break)
    - tests/unit/encoding/test_keyframes.py (the four EBML-primitive tests to move: test_ebml_num_single_byte_id_keeps_marker, test_ebml_num_single_byte_size_strips_marker, test_eid_two_byte_id, test_esz_two_byte_size)
  </read_first>
  <files>tests/unit/mkv/_ebml_builder.py, tests/unit/mkv/test_ebml.py, tests/unit/encoding/test_keyframes.py</files>
  <action>
    Create `tests/unit/mkv/_ebml_builder.py` per RESEARCH "Recommended approach for the test suite": a small, well-commented private builder exposing `vint(value, length)`, `elem(id_bytes, body)`, and named element-ID constants (CUES_ID=0x1C53BB6B, SEEKHEAD_ID=0x114D9B74, etc. — same magic numbers the parser uses, so a reviewer cross-checks them against `mkv/ebml.py`). Do NOT commit binary `.mkv` fixtures.

    Create `tests/unit/mkv/test_ebml.py` importing from `enpipe.mkv.ebml`. Implement the five fixture cases from RESEARCH with their EXACT expected outputs: Case A well-formed → find_cues_position returns (42, 1_000_000, 1), peek_element_header(head,42) → (0x1C53BB6B, 41, 5), parse_cues_body → [(0,0.0),(12,0.5),(24,1.0)] at fps=24; Case B missing-SeekHead → find_cues_position → None; Case C SeekHead-past-EOF via total_size=10 → find_cues_position → None; Case D mid-CuePoint truncation → parse_cues_body → None (assert `is None`, never `pytest.raises`, per Pitfall 2); Case E no-keyframe-at-frame-0 → parse_cues_body → None. Build fixtures with `_ebml_builder` helpers (legible, commented "why"), OR reuse the exact hex blobs in RESEARCH Cases A/B if the builder cannot reproduce them byte-for-byte — the expected outputs are the contract.

    MOVE the four EBML-primitive tests out of `tests/unit/encoding/test_keyframes.py` into `tests/unit/mkv/test_ebml.py`, importing `_eid`/`_ebml_num`/`_esz` from `enpipe.mkv.ebml`. In `tests/unit/encoding/test_keyframes.py`, update the top import line to drop `_eid, _ebml_num, _esz` (keep `fmt_seek, kf_before`) so the file still collects (Pitfall 4). Do not touch the remaining kf_before/fmt_seek tests.
  </action>
  <verify>
    <automated>uv run pytest -m "not hardware" tests/unit/mkv/test_ebml.py tests/unit/encoding/test_keyframes.py -q</automated>
  </verify>
  <done>tests/unit/mkv/test_ebml.py covers Cases A-E with the exact expected outputs plus the four moved primitive tests; tests/unit/encoding/test_keyframes.py collects and passes with no _eid/_ebml_num/_esz import; every anomaly case asserts a None return, never a raised exception.</done>
</task>

<task type="auto">
  <name>Task 3: Add the D-08 cross-validation integration test and confirm the full fast tier passes</name>
  <read_first>
    - .planning/phases/02-correctness-critical-extraction/02-RESEARCH.md "Cross-Validation Harness (D-08)" (the exact ffmpeg recipe with -cues_to_front 1 and expected table [(0,0.0),(12,0.5),(24,1.0),(36,1.5)])
  </read_first>
  <files>tests/integration/test_ebml_cross_validation.py</files>
  <action>
    Create `tests/integration/test_ebml_cross_validation.py` implementing D-08 exactly as in RESEARCH "Cross-Validation Harness". In a `tmp_path`, generate a synthetic `.mkv` via `subprocess.run(["ffmpeg", ...])` using the verified recipe: `-f lavfi -i testsrc2=size=64x64:rate=24:duration=2 -c:v libx264 -g 12 -keyint_min 12 -sc_threshold 0 -pix_fmt yuv420p -f matroska -cues_to_front 1` (the `-cues_to_front 1` flag is REQUIRED — the default muxer writes no Cues for a short clip). Assert `keyframe_table_cues(mkv, 24.0)` is not None and equals both `keyframe_table_ffprobe(mkv, 24.0)` and `[(0,0.0),(12,0.5),(24,1.0),(36,1.5)]` — this is the DEBT-01 correctness proof that the split module matches the trusted ffprobe path. Guard with `@pytest.mark.skipif(shutil.which("ffmpeg") is None, ...)` as a defensive fallback (ffmpeg is a hard project dependency, present in the devcontainer). Place under `tests/integration/` so it runs in the default (`not hardware`) tier — it needs ffmpeg only, no GPU/QSV, so do NOT mark it `hardware`.

    Run the entire fast tier to confirm zero regressions from the whole plan (D-10).
  </action>
  <verify>
    <automated>uv run pytest -m "not hardware" -q</automated>
  </verify>
  <done>The cross-validation test passes: keyframe_table_cues == keyframe_table_ffprobe == [(0,0.0),(12,0.5),(24,1.0),(36,1.5)] on the synthetic .mkv; the full `pytest -m "not hardware"` suite (Phase-1 tests + new mkv tests + cross-validation) passes with no collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.mkv` file bytes → EBML parser | A local video file (possibly corrupt, truncated, or adversarially malformed) crosses into the hand-rolled byte parser. This is the phase's only real attack/robustness surface — no network, auth, or multi-user surface exists (PROJECT.md: not a deployed service). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | `mkv.ebml.find_cues_position` / `parse_cues_body` on malformed/truncated bytes | mitigate | Preserve the bounded VINT loop (`while length <= 8`) and each pure function's own `except (IndexError, ValueError): return None` verbatim (Pitfall 2); prove containment with fixtures Case B (missing SeekHead), Case C (past-EOF via total_size bound), Case D (mid-CuePoint truncation → None, not a traceback). |
| T-02-02 | Tampering (silent data corruption) | `parse_cues_body` returning a wrong-but-parseable keyframe table that shifts frame boundaries downstream | mitigate | Keep the `table[0][0] != 0 → None` defensive frame-0 guard (Case E) and cross-validate the full table against the independent ffprobe path (D-08, Task 3). Anomaly → None → safe ffprobe fallback, never silently-wrong data. |
| T-02-03 | Tampering (D-13-class silent behavior change) | The extraction itself botching the parse and shifting output vs `legacy/` | mitigate | Byte-identical contract: cross-validation test + full fast-tier regression (Task 3); `legacy/` untouched as oracle; the hardware `scratch/parity_encode.py` gate (D-10, verified in 02-02) is the end-to-end backstop. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (pure stdlib extraction; RESEARCH "Package Legitimacy Audit: Not applicable"). Nothing to disposition. |
</threat_model>

<verification>
- `uv run pytest -m "not hardware" -q` passes with zero collection errors (Phase-1 suite intact + new tests).
- `enpipe.mkv.ebml` is pure (no `import`/call of `Path`/`open`/`stat`/subprocess); all file I/O lives in the `keyframe_table_cues` shell.
- Byte-fixture Cases A-E return the exact RESEARCH expected outputs; anomaly cases return `None` (no exception leaks).
- Cross-validation: isolated `keyframe_table_cues` == `keyframe_table_ffprobe` on a real synthetic `.mkv`.
</verification>

<success_criteria>
- DEBT-01 satisfied: EBML/Cues parser isolated in `enpipe.mkv.ebml` behind a read/parse split; `keyframe_table_cues` is a thin I/O shell; dispatcher fallback-on-anomaly (None → ffprobe) preserved exactly.
- ROADMAP SC #1 (cross-validation vs ffprobe) and SC #2 (byte-fixture corpus, no real media) both met.
- Zero behavior change: `keyframe_table`/`keyframe_table_cues`/`keyframe_table_ffprobe` keep exact names, signatures, module locations; `legacy/` untouched.
</success_criteria>

<output>
Create `.planning/phases/02-correctness-critical-extraction/02-01-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

Locked CONTEXT.md decisions implemented by this plan:
- **D-01**: create `src/enpipe/mkv/{__init__,ebml}.py`; move `_ebml_num`/`_eid`/`_esz` + Cues parsing out of `encoding/keyframes.py`.
- **D-02**: read/parse split — pure `parse_cues_body` core (no file I/O) + thin I/O shell (`find_cues_position`/`peek_element_header`).
- **D-03**: `keyframe_table_ffprobe` + `keyframe_table` dispatcher STAY in keyframes.py, which imports the Cues entry point from `enpipe.mkv.ebml`; fallback-on-anomaly (None → ffprobe) preserved.
- **D-07**: EBML byte-fixture corpus (normal / missing-SeekHead / malformed / truncated) — safe None on unparseable, never wrong-but-parseable.
- **D-08**: cross-validation on a synthetic real `.mkv` (`-cues_to_front 1`): `mkv.ebml` == legacy inline `keyframe_table_cues` semantics == `keyframe_table_ffprobe`.
- **D-11**: preserve conventions verbatim (Russian docstrings, typing style, banners); `legacy/` untouched.
