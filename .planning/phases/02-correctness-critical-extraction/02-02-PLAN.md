---
phase: 02-correctness-critical-extraction
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/enpipe/encoding/keyframes.py
  - src/enpipe/encoding/pipeline.py
  - tests/unit/encoding/test_keyframes.py
  - tests/unit/encoding/test_pipeline_ordering.py
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "Per-scene seek/trim is computed by a pure compute_chunk_seek_trim(table, s, e) co-located with kf_before/fmt_seek; pipeline.py calls it instead of the inline lines"
    - "High-water-mark flush ordering is decided by a pure contiguous_run(next_append, ready); flush_appends is a thin I/O shell that consumes its result"
    - "The exact seek/trim strings and the concat order are byte-identical to the pre-extraction inline code"
    - "compute_chunk_seek_trim and contiguous_run have direct unit tests over synthetic edge cases (boundaries on/off keyframe; out-of-order completion)"
    - "The full fast test tier still passes; the hardware scratch/parity_encode.py gate still yields byte-identical movie.obu"
  artifacts:
    - path: "src/enpipe/encoding/keyframes.py"
      provides: "Pure compute_chunk_seek_trim next to kf_before/fmt_seek"
      contains: "def compute_chunk_seek_trim"
    - path: "src/enpipe/encoding/pipeline.py"
      provides: "Module-level pure contiguous_run + thin flush_appends shell + compute_chunk_seek_trim call site"
      contains: "def contiguous_run"
    - path: "tests/unit/encoding/test_keyframes.py"
      provides: "compute_chunk_seek_trim unit tests (on/off keyframe)"
      contains: "compute_chunk_seek_trim"
    - path: "tests/unit/encoding/test_pipeline_ordering.py"
      provides: "contiguous_run edge-case unit tests"
      contains: "contiguous_run"
  key_links:
    - from: "src/enpipe/encoding/pipeline.py"
      to: "compute_chunk_seek_trim"
      via: "chunk-task build loop calls it for (s,e)"
      pattern: "compute_chunk_seek_trim\\(table, s, e\\)"
    - from: "src/enpipe/encoding/pipeline.py flush_appends"
      to: "contiguous_run"
      via: "iterate the pure contiguous run and advance next_append"
      pattern: "contiguous_run\\(next_append, ready\\)"
---

<objective>
Extract the two correctness-critical arithmetic pieces from `pipeline.py` into pure, directly unit-testable functions with ZERO behavior change (DEBT-02): `compute_chunk_seek_trim(table, s, e)` (the per-scene seek/trim math, D-04) co-located in `encoding/keyframes.py`, and `contiguous_run(next_append, ready)` (the high-water-mark flush ordering, D-05) module-level in `encoding/pipeline.py`. The file I/O (copyfileobj/unlink/next_append advance) stays in `pipeline.py` as a thin shell.

Purpose: PITFALLS.md flags these two as the highest silent-corruption risk in the codebase (a shifted seek/trim or a mis-ordered concat silently corrupts output). Making them pure functions gives them their first direct unit tests over synthetic edge cases, without touching a byte of encoded output.
Output: two pure functions, rewired call sites in `pipeline.py`, unit tests covering on/off-keyframe boundaries and out-of-order chunk completion, and a re-run of the Phase-1 encode-parity gate proving the extraction left `movie.obu` byte-identical.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions (D-04, D-05, D-06, D-09, D-10, D-11) + VERIFIED signatures,
# the exact flush_appends thin-shell rewrite, and the contiguous_run edge-case table:
@.planning/phases/02-correctness-critical-extraction/02-CONTEXT.md
@.planning/phases/02-correctness-critical-extraction/02-RESEARCH.md

# Extraction sources:
@src/enpipe/encoding/pipeline.py
@src/enpipe/encoding/keyframes.py

<interfaces>
<!-- Signatures LOCKED by RESEARCH.md Pattern 2/3 (verified against current inline math).
     compute_chunk_seek_trim COMPOSES the already-tested kf_before/fmt_seek — do not rewrite them. -->

In src/enpipe/encoding/keyframes.py (co-located with kf_before/fmt_seek, D-04):
  compute_chunk_seek_trim(table: List[Tuple[int, float]], s: int, e: int) -> Tuple[str, str]
    # returns (seek, trim); internally: kf_frame, kf_time = kf_before(table, s);
    #   seek = fmt_seek(kf_time); trim = f"{s - kf_frame}:{e - 1 - kf_frame}"

In src/enpipe/encoding/pipeline.py (module level, D-05):
  contiguous_run(next_append: int, ready: Dict[int, int] | set[int]) -> List[int]
    # unbroken run of ready indices starting at next_append; returns a concrete
    # list (NOT a generator, Pitfall 3); does NOT mutate ready or advance next_append

Existing (Phase 1, DO NOT modify): kf_before, fmt_seek
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract compute_chunk_seek_trim and contiguous_run, rewire the two pipeline call sites</name>
  <read_first>
    - src/enpipe/encoding/pipeline.py lines 107-115 (inline seek/trim in the chunk-task loop) and 134-143 (flush_appends closure) and its import line 35
    - src/enpipe/encoding/keyframes.py kf_before (199-209) + fmt_seek (212-222) — compose these, do not rewrite (D-06)
    - .planning/phases/02-correctness-critical-extraction/02-RESEARCH.md "Pattern 2" and "Pattern 3" (verified target code + the flush_appends thin-shell) and "Pitfall 3" (return a list, not an iterator)
  </read_first>
  <files>src/enpipe/encoding/keyframes.py, src/enpipe/encoding/pipeline.py</files>
  <action>
    In `encoding/keyframes.py`, add `compute_chunk_seek_trim(table, s, e) -> Tuple[str, str]` immediately next to `kf_before`/`fmt_seek` (D-04), with the body extracted VERBATIM from pipeline.py:108-110: `kf_frame, kf_time = kf_before(table, s)`, `seek = fmt_seek(kf_time)`, `trim = f"{s - kf_frame}:{e - 1 - kf_frame}"`, `return seek, trim`. Keep the Russian docstring style (D-11). Do NOT rewrite kf_before/fmt_seek (D-06). Use the 2-tuple `(seek, trim)` — today's call site does not use `kf_frame` after computing `trim` (minimal-diff choice per D-04).

    In `encoding/pipeline.py`, add `contiguous_run(next_append, ready) -> List[int]` at module level (D-05): materialize the concrete list of the unbroken run of indices starting at `next_append` while `i in ready` (NOT a generator — Pitfall 3); it must not mutate `ready` or advance `next_append`.

    Rewire the two call sites with no behavior change: (1) replace pipeline.py:108-110's three inline lines inside the chunk-task loop with `seek, trim = compute_chunk_seek_trim(table, s, e)` (add it to the existing `from .keyframes import ...` line at pipeline.py:35); (2) rewrite `flush_appends` as the thin shell from RESEARCH Pattern 3 — `for i in contiguous_run(next_append, ready):` then copyfileobj + flush + conditional unlink + `next_append = i + 1`, keeping the `nonlocal next_append` and preserving the exact I/O and the `args.keep` guard. The emitted `seek`/`trim` strings, the concat order, and the final `next_append` value must be byte-for-byte what the pre-extraction `while next_append in ready` loop produced (D-06). Do not add logging or branching not present before (Pitfall 5-style discipline).
  </action>
  <verify>
    <automated>uv run python -c "from enpipe.encoding.keyframes import compute_chunk_seek_trim; from enpipe.encoding.pipeline import contiguous_run; assert compute_chunk_seek_trim([(0,0.0),(48,2.0),(96,4.0)], 70, 96) == ('00:00:02.000','22:47'); assert contiguous_run(0, {0,1,3}) == [0,1]; import enpipe.encoding.pipeline as p, inspect; src=inspect.getsource(p); assert 'compute_chunk_seek_trim(table, s, e)' in src and 'contiguous_run(next_append, ready)' in src; print('ok')"</automated>
  </verify>
  <done>compute_chunk_seek_trim is importable from enpipe.encoding.keyframes and returns the same (seek,trim) as the old inline math; contiguous_run is importable from enpipe.encoding.pipeline and returns a list; both call sites in pipeline.py use the extracted functions; flush_appends is a thin shell that advances next_append identically.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test both pure functions and confirm the full regression backstop</name>
  <read_first>
    - .planning/phases/02-correctness-critical-extraction/02-RESEARCH.md "Pattern 3" edge-case table (the six contiguous_run rows) and "Pattern 2"
    - tests/unit/encoding/test_keyframes.py (add compute_chunk_seek_trim tests here; import line updated in 02-01)
  </read_first>
  <files>tests/unit/encoding/test_keyframes.py, tests/unit/encoding/test_pipeline_ordering.py</files>
  <action>
    Add `compute_chunk_seek_trim` tests to `tests/unit/encoding/test_keyframes.py` (add it to the module import), covering the D-09 edge cases: scene boundary exactly on a keyframe (e.g. table `[(0,0.0),(48,2.0),(96,4.0)]`, s=48 → kf_frame 48 → trim `0:...`) and off a keyframe (s=70 → kf_frame 48 → seek `00:00:02.000`, trim `22:47`), asserting the exact `(seek, trim)` strings. Reuse the existing kf_before/fmt_seek table style already in the file.

    Create `tests/unit/encoding/test_pipeline_ordering.py` importing `contiguous_run` from `enpipe.encoding.pipeline`, covering every row of the RESEARCH Pattern 3 edge-case table: `{}`@0→`[]`; `{0}`@0→`[0]`; `{0,1,3}`@0→`[0,1]` (3 blocked by missing 2 — the D-05 named case); `{1,2}`@0→`[]` (next_append itself not ready); `{0,1,2}`@0→`[0,1,2]`; `{5}`@5→`[5]` (non-zero high-water mark). Assert `contiguous_run` does not mutate the input `ready`.

    Run the full fast tier to confirm the DEBT-02 extraction changed no encoded-output-affecting behavior and broke no Phase-1 test (D-10).
  </action>
  <verify>
    <automated>uv run pytest -m "not hardware" -q</automated>
  </verify>
  <done>compute_chunk_seek_trim tests cover on/off-keyframe boundaries with exact seek/trim strings; test_pipeline_ordering.py covers all six contiguous_run edge cases and the no-mutation guarantee; the full `pytest -m "not hardware"` suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the Phase-1 encode-parity gate to prove byte-identical movie.obu (DEBT-02 end-to-end backstop, D-10)</name>
  <read_first>
    - scratch/parity_encode.py (the Phase-1 hardware-gated gate — DO NOT modify. `_hardware_available()` (line 97) probes `/dev/dri/renderD128` + `qsvencc`; `main()` (line 180) prints `SKIP: no Arc hardware` and returns 0 when either is absent, else runs the legacy vs. migrated encode and cmp's the pre-mux `movie.obu`.)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-03-PLAN.md Task 3 (the Phase-1 precedent this mirrors — same `<automated>` command and PASS/SKIP contract)
  </read_first>
  <files>scratch/parity_encode.py</files>
  <action>
    Re-invoke the UNCHANGED Phase-1 encode-parity gate (`scratch/parity_encode.py`) as the end-to-end backstop for this phase's DEBT-02 extraction — proving that routing per-scene seek/trim through the new pure `compute_chunk_seek_trim` and the concat order through the new pure `contiguous_run` (plus the thin `flush_appends` shell) produced a byte-identical pre-mux `movie.obu` vs the `legacy/` oracle. Make NO code changes to `scratch/parity_encode.py` — this task only RE-RUNS it. The script is safely auto-runnable in any environment because it self-detects hardware first (`_hardware_available()` probing `/dev/dri/renderD128` + `qsvencc`) and, when hardware is absent, prints `SKIP: no Arc hardware` and exits 0 (a clean, documented SKIP — NOT a failure), exactly as Phase 1's 01-03 used it. When Arc hardware IS present it runs its determinism pre-check and compares the raw `movie.obu` bytes (byte-identical when qsvencc is deterministic; frame-count + SSIM/PSNR-epsilon fallback otherwise, per the script's own logic). Do NOT gate the fast tier (`pytest -m "not hardware"`) on this; it is a standalone auto-verification step, run directly, that backs the `must_haves.truths` "still yields byte-identical movie.obu" claim with a real executable check rather than a manual-only note.
  </action>
  <verify>
    <automated>uv run python scratch/parity_encode.py && echo "PARITY_OK_OR_SKIPPED"</automated>
  </verify>
  <acceptance_criteria>
    - `scratch/parity_encode.py` is re-run UNCHANGED (no diff to the script) and exits 0 under its PASS/SKIP contract.
    - On Arc hardware: the migrated `run_encode` (now calling the extracted `compute_chunk_seek_trim`/`contiguous_run`) yields a byte-identical pre-mux `movie.obu` vs the `legacy/` oracle — or the frame-count + SSIM/PSNR-epsilon fallback when the script's determinism pre-check proves qsvencc non-deterministic.
    - Off hardware: the script prints `SKIP: no Arc hardware` and exits 0 (clean documented skip), so the DEBT-02 "zero encoded-output change" truth is backed by a real, auto-runnable step in any environment.
  </acceptance_criteria>
  <done>The "zero encoded-output change" truth is backed by an executable, automated re-run of the Phase-1 parity gate: byte-identical movie.obu on Arc hardware, or a clean documented SKIP when hardware is absent — no code change to the gate itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyframe table + scene list → seek/trim + concat ordering arithmetic | Pure in-memory arithmetic over already-validated data (the table came from the parser/ffprobe; scenes from `read_scenes`). No untrusted-byte, network, or auth surface — this plan is pure-function extraction of arithmetic already present. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (silent data corruption) | `compute_chunk_seek_trim` producing a shifted seek/trim vs the inline math (PITFALLS.md Pitfall 1 — the highest-risk arithmetic) | mitigate | Extract the body verbatim (compose the already-tested kf_before/fmt_seek, D-06); assert exact `(seek, trim)` strings for on/off-keyframe boundaries (Task 2); full fast-tier regression. |
| T-02-05 | Tampering (silent reorder) | `contiguous_run` flushing more/fewer/out-of-order indices than the `while next_append in ready` loop | mitigate | Return a concrete list, not a lazy generator (Pitfall 3); unit-test all six edge-case rows incl. the `{0,1,3}` gap case; no-mutation assertion; thin-shell flush preserves I/O and next_append advance exactly. |
| T-02-06 | Tampering (D-13-class end-to-end change) | The refactor altering encoded `movie.obu` bytes or concat order | mitigate | ZERO-behavior-change contract: full `pytest -m "not hardware"` regression (Task 2) + Task 3 re-runs the hardware `scratch/parity_encode.py` gate, which must still yield a byte-identical pre-mux `movie.obu` vs the `legacy/` oracle (D-10) — auto-runnable, cleanly SKIPping when Arc hardware is absent. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (pure stdlib extraction). Nothing to disposition. |
</threat_model>

<verification>
- `uv run pytest -m "not hardware" -q` passes (Phase-1 + 02-01 + new DEBT-02 tests).
- `compute_chunk_seek_trim` and `contiguous_run` are pure (no I/O); `flush_appends` retains all file I/O.
- Emitted seek/trim strings and concat order are byte-identical to pre-extraction (unit tests + regression).
- ENCODE-PARITY GATE (auto-runnable, Task 3): `uv run python scratch/parity_encode.py` re-runs the UNCHANGED Phase-1 gate as the end-to-end D-10 backstop. It self-detects hardware (`_hardware_available()`) and exits 0 either way: on Arc hardware it must produce a byte-identical pre-mux `movie.obu` vs `legacy/` (or the script's frame-count + SSIM/PSNR-epsilon fallback when qsvencc is non-deterministic); off hardware it prints `SKIP: no Arc hardware` and exits 0 — so this is a real automated verification, not a manual-only note.
</verification>

<success_criteria>
- DEBT-02 satisfied: seek/trim math (`compute_chunk_seek_trim`) and high-water-mark flush ordering (`contiguous_run`) extracted into pure, directly unit-tested functions; `pipeline.py` calls them; `flush_appends` is a thin I/O shell.
- ROADMAP SC #3 met: pure functions with unit tests over synthetic edge cases (off-keyframe boundaries, out-of-order completion), with no change to encoded output vs `legacy/` — proven by the Task 3 re-run of the encode-parity gate.
- `kf_before`/`fmt_seek` untouched; `scratch/parity_encode.py` re-run unchanged; `legacy/` untouched as oracle.
</success_criteria>

<output>
Create `.planning/phases/02-correctness-critical-extraction/02-02-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

Locked CONTEXT.md decisions implemented by this plan:
- **D-04**: extract inline seek/trim (pipeline.py) into pure `compute_chunk_seek_trim(table, s, e)` co-located in keyframes.py; exact seek/trim strings unchanged.
- **D-05**: extract the `flush_appends` high-water-mark ordering into pure `contiguous_run(next_append, ready)`; file I/O shell stays in pipeline.py; concat order unchanged.
- **D-06**: behavior-preserving refactor only — encoded output byte-identical; `kf_before`/`fmt_seek` reused, not rewritten.
- **D-09**: pure unit tests for `compute_chunk_seek_trim` (on/off-keyframe boundaries) and `contiguous_run` (out-of-order completion).
- **D-10**: regression backstop — Phase-1 fast suite still passes; the hardware `scratch/parity_encode.py` gate re-run (Task 3) yields byte-identical `movie.obu` (or clean SKIP).
- **D-11**: preserve conventions verbatim; `legacy/` untouched as the parity oracle.
