---
phase: 04-unified-cli-hardware-gated-real-media-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enpipe/detection/pipeline.py
  - src/enpipe/cli/__init__.py
  - src/enpipe/cli/main.py
  - pyproject.toml
  - tests/unit/cli/test_run_detect_roundtrip.py
  - tests/unit/cli/test_cli_dispatch.py
autonomous: true
requirements: [PKG-01]

must_haves:
  truths:
    - "`enpipe detect <video>` writes `<video>.scenes` in the exact legacy line format that `read_scenes` parses"
    - "`enpipe encode <video> <video>.scenes` dispatches to the existing run_encode with an argv-compatible flag surface"
    - "The two-stage `<video>.scenes` handoff round-trips: read_scenes(run_detect output) == the (start,end) pairs detect_scenes produced"
    - "The `enpipe` console_script is installed and `enpipe detect --help` / `enpipe encode --help` exit 0 with Russian help text (exercised via the real `uv run enpipe` subprocess entry, not only in-process main())"
    - "Unspecified `--jobs` resolves to detect=4 (hardcoded) and encode=encoding.pipeline.JOBS (env-var), asserted explicitly in the dispatch test"
    - "legacy/scene_detection.py and legacy/encode_scenes.py are untouched and still runnable"
  artifacts:
    - path: "src/enpipe/detection/pipeline.py"
      provides: "run_detect(args) — Namespace-shaped detect orchestration mirroring run_encode"
      exports: ["run_detect"]
      min_lines: 25
    - path: "src/enpipe/cli/main.py"
      provides: "argparse dispatcher: build_parser() + main() for `enpipe detect`/`enpipe encode`"
      exports: ["build_parser", "main"]
      contains: "add_subparsers"
    - path: "src/enpipe/cli/__init__.py"
      provides: "cli package marker"
    - path: "pyproject.toml"
      provides: "[project.scripts] enpipe entry point"
      contains: "enpipe.cli.main:main"
  key_links:
    - from: "src/enpipe/cli/main.py"
      to: "src/enpipe/detection/pipeline.py"
      via: "import run_detect + set_defaults(func=run_detect)"
      pattern: "from enpipe.detection.pipeline import run_detect"
    - from: "src/enpipe/cli/main.py"
      to: "src/enpipe/encoding/pipeline.py"
      via: "import run_encode + set_defaults(func=run_encode)"
      pattern: "from enpipe.encoding.pipeline import run_encode"
    - from: "src/enpipe/detection/pipeline.py"
      to: "src/enpipe/encoding/scenes_io.py"
      via: ".scenes line format consumed by _SCENE_RE"
      pattern: "frames \\[\\{?.*start_frame"
---

<objective>
Add the single `enpipe` console entry point (PKG-01) over the two independently-verified stages, as a THIN argparse dispatcher. The only new business logic is `run_detect(args)` — the detect-side counterpart of the already-existing `run_encode(args)` — which migrates the `.scenes`-writing logic out of `legacy/scene_detection.py`'s `__main__` block. `enpipe encode` wraps the unchanged `run_encode`. Both legacy scripts remain the untouched parity oracle.

Purpose: Close PKG-01 — a unified `enpipe detect`/`enpipe encode` entry point that preserves the two-stage `<video>.scenes` handoff with zero behavior change to detect/encode.
Output: `src/enpipe/detection/pipeline.py::run_detect`, `src/enpipe/cli/{__init__,main.py}`, the `[project.scripts]` wiring, and fast hardware-free tests for the round-trip contract and CLI dispatch.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-CONTEXT.md
@.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-RESEARCH.md

<read_first>
- legacy/scene_detection.py:647-692 — the detect `__main__`/argparse + `.scenes`-writing logic to migrate verbatim (minus argparse) into run_detect.
- src/enpipe/encoding/pipeline.py — run_encode(args): the Namespace-shaped template + its docstring's "sanctioned deviation" wording style to mirror for run_detect's docstring; also exports module-level JOBS (env-var default) for the encode --jobs default.
- src/enpipe/detection/detect.py — detect_scenes(path, config, jobs) signature.
- src/enpipe/detection/config.py — DetectionConfig fields (analysis_width, use_qsv, qsv_device, adaptive_threshold, min_scene_len_frames, min_scene_len_sec).
- src/enpipe/encoding/scenes_io.py — read_scenes + _SCENE_RE = re.compile(r"frames \[\s*(\d+),\s*(\d+)\)"): the round-trip contract run_detect's output must satisfy.
- pyproject.toml:15-16 — the reserved (currently commented) [project.scripts] slot; note the comment references the superseded 3-file layout and must be replaced with the D-01 single-file target.
</read_first>

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — use directly, no exploration. -->

detect_scenes(path, config: DetectionConfig = DetectionConfig(), jobs: int = 1) -> List[Scene]
Scene(index, start_frame, end_frame, start_sec, end_sec)  # frozen dataclass
DetectionConfig(analysis_width=320, use_qsv=True, qsv_device=None, adaptive_threshold=3.0,
                min_scene_len_frames=72, min_scene_len_sec=3.0, ...)  # frozen dataclass
run_encode(args) -> None   # in encoding/pipeline.py; reads args.video/scenes/out/frm/to/
                           # workdir/keep/jobs/no_audio/no_metrics/csv
JOBS = int(os.environ.get("JOBS", "3"))   # module-level in encoding/pipeline.py
read_scenes(path: Path) -> List[Tuple[int,int]]   # parses <video>.scenes via _SCENE_RE
_SCENE_RE = re.compile(r"frames \[\s*(\d+),\s*(\d+)\)")   # in encoding/scenes_io.py

Legacy .scenes line format (write it byte-for-byte, do NOT reformat):
  f"scene {scene.index:4d}  frames [{scene.start_frame:8d}, "
  f"{scene.end_frame:8d})  {scene.start_sec:10.3f}s .. {scene.end_sec:10.3f}s"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_detect(args) in detection/pipeline.py</name>
  <files>src/enpipe/detection/pipeline.py, tests/unit/cli/test_run_detect_roundtrip.py</files>
  <behavior>
    - Given an argparse.Namespace with input/output/width/threshold/min_scene_len_frames/min_scene_len/no_qsv/qsv_device/jobs and detect_scenes monkeypatched to return synthetic Scene objects, run_detect writes an output file whose every line matches encoding.scenes_io._SCENE_RE.
    - read_scenes(run_detect's output path) returns exactly the (start_frame, end_frame) pairs of the synthetic Scene objects, in order (round-trip contract, D-04).
    - When args.output is None, the file is written to Path(str(args.input) + ".scenes") (e.g. movie.mkv -> movie.mkv.scenes).
    - min-scene-len precedence: frames given -> (frames, 3.0); else seconds given -> (None, seconds); else default (72, 3.0) — matching legacy:666-672 exactly.
    - --no-qsv maps to DetectionConfig(use_qsv=False); absence maps to use_qsv=True.
  </behavior>
  <action>Create src/enpipe/detection/pipeline.py exporting run_detect(args) -> None, migrated VERBATIM (minus the argparse block) from legacy/scene_detection.py:666-692 per D-02, using the already-migrated enpipe.detection.detect.detect_scenes and enpipe.detection.config.DetectionConfig. Start the module with `from __future__ import annotations` and a substantial Russian design-rationale docstring in the style of encoding/pipeline.py — state it is "перенесено дословно из __main__ (legacy/scene_detection.py:647-692) минус argparse-блок -> run_detect(args) (D-02), симметрично run_encode(args)". Preserve the min-scene-len precedence branch, the DetectionConfig construction, the `out_path = args.output or Path(str(args.input) + ".scenes")` default, the exact `.scenes` line format string from <interfaces>, and the trailing `print(f"{len(scenes)} сцен -> {out_path}")`. CRITICAL asymmetry to preserve per D-09 / RESEARCH Pattern 1: do NOT add a shutil.which tool-preflight check — the legacy detect __main__ never had one (unlike run_encode), and adding it would be a behavior change. Add a one-line "САНКЦИОНИРОВАННОЕ ОТКЛОНЕНИЕ" note to run_detect's docstring (mirroring run_encode's docstring style, qwen M1) documenting that the shutil.which tool-preflight is INTENTIONALLY absent here — the legacy detect __main__ never had one (D-09/legacy parity), so a missing ffmpeg surfaces as a cryptic FileNotFoundError exactly as legacy does, NOT a clean die(). Use `typing`-module generics for ALL annotations (e.g. `Optional[List[str]]`), NOT PEP 604 (`list[str] | None`). Write tests/unit/cli/test_run_detect_roundtrip.py exercising the <behavior> cases: monkeypatch enpipe.detection.pipeline.detect_scenes to return a fixed list of Scene objects, call run_detect with a Namespace pointing at a tmp_path input, then assert every written line matches enpipe.encoding.scenes_io._SCENE_RE and that read_scenes(out) equals the synthetic pairs.</action>
  <verify>
    <automated>uv run pytest tests/unit/cli/test_run_detect_roundtrip.py -x && ! grep -Eq " \| None| list\[| tuple\[|:list\[|:tuple\[" src/enpipe/detection/pipeline.py</automated>
  </verify>
  <done>run_detect exists and is importable; the round-trip test passes; detect_scenes is not invoked for real (monkeypatched); no shutil.which preflight was added to run_detect and its docstring carries the one-line sanctioned-deviation note; detection/pipeline.py uses typing-module generics only (grep gate passes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cli/main.py argparse dispatcher + cli package</name>
  <files>src/enpipe/cli/__init__.py, src/enpipe/cli/main.py, tests/unit/cli/test_cli_dispatch.py</files>
  <behavior>
    - build_parser() returns an ArgumentParser with prog="enpipe" and two subparsers ("detect", "encode"); parse of `detect video.mkv` yields a Namespace whose func is run_detect and whose input is Path("video.mkv").
    - parse of `encode video.mkv video.mkv.scenes` yields a Namespace whose func is run_encode with video/scenes populated and dest names frm/to/out matching run_encode's expected attributes.
    - detect --jobs default is the hardcoded int 4; encode --jobs default equals encoding.pipeline.JOBS (env-var-derived) — the two asymmetric defaulting mechanisms are preserved (RESEARCH Pattern 3 asymmetry 1). EXPLICITLY (qwen M4): parsing `detect video.mkv` with NO --jobs yields a Namespace whose jobs == 4; parsing `encode video.mkv video.mkv.scenes` with NO --jobs yields a Namespace whose jobs == encoding.pipeline.JOBS — assert BOTH defaults directly, not merely that --jobs accepts a supplied value.
    - detect's scenes-file flag is -o/--output (dest "output"); encode's is -o/--out (dest "out") — both preserved distinctly (asymmetry 2).
    - main(argv) parses argv and calls args.func(args); with run_detect and run_encode monkeypatched, invoking main(["detect","x.mkv"]) calls the detect stub exactly once with the parsed Namespace, and main(["encode","x.mkv","x.mkv.scenes"]) calls the encode stub once.
    - Invoking with no subcommand errors (subparsers required=True).
    - cli/main.py uses only `typing`-module generics — no ` | None`, bare `list[`, or `tuple[` annotations anywhere.
  </behavior>
  <action>Create src/enpipe/cli/__init__.py (empty package marker with a one-line Russian docstring) and src/enpipe/cli/main.py per D-01 — a SINGLE dispatcher file (do NOT create cli/app.py, cli/detect.py, or cli/encode.py; the 3-file split from the pre-phase ARCHITECTURE.md is superseded by D-01 — RESEARCH Pitfall 1). Use `typing`-module generics for ALL annotations (e.g. `Optional[List[str]]`, `Optional[Sequence[str]]`), NOT PEP 604 (`list[str] | None`) — the RESEARCH.md `main(argv)` example shows the FORBIDDEN PEP 604 form (`Sequence[str] | None`), but CLAUDE.md/CONVENTIONS.md require typing-module generics; translate the RESEARCH signature into the typing form. main.py starts with `from __future__ import annotations`, a Russian module docstring stating it is pure dispatch with all logic in detection.pipeline.run_detect / encoding.pipeline.run_encode (D-01/D-09), then imports run_detect, run_encode, and JOBS (as ENCODE_JOBS) from encoding.pipeline. Implement build_parser() -> argparse.ArgumentParser: prog="enpipe", add_subparsers(dest="command", required=True). Reconstitute the detect subparser flags EXACTLY as legacy/scene_detection.py:650-663 (input positional type=Path; -o/--output dest output; --width int default 320; --threshold float default 3.0; --min-scene-len-frames int default None; --min-scene-len float default None; --no-qsv store_true; --qsv-device default None; --jobs int default 4) with Russian help text preserved verbatim, then detect_p.set_defaults(func=run_detect). Reconstitute the encode subparser flags to match run_encode's Namespace attributes per D-03 (video positional type=Path; scenes positional type=Path; -o/--out dest out default None; --from dest frm int default 0; --to dest to int default None; --workdir type=Path default None; --keep store_true; --jobs int default ENCODE_JOBS; --no-audio store_true; --no-metrics store_true; --csv type=Path default None) with Russian help, then encode_p.set_defaults(func=run_encode). Implement main(argv=None) -> None that builds the parser, parses argv, and calls args.func(args) — NO orchestration logic in main (Anti-Pattern: fused runtime). Write tests/unit/cli/test_cli_dispatch.py covering all <behavior> cases; monkeypatch enpipe.cli.main.run_detect / run_encode with capturing stubs to assert dispatch without invoking real stages. Include EXPLICIT assertions that the DEFAULT --jobs (flag omitted) parses to 4 for detect and to encoding.pipeline.JOBS for encode (qwen M4) — import JOBS from encoding.pipeline and compare, do not hardcode the encode default.</action>
  <verify>
    <automated>uv run pytest tests/unit/cli/test_cli_dispatch.py -x && ! grep -Eq " \| None| list\[| tuple\[|:list\[|:tuple\[" src/enpipe/cli/main.py</automated>
  </verify>
  <done>build_parser and main exist; both subparsers parse to the correct func and dest names; the two flag-default asymmetries are preserved and the unspecified-`--jobs` defaults (detect=4, encode=encoding.pipeline.JOBS) are asserted explicitly; dispatch calls the correct stub; no fused orchestration in main; cli/main.py uses typing-module generics only — no ` | None`, bare `list[`, or `tuple[` annotations (grep gate passes).</done>
</task>

<task type="auto">
  <name>Task 3: [project.scripts] wiring + console-script smoke + legacy-untouched guard</name>
  <files>pyproject.toml</files>
  <action>In pyproject.toml, replace the commented placeholder at lines 15-16 with a real `[project.scripts]` table containing `enpipe = "enpipe.cli.main:main"` per D-01 (the reserved slot). Remove the stale comment referencing the superseded `enpipe.cli.app:main` / `cli/detect.py + cli/encode.py` 3-file layout. Reinstall so the console script is materialized (`uv sync --locked` if the lockfile still resolves, else fall through to `uv pip install -e .` if `uv sync` fails — the executor must NOT abort on a stale lockfile, opencode L1). Do NOT modify any file under legacy/ — this phase's files_modified list contains zero legacy paths, and legacy remains the parity oracle (D-09, SC4).</action>
  <verify>
    <automated>uv sync 2>/dev/null || uv pip install -e . ; uv run enpipe --help >/dev/null && uv run enpipe detect --help | grep -q "Детектирование" && uv run enpipe encode --help | grep -q "AV1" && uv run python legacy/scene_detection.py --help >/dev/null && uv run python legacy/encode_scenes.py --help >/dev/null && echo WIRED_OK</automated>
  </verify>
  <done>`enpipe`, `enpipe detect --help`, and `enpipe encode --help` all exit 0 with Russian help text — this `uv run enpipe ...` path exercises the `[project.scripts]` console_script entry end-to-end via a real subprocess (NOT in-process main()), satisfying the console-script smoke that 04-02's in-process pipeline calls do not cover (qwen M2 / opencode L4). `[project.scripts] enpipe = "enpipe.cli.main:main"` is present; BOTH legacy scripts (scene_detection.py AND encode_scenes.py) still run their own --help (SC4/D-04 symmetric check); no files under legacy/ were modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → CLI (argv) | Operator-supplied argv + file paths cross into `enpipe`; already-existing trust surface, no network/auth |
| detect stage → encode stage | The `<video>.scenes` text file is the only handoff artifact between the two stages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | cli/main.py + run_detect argv/path handling | mitigate | Keep argparse `type=Path/int/float` coercion as the (sufficient) validation layer, unchanged from legacy; run_detect adds NO string-interpolated shell — all subprocess flow stays list-form via the existing shared.proc seam (no shell=True introduced) |
| T-04-02 | Tampering / Integrity | run_detect `.scenes` writer ↔ read_scenes | mitigate | Write the legacy line format byte-for-byte so `_SCENE_RE` round-trips; a reformatted line fails SILENTLY (skipped, not errored) and would desync the two stages — Task 1's round-trip test is the guard |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — stdlib `argparse` only, `[project.scripts]` adds no dependency; RESEARCH Package Legitimacy Audit confirms "Not applicable" |
</threat_model>

<verification>
- `uv run pytest -m "not hardware"` stays green (new fast tests included, no hardware invoked).
- `enpipe detect --help` and `enpipe encode --help` exit 0 with Russian help via the real console_script subprocess.
- run_detect's output round-trips through read_scenes to identical (start,end) pairs.
- Unspecified `--jobs` defaults (detect=4, encode=encoding.pipeline.JOBS) are asserted in the dispatch test.
- Both legacy scripts (scene_detection.py + encode_scenes.py) still run their own --help; no file under legacy/ is modified by this plan.
</verification>

<success_criteria>
- PKG-01 satisfied: `enpipe detect`/`enpipe encode` are thin argv-compatible dispatchers; the `<video>.scenes` handoff is preserved and round-trip-tested; the `enpipe` console_script installs and is subprocess-smoke-tested.
- run_detect mirrors run_encode with zero added preflight/behavior change (D-09), with the intentionally-absent preflight documented as a sanctioned deviation.
- Both legacy scripts untouched and runnable (SC4).
</success_criteria>

<output>
Create `.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-01-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

- **D-01** (single `cli/{__init__,main.py}` argparse dispatcher + `[project.scripts] enpipe = "enpipe.cli.main:main"`): Tasks 2 & 3. NOT the superseded 3-file split.
- **D-02** (migrate `.scenes`-writing `__main__` logic into `run_detect(args)` mirroring `run_encode`): Task 1.
- **D-03** (reconstitute exact legacy flag surfaces argv-compatibly, Russian help, correct dest names): Task 2.
- **D-04** (preserve the two-stage `<video>.scenes` handoff; no fused orchestrator): Task 1 (round-trip) + Task 2 (dispatch, no fusion).
- **D-09** (zero behavior change to detect/encode; legacy untouched; conventions verbatim): Task 1 (no added preflight, documented as sanctioned deviation), Task 2 (asymmetries + explicit default-jobs preserved), Task 3 (no legacy edits, both legacy scripts exercised, console_script subprocess-smoked).
</output>
