---
phase: 04-unified-cli-hardware-gated-real-media-validation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/integration/test_hardware_real_media.py
  - tests/fixtures/media/README.md
  - .gitignore
  - .github/workflows/hardware-integration.yml
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "A `hardware`-marked integration test runs the full `enpipe detect` → `enpipe encode` → mux pipeline on real Arc and is excluded from the default `pytest -m \"not hardware\"` tier"
    - "The test self-skips cleanly (no failure) when /dev/dri/renderD128 or qsvencc is absent"
    - "SDR and synthetic HDR10 sources are generated and validated end-to-end on real hardware (not skipped in this devcontainer)"
    - "Per-chunk and total frame counts are verified INDEPENDENTLY (ffprobe on --keep chunks vs the .scenes pairs), not by trusting the pipeline's own count_frames self-check"
    - "Chunk seek points are cross-validated against source keyframes via an independent keyframe_table_ffprobe scan"
    - "HDR10+/DV cases are fixture-gated: present → validated; absent → clean skip with a how-to-supply message; a pass is NEVER faked"
    - "DV RPU survival is checked via read-only ffprobe side-data — NEVER the dovi_rpu bsf (fabricates) nor dovi_tool extract-rpu (fails on AV1)"
  artifacts:
    - path: "tests/integration/test_hardware_real_media.py"
      provides: "TEST-04 hardware-gated end-to-end validation (SDR/HDR10 live, HDR10+/DV fixture-gated)"
      contains: "pytest.mark.hardware"
      min_lines: 120
    - path: "tests/fixtures/media/README.md"
      provides: "documented fixtures location + how to supply hdr10plus.mkv / dv.mkv"
    - path: ".gitignore"
      provides: "gitignore for operator-supplied non-redistributable media fixtures"
      contains: "tests/fixtures/media"
    - path: ".github/workflows/hardware-integration.yml"
      provides: "D-08 self-hosted-Arc CI stub (workflow_dispatch only)"
      contains: "workflow_dispatch"
  key_links:
    - from: "tests/integration/test_hardware_real_media.py"
      to: "enpipe.cli.main"
      via: "invokes the enpipe CLI end-to-end (detect → encode)"
      pattern: "enpipe (detect|encode)|cli.main"
    - from: "tests/integration/test_hardware_real_media.py"
      to: "enpipe.encoding.keyframes.keyframe_table_ffprobe"
      via: "independent ground-truth keyframe cross-validation"
      pattern: "keyframe_table_ffprobe"
    - from: "tests/integration/test_hardware_real_media.py"
      to: "ffprobe side_data_list"
      via: "read-only DV RPU per-frame parity (no -bsf:v)"
      pattern: "side_data_list"
---

<objective>
Build TEST-04: a `hardware`-marked integration test that runs the FULL pipeline end-to-end via the `enpipe` CLI (detect → encode → mux) on real Intel Arc hardware and independently verifies the load-bearing correctness invariants — per-chunk + total frame counts, keyframe alignment, and (fixture-gated) DV RPU survival. SDR and synthetic HDR10 sources are generated in-test and genuinely run on this devcontainer's Arc GPU; HDR10+/DV are fixture-gated with honest clean skips. The DV RPU check uses read-only ffprobe side-data inspection, never the fabricating `dovi_rpu` bsf.

Purpose: Close TEST-04 and the milestone's "never run on real video" gap — with honest coverage scoping (no coverage theater) and independent verification (no re-checking the pipeline's own arithmetic).
Output: `tests/integration/test_hardware_real_media.py`, `tests/fixtures/media/README.md`, a `.gitignore` entry for operator media, and the optional D-08 self-hosted-runner CI stub.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-CONTEXT.md
@.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-RESEARCH.md

<read_first>
- scratch/parity_encode.py — the proven hardware-gate pattern (_hardware_available() probing /dev/dri/renderD128 + qsvencc), synthetic-sample ffmpeg recipe, video-only-sample seek caveat, and --keep/frame-count comparison approach. MODEL for the harness (but this is a real pytest test, not a scratch script).
- tests/integration/test_parallel_regression.py — the existing multi_scene_clip fixture pattern (four ~55s color=/smptebars= lavfi segments via filter_complex concat=n=4) that reliably produces multiple AdaptiveDetector scene cuts; reuse it so TEST-04 exercises MULTIPLE chunks/keyframe points.
- src/enpipe/encoding/keyframes.py — keyframe_table_ffprobe(src, fps) (slow ground-truth path; NOT keyframe_table which may take the fast EBML path).
- src/enpipe/encoding/chunk.py — count_frames(path) (ffprobe packet count).
- src/enpipe/encoding/pipeline.py — probe_fps(src); run_encode(args) attribute names.
- src/enpipe/encoding/hdr.py — detect_hdr(src) classification.
- src/enpipe/encoding/scenes_io.py — read_scenes(path).
- 04-RESEARCH.md §"DV RPU Verification Mechanism", §"Synthetic Fixture Generation", §"Code Examples", §"Common Pitfalls" (Pitfalls 2/3/4) — the empirically-verified recipes and the DO-NOT list.
- src/enpipe/cli/main.py (from Plan 04-01) — the entry point the test drives end-to-end.
</read_first>

<interfaces>
<!-- Contracts the executor needs. Use directly, no exploration. -->
_hardware_available() -> bool  # Path("/dev/dri/renderD128").exists() and shutil.which("qsvencc")
keyframe_table_ffprobe(src: Path, fps: float) -> List[Tuple[int, float]]   # ground-truth, slow
keyframe_table(src, fps)   # DO NOT use for verification — may take fast EBML path
count_frames(path: Path) -> int
probe_fps(src: Path) -> float
detect_hdr(src: Path) -> List[str]   # [] for SDR; ['--master-display','copy','--max-cll','copy'] for HDR10
read_scenes(path: Path) -> List[Tuple[int,int]]
run_encode(args) Namespace attrs: video, scenes, out, frm, to, workdir, keep, jobs, no_audio, no_metrics, csv

# HDR10 synthetic recipe (VERIFIED end-to-end on this Arc in RESEARCH):
#   ffmpeg -f lavfi -i testsrc... -pix_fmt yuv420p10le -color_primaries bt2020
#   -color_trc smpte2084 -colorspace bt2020nc -c:v libx265 -x265-params
#   "hdr10=1:hdr10-opt=1:repeat-headers=1:colorprim=bt2020:transfer=smpte2084:
#    colormatrix=bt2020nc:master-display=G(13250,34500)B(7500,3000)R(34000,16000)
#    WP(15635,16450)L(10000000,1):max-cll=1000,400"
# SDR recipe: libx264 / -pix_fmt yuv420p / bt709 (detect_hdr -> [])
# DV RPU read-only probe (NO -bsf:v):
#   ffprobe -v error -select_streams v:0 -show_frames
#     -show_entries frame=side_data_list -of json <file>
# assert every frame carries a "Dolby Vision RPU Data" side_data_type entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: hardware gate + SDR end-to-end + independent frame-count/keyframe-alignment verification</name>
  <files>tests/integration/test_hardware_real_media.py</files>
  <action>Create tests/integration/test_hardware_real_media.py with module-level `pytestmark = pytest.mark.hardware` and an autouse module-scoped fixture `_require_hardware` that calls `_hardware_available()` (Path("/dev/dri/renderD128").exists() and shutil.which("qsvencc")) and `pytest.skip("no Arc hardware ...")` when absent — mirroring scratch/parity_encode.py exactly. Add a SHARED synthetic-clip helper `_make_multiscene_clip(dst, *, codec_args)` that reuses the four-segment lavfi concat pattern from tests/integration/test_parallel_regression.py (four ~55s color=/smptebars= segments via filter_complex concat=n=4) so multiple genuine scene cuts occur (multiple chunks → multiple keyframe-alignment points); it is parametrized by the codec/pix_fmt/color arguments so Task 1b can reuse it for HDR10. Add a SHARED independent-verification helper `_verify_frame_counts_and_keyframes(src, workdir, scenes_path, final_mkv)` implementing (per RESEARCH Pitfall 4 — do NOT trust the CLI exit code or the pipeline's own count_frames): (a) parse scenes_path with read_scenes (independent re-parse), and for each chunk_{i:05d}.obu in workdir assert count_frames(chunk) == (end - start) for that scene, and assert the summed chunk frames == count_frames(final_mkv) == sum of scene spans; (b) keyframe alignment — compute fps via probe_fps(src), call keyframe_table_ffprobe(src, fps) (ground-truth slow path, NOT keyframe_table), collect the set of keyframe frame numbers, and for each scene start `s` assert max({f for f in kf_frames if f <= s}) exists and is a real keyframe (each chunk's seek point lands on a source keyframe). Then write test_sdr: generate an SDR clip via `_make_multiscene_clip` (libx264 / -pix_fmt yuv420p / bt709), assert detect_hdr(sdr) == [] (sanity), run the FULL pipeline via the enpipe CLI end-to-end — invoke enpipe.cli.main.main(["detect", str(src), "--jobs", "2"]) then main(["encode", str(src), str(scenes), "-o", str(out), "--workdir", str(wd), "--keep", "--no-audio", "--jobs", "2"]) (--keep so per-chunk .obu files survive; --no-audio to avoid the lavfi audio/seek artifact documented in scratch/parity_encode.py) — then call `_verify_frame_counts_and_keyframes(src, wd, scenes, out)`. Keep Russian only where it matches convention; test code identifiers stay English.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "sdr" -x -rs</automated>
  </verify>
  <done>On this Arc devcontainer test_sdr actually RUNS (not skipped) and passes: independent per-chunk + total frame counts match and every chunk seek lands on a ground-truth source keyframe. On non-Arc environments the module skips cleanly. The `_make_multiscene_clip` and `_verify_frame_counts_and_keyframes` helpers exist and are reusable by Task 1b. The test drives the enpipe CLI end-to-end (not run_encode directly).</done>
</task>

<task type="auto">
  <name>Task 1b: synthetic HDR10 end-to-end + HDR10-metadata-survival verification</name>
  <files>tests/integration/test_hardware_real_media.py</files>
  <action>Extend tests/integration/test_hardware_real_media.py with test_hdr10, reusing the `_make_multiscene_clip` and `_verify_frame_counts_and_keyframes` helpers from Task 1a. Generate an HDR10 clip by calling `_make_multiscene_clip` with the HDR10 codec args from <interfaces> (libx265 / -pix_fmt yuv420p10le with the exact bt2020/smpte2084 + master-display/max-cll x265-params — HDR signaling is stream-level and constant across scenes per RESEARCH, so it is applied to the single encode pass over the concatenated segments). Assert detect_hdr(hdr10) classifies HDR10 (contains '--master-display') as a sanity check. Run the FULL pipeline via the enpipe CLI end-to-end exactly as test_sdr does: main(["detect", str(src), "--jobs", "2"]) then main(["encode", str(src), str(scenes), "-o", str(out), "--workdir", str(wd), "--keep", "--no-audio", "--jobs", "2"]). Then call `_verify_frame_counts_and_keyframes(src, wd, scenes, out)` for the per-chunk/total frame-count + keyframe-alignment invariants, AND additionally assert HDR10 metadata SURVIVES to the final .mkv: run ffprobe on the final output and assert its frame side_data_list still reports Mastering display metadata / Content light level. Keep test code identifiers English.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "hdr10 and not hdr10plus" -x -rs</automated>
  </verify>
  <done>On this Arc devcontainer test_hdr10 actually RUNS (not skipped) and passes: independent per-chunk + total frame counts match, every chunk seek lands on a source keyframe, and HDR10 Mastering-display / max-CLL metadata survives to the final .mkv. Reuses the Task 1a helpers (no duplicated verification logic).</done>
</task>

<task type="auto">
  <name>Task 2: fixture-gated HDR10+/DV cases + ffprobe-native DV RPU parity + fixtures README/gitignore</name>
  <files>tests/integration/test_hardware_real_media.py, tests/fixtures/media/README.md, .gitignore</files>
  <action>Extend tests/integration/test_hardware_real_media.py with the fixture-gating layer per D-06/D-07. Define FIXTURES_DIR = Path(os.environ.get("ENPIPE_TEST_MEDIA", <repo>/tests/fixtures/media)) and a `_fixture(name)` helper returning the path if it is_file() else None. Add a DV-mechanism SELF-CHECK helper that asserts the installed ffmpeg parses AV1 DOVI OBU — run the read-only `ffmpeg -h bsf=dovi_rpu` and assert "av1" appears in its Supported codecs line; if not, skip the DV case with a message that the toolchain lacks AV1 DOVI parsing (per RESEARCH Assumption A2 — fail loud, never silently under-report). Add a read-only `_dv_rpu_frame_count(path) -> (with_rpu, total)` helper that runs `ffprobe -v error -select_streams v:0 -show_frames -show_entries frame=side_data_list -of json` (NO -bsf:v) and counts frames whose side_data_list contains a "Dolby Vision RPU Data" side_data_type. Add test_hdr10plus and test_dv cases: each calls `_fixture("hdr10plus.mkv")` / `_fixture("dv.mkv")`, and if None does `pytest.skip(<explanatory message pointing at tests/fixtures/media/README.md and $ENPIPE_TEST_MEDIA, stating genuine HDR10+/DV cannot be synthesized (D-06) and must be operator-supplied — this is NOT a failure>)`. When a fixture IS present: run the same end-to-end enpipe CLI pipeline (detect → encode --keep --no-audio) on it, then for DV run the AV1-DOVI self-check, then assert `_dv_rpu_frame_count(final_mkv)` has with_rpu == total > 0 (RPU on EVERY frame — a gap means the splice/mux dropped RPU) AND run the same parity on the pre-mux .obu chunks too (RESEARCH Assumption A3 — mkvmerge DV-side-data path is distinct from HDR10). CRITICAL false-confidence guard (D-07): the DV survival check MUST use ONLY read-only ffprobe side-data inspection (side_data_list) of AV1 DOVI OBUs. Do NOT `dovi_tool extract-rpu` (broken on AV1 in 2.3.2), do NOT pass the mutating dovi_rpu bitstream filter to any verification/output command (it FABRICATES a fake DOVI record — RESEARCH Pitfalls 2/3), and do NOT transcode AV1→HEVC. Additionally, to keep the CI forbidden-usage grep gate unambiguous, do NOT write the literal tokens `-bsf:v dovi_rpu`, `extract-rpu`, or any non-self-check `bsf=dovi_rpu` ANYWHERE in the file — not even inside comments; express the DO-NOT rules in prose (e.g. "the dovi_rpu bitstream filter fabricates RPU and must never be used to verify") WITHOUT the literal flag strings. The ONLY permitted occurrence of the token `bsf=dovi_rpu` in the whole file is the read-only `ffmpeg -h bsf=dovi_rpu` AV1-support self-check. Create tests/fixtures/media/README.md documenting: the expected filenames (hdr10plus.mkv, dv.mkv), that they are operator-supplied and gitignored (copyright), the $ENPIPE_TEST_MEDIA override, and why they cannot be synthesized (D-06). Add a `.gitignore` block ignoring operator media under `tests/fixtures/media/` (e.g. `tests/fixtures/media/*` + `!tests/fixtures/media/README.md`) so README.md stays tracked while *.mkv/*.obu stay untracked.</action>
  <verify>
    <automated>bash -c 'set -e
uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "hdr10plus or dv" -rs 2>&1 | grep -Eq "skipped|passed"
grep -Rq "ENPIPE_TEST_MEDIA" tests/fixtures/media/README.md
grep -q "tests/fixtures/media" .gitignore
grep -q "side_data_list" tests/integration/test_hardware_real_media.py
if grep -Eq "extract-rpu|-bsf:v[[:space:]]*dovi_rpu" tests/integration/test_hardware_real_media.py; then echo "GATE FAIL: forbidden mutating dovi_rpu write / extract-rpu present"; exit 1; fi
if grep -n "bsf=dovi_rpu" tests/integration/test_hardware_real_media.py | grep -vq -- "-h bsf=dovi_rpu"; then echo "GATE FAIL: non-self-check bsf=dovi_rpu present"; exit 1; fi
echo GATE_OK'</automated>
  </verify>
  <done>Absent fixtures → HDR10+/DV cases skip cleanly with an explanatory how-to-supply message (never a faked pass). The DV check uses only read-only ffprobe side-data (side_data_list) of AV1 DOVI OBUs; the ONLY dovi_rpu token in the file is the read-only `ffmpeg -h bsf=dovi_rpu` self-check. The gate command CANNOT print GATE_OK / exit 0 if `extract-rpu`, a mutating `-bsf:v dovi_rpu`, or any non-self-check `bsf=dovi_rpu` appears in the test file — each forbidden pattern forces `exit 1` via an explicit `if` guard (no `&&`/`||` precedence bypass). README.md and the gitignore entry exist; operator media stays untracked.</done>
</task>

<task type="auto">
  <name>Task 3: D-08 self-hosted-Arc CI stub (workflow_dispatch only)</name>
  <files>.github/workflows/hardware-integration.yml</files>
  <action>Create .github/workflows/hardware-integration.yml per D-08 — a distinct, NEVER-auto-triggered GitHub Actions job giving the `hardware` tier a named home without letting hosted-CI green be mistaken for hardware validation. Trigger ON `workflow_dispatch` ONLY (no push, no pull_request — RESEARCH Security: a self-hosted runner with /dev/dri must never execute untrusted fork PR code; Elevation-of-Privilege mitigation). Add a top comment block stating: this requires a self-hosted Intel Arc runner (labels e.g. [self-hosted, arc]); it is intentionally decoupled from ci.yml's hosted cpu-fallback tier; hosted CI green ≠ hardware validation. The job runs on the self-hosted label, does `uv sync --locked`, and runs `uv run pytest -m hardware -rs` (the ONLY workflow that selects the hardware marker). Keep it a documented stub: it will only ever run when a maintainer manually dispatches it on a machine with real Arc hardware.</action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/hardware-integration.yml')); t=d.get(True) or d.get('on'); assert 'workflow_dispatch' in t and 'push' not in t and 'pull_request' not in t, t; print('DISPATCH_ONLY_OK')"</automated>
  </verify>
  <done>hardware-integration.yml exists, triggers on workflow_dispatch ONLY (no push/pull_request), targets a self-hosted Arc runner label, runs `pytest -m hardware`, and documents that hosted-CI green is not hardware validation. ci.yml is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real Arc GPU | The hardware test drives qsvencc/ffmpeg subprocesses on /dev/dri; correctness (not security) is the risk surface |
| CI trigger → self-hosted runner | A self-hosted runner with /dev/dri passthrough must not execute untrusted (fork PR) code |
| verification tooling → bitstream | The DV verification tool must not itself mutate/fabricate the metadata it reports on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Repudiation / false-confidence | DV RPU verification step | mitigate | Use READ-ONLY `ffprobe -show_entries frame=side_data_list` (no bitstream-filter write); NEVER the mutating `dovi_rpu` bsf (empirically fabricates a fake DOVI record on non-DV content) nor `dovi_tool extract-rpu` (fails `Invalid input file type` on AV1). Add a read-only `ffmpeg -h bsf=dovi_rpu` self-check asserting `av1` support so a toolchain downgrade fails loud, not silent. Task 2's CI gate is an explicit `if`/grep guard that exits 1 on any forbidden write-usage token (no `&&`/`||` precedence bypass) |
| T-04-04 | Information disclosure / false-confidence | test coverage claims (HDR10+/DV) | mitigate | Fixture-gate HDR10+/DV: absent → `pytest.skip` with how-to-supply message; NEVER fake a pass; document the coverage boundary in README.md (D-06 honesty requirement) |
| T-04-05 | Elevation of Privilege | D-08 self-hosted Arc runner | mitigate | Workflow triggers on `workflow_dispatch` ONLY — no `push`/`pull_request`; never runs untrusted fork code with `/dev/dri` access |
| T-04-06 | Integrity | frame-count / keyframe verification | mitigate | Verify INDEPENDENTLY (ffprobe on `--keep` chunks vs independently-parsed `.scenes`; `keyframe_table_ffprobe` ground-truth path) — do not re-check the pipeline's own `count_frames`/EBML fast-path arithmetic (RESEARCH Pitfall 4/6) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — all tools (ffmpeg/qsvencc/mkvmerge/dovi_tool) are pre-provisioned system binaries; RESEARCH Package Legitimacy Audit: "Not applicable" |
</threat_model>

<verification>
- `uv run pytest -m "not hardware"` is UNAFFECTED (the new test is marker-excluded from the default tier).
- On this Arc devcontainer: `pytest -m hardware -k "sdr"` and `pytest -m hardware -k "hdr10 and not hdr10plus"` actually run and pass.
- HDR10+/DV cases skip cleanly (no fixtures) with explanatory messages, or run + pass when fixtures are supplied.
- No verification path invokes `dovi_tool extract-rpu` or the mutating dovi_rpu bitstream filter; the Task 2 gate exits 1 on any such token.
- hardware-integration.yml triggers on workflow_dispatch only.
</verification>

<success_criteria>
- TEST-04 satisfied: a hardware-gated end-to-end (detect→encode→mux via the `enpipe` CLI) test independently verifies per-chunk + total frame counts, keyframe alignment, and DV RPU survival; excluded from default CI.
- SDR + synthetic HDR10 validated live on real Arc; HDR10+/DV fixture-gated with honest clean skips (D-06).
- DV RPU checked via ffprobe-native side-data only (D-07); coverage boundary documented; no coverage theater.
- Hardware tier has a named self-hosted CI home (D-08) that can never be confused with hosted-CI green.
</success_criteria>

<output>
Create `.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-02-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

- **D-05** (hardware-marked full detect→encode→mux test verifying per-chunk/total frame counts, keyframe alignment, DV RPU survival): Task 1a (frame counts + keyframe alignment, live SDR) + Task 1b (same invariants, live HDR10) + Task 2 (DV RPU).
- **D-06** (SDR/HDR10 synthesized + validated; HDR10+/DV fixture-gated, clean skip, never fake a pass, document boundary): Task 1a (SDR live) + Task 1b (HDR10 live) + Task 2 (fixture-gating + README).
- **D-07** (DV RPU mechanism: read-only ffprobe side-data; NOT dovi_tool extract-rpu, NOT the fabricating dovi_rpu bsf; self-check AV1 DOVI parsing; forbidden-usage CI grep gate): Task 2.
- **D-08** (optional self-hosted-Arc CI stub, never auto-triggered, hardware tier named home): Task 3.
- **D-09** (legacy untouched; conventions preserved): this plan modifies only tests/CI/fixtures — zero production/legacy edits.
</output>
