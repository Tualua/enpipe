---
phase: 04-unified-cli-hardware-gated-real-media-validation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/integration/test_hardware_real_media.py
  - tests/fixtures/media/README.md
  - .gitignore
  - .github/workflows/hardware-integration.yml
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "A `hardware`-marked integration test runs the full `enpipe detect` → `enpipe encode` → mux pipeline on real Arc and is excluded from the default `pytest -m \"not hardware\"` tier"
    - "The test self-skips cleanly (no failure) when /dev/dri/renderD128 or qsvencc is absent"
    - "SDR and synthetic HDR10 sources are generated and validated end-to-end on real hardware (not skipped in this devcontainer)"
    - "ALL hardware `enpipe encode` invocations pass --no-metrics — qsvencc --psnr/--ssim requires an OpenCL device unavailable on this Debian-trixie devcontainer (scratch/parity_encode.py:81-94 METRICS_UNAVAILABLE, rc=255); without it the hardware encode DETERMINISTICALLY FAILS. Metrics parsing is TEST-01's coverage, not TEST-04's"
    - "Per-chunk and total frame counts are verified INDEPENDENTLY (count_frames on --keep chunks vs independently re-parsed .scenes pairs); per-chunk count_frames(chunk)==e-s is the LOAD-BEARING trim-correctness invariant"
    - "Chunk seek keyframes are cross-validated FOR REAL: the pipeline's own compute_chunk_seek_trim/kf_before decision, fed an INDEPENDENT keyframe_table_ffprobe ground-truth set, is asserted to land on a member of that set (the earlier tautological `max({f<=s}) exists` check — frame 0 is always a keyframe so it could never fail — is removed)"
    - "On the SDR synthetic sample, enpipe encode is parity-checked against the frozen legacy/encode_scenes.py oracle on the same sample+scenes (frame-count parity; byte-identical pre-mux movie.obu when qsvencc is deterministic, frame-count fallback otherwise) — legacy remains the parity oracle (SC4)"
    - "HDR10+/DV cases are fixture-gated: present → validated; absent → clean skip with a how-to-supply message; a pass is NEVER faked"
    - "HDR10 mastering-display/max-CLL survival is checked via STREAM-level side data (stream_side_data_list / ffprobe -show_streams), not per-frame; per-frame frame=side_data_list is used ONLY for DV RPU"
    - "DV RPU survival is checked via read-only ffprobe side-data (frame=side_data_list) and asserted to equal the SOURCE fixture's RPU frame-count (survives splice/mux) — NEVER the dovi_rpu bsf (fabricates) nor dovi_tool extract-rpu (fails on AV1)"
  artifacts:
    - path: "tests/integration/test_hardware_real_media.py"
      provides: "TEST-04 hardware-gated end-to-end validation (SDR/HDR10 live + legacy-oracle parity, HDR10+/DV fixture-gated)"
      contains: "pytest.mark.hardware"
      min_lines: 160
    - path: "tests/fixtures/media/README.md"
      provides: "documented fixtures location + how to supply hdr10plus.mkv / dv.mkv"
    - path: ".gitignore"
      provides: "gitignore for operator-supplied non-redistributable media fixtures"
      contains: "tests/fixtures/media"
    - path: ".github/workflows/hardware-integration.yml"
      provides: "D-08 self-hosted-Arc CI stub (workflow_dispatch only)"
      contains: "workflow_dispatch"
  key_links:
    - from: "tests/integration/test_hardware_real_media.py"
      to: "enpipe.cli.main"
      via: "invokes the enpipe CLI end-to-end (detect → encode) via a SystemExit-safe wrapper"
      pattern: "enpipe (detect|encode)|cli.main"
    - from: "tests/integration/test_hardware_real_media.py"
      to: "enpipe.encoding.keyframes.compute_chunk_seek_trim"
      via: "non-tautological keyframe cross-validation against an independent keyframe_table_ffprobe ground truth"
      pattern: "compute_chunk_seek_trim|keyframe_table_ffprobe"
    - from: "tests/integration/test_hardware_real_media.py"
      to: "legacy/encode_scenes.py"
      via: "read-only subprocess execution of the frozen parity oracle (SC4)"
      pattern: "encode_scenes.py"
    - from: "tests/integration/test_hardware_real_media.py"
      to: "ffprobe side_data_list"
      via: "read-only DV RPU source-parity (frame=side_data_list) + HDR10 stream=side_data_list (no -bsf:v)"
      pattern: "side_data_list"
---

<objective>
Build TEST-04: a `hardware`-marked integration test that runs the FULL pipeline end-to-end via the `enpipe` CLI (detect → encode → mux) on real Intel Arc hardware and independently verifies the load-bearing correctness invariants — per-chunk + total frame counts, non-tautological keyframe alignment, legacy-oracle parity, and (fixture-gated) DV RPU survival. SDR and synthetic HDR10 sources are generated in-test and genuinely run on this devcontainer's Arc GPU; HDR10+/DV are fixture-gated with honest clean skips. Every hardware encode runs with --no-metrics (qsvencc --psnr/--ssim needs an OpenCL device this devcontainer lacks). The DV RPU check uses read-only ffprobe side-data inspection, never the fabricating `dovi_rpu` bsf.

Purpose: Close TEST-04 and the milestone's "never run on real video" gap — with honest coverage scoping (no coverage theater), independent verification (no re-checking the pipeline's own arithmetic), and an executable legacy-oracle parity check (SC4).
Output: `tests/integration/test_hardware_real_media.py`, `tests/fixtures/media/README.md`, a `.gitignore` entry for operator media, and the optional D-08 self-hosted-runner CI stub.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-CONTEXT.md
@.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-RESEARCH.md

<read_first>
- scratch/parity_encode.py — the proven hardware-gate pattern (_hardware_available() probing /dev/dri/renderD128 + qsvencc), the METRICS_UNAVAILABLE/--no-metrics precedent (lines 81-94), the DETERMINISM-AWARE legacy-vs-migrated movie.obu comparison (byte-identical when qsvencc deterministic, else frame-count parity — lines 199-234), synthetic-sample ffmpeg recipe, and the video-only-sample seek caveat. MODEL for the harness (but this is a real pytest test, not a scratch script).
- legacy/encode_scenes.py — the FROZEN parity oracle; run it read-only via subprocess (do NOT import, do NOT modify) for the SDR legacy-oracle parity check. Note its chunk workdir emits `chunk_{i:05d}.obu` + `movie.obu` under --keep, identical to run_encode.
- tests/integration/test_parallel_regression.py — the existing multi_scene_clip fixture pattern (four color=/smptebars= lavfi segments via filter_complex concat=n=4) that reliably produces multiple AdaptiveDetector scene cuts; reuse it so TEST-04 exercises MULTIPLE chunks/keyframe points.
- src/enpipe/encoding/keyframes.py — keyframe_table_ffprobe(src, fps) (slow ground-truth path; NOT keyframe_table which may take the fast EBML path), compute_chunk_seek_trim(table, s, e), kf_before(table, frame).
- src/enpipe/encoding/chunk.py — count_frames(path) (ffprobe packet count); chunk_command adds `--psnr --ssim` UNLESS metrics is False (i.e. unless --no-metrics), which is why --no-metrics is mandatory here.
- src/enpipe/encoding/pipeline.py — probe_fps(src); run_encode(args) attribute names; chunks named `chunk_{i:05d}.obu`, joined into `movie.obu`, in the workdir.
- src/enpipe/encoding/hdr.py — detect_hdr(src) classification.
- src/enpipe/encoding/scenes_io.py — read_scenes(path).
- 04-RESEARCH.md §"DV RPU Verification Mechanism", §"Synthetic Fixture Generation", §"Code Examples", §"Common Pitfalls" (Pitfalls 2/3/4) + Assumptions A2/A3 — the empirically-verified recipes and the DO-NOT list.
- src/enpipe/cli/main.py (from Plan 04-01) — the entry point the test drives end-to-end.
</read_first>

<interfaces>
<!-- Contracts the executor needs. Use directly, no exploration. -->
_hardware_available() -> bool  # Path("/dev/dri/renderD128").exists() and shutil.which("qsvencc")
keyframe_table_ffprobe(src: Path, fps: float) -> List[Tuple[int, float]]   # ground-truth, slow
keyframe_table(src, fps)   # DO NOT use for verification — may take fast EBML path
compute_chunk_seek_trim(table, s, e) -> Tuple[str, str]   # pipeline's OWN pure seek/trim fn
kf_before(table, frame) -> Tuple[int, float]              # (kf_frame, kf_time) — derives the seek keyframe
count_frames(path: Path) -> int
probe_fps(src: Path) -> float
detect_hdr(src: Path) -> List[str]   # [] for SDR; ['--master-display','copy','--max-cll','copy'] for HDR10
read_scenes(path: Path) -> List[Tuple[int,int]]
run_encode(args) Namespace attrs: video, scenes, out, frm, to, workdir, keep, jobs, no_audio, no_metrics, csv
# The CLI is driven end-to-end (detect → encode) — NEVER call run_encode directly here.
# EVERY `enpipe encode` invocation MUST include "--no-metrics" (see chunk.py: without it
# chunk_command appends --psnr --ssim → qsvencc rc=255 on this devcontainer, OpenCL ICD absent).

# HDR10 synthetic recipe (VERIFIED end-to-end on this Arc in RESEARCH), SHORTENED to ~40s
# (4×10s segments — still 3 cuts for keyframe coverage) + `-preset ultrafast` to avoid a
# software-x265 timeout:
#   ffmpeg -f lavfi -i testsrc... -pix_fmt yuv420p10le -color_primaries bt2020
#   -color_trc smpte2084 -colorspace bt2020nc -c:v libx265 -preset ultrafast -x265-params
#   "hdr10=1:hdr10-opt=1:repeat-headers=1:colorprim=bt2020:transfer=smpte2084:
#    colormatrix=bt2020nc:master-display=G(13250,34500)B(7500,3000)R(34000,16000)
#    WP(15635,16450)L(10000000,1):max-cll=1000,400"
#   Gate: SKIP test_hdr10 cleanly if `ffmpeg -hide_banner -encoders` lacks libx265 (\blibx265\b).
# SDR recipe: libx264 / -pix_fmt yuv420p / bt709 (detect_hdr -> [])
#
# HDR10 mastering-display/max-cll SURVIVAL probe — STREAM-level (NOT per-frame):
#   ffprobe -v error -select_streams v:0 -show_streams
#     -show_entries stream=side_data_list -of json <final.mkv>
#   (verify the exact key empirically on the produced HDR10 output — opencode M2).
# DV RPU read-only probe (NO -bsf:v) — PER-FRAME:
#   ffprobe -v error -select_streams v:0 -show_frames
#     -show_entries frame=side_data_list -of json <file>
#   count frames carrying a "Dolby Vision RPU Data" side_data_type; assert the OUTPUT count
#   equals the SOURCE fixture's count (survives splice/mux), not merely == total.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: hardware gate + SDR end-to-end + independent frame-count/NON-tautological keyframe verification</name>
  <files>tests/integration/test_hardware_real_media.py</files>
  <action>Create tests/integration/test_hardware_real_media.py with module-level `pytestmark = pytest.mark.hardware` and an autouse module-scoped fixture `_require_hardware` that calls `_hardware_available()` (Path("/dev/dri/renderD128").exists() and shutil.which("qsvencc")) and `pytest.skip("no Arc hardware ...")` when absent — mirroring scratch/parity_encode.py exactly. Add a top-of-file comment clarifying: source clips are generated via SOFTWARE encode (libx264/libx265); the hardware gate applies ONLY to the enpipe pipeline (QSV encode via qsvencc) — qwen H1. Add a SystemExit-safe CLI wrapper `_run_cli(argv)` (opencode L1/L5) that calls enpipe.cli.main.main(argv) inside try/except SystemExit and, on SystemExit (raised by die() on any pipeline failure), converts it to `pytest.fail(f"enpipe {argv[0]} failed: {exc}")` so a pipeline failure surfaces as a clean test failure, not an unhandled SystemExit. Add a SHARED synthetic-clip helper `_make_multiscene_clip(dst, *, codec_args, seg_dur)` reusing the four-segment lavfi concat pattern from tests/integration/test_parallel_regression.py (four color=/smptebars= segments via filter_complex concat=n=4) so multiple genuine scene cuts occur (multiple chunks → multiple keyframe points); it is parametrized by codec/pix_fmt/color args AND per-segment duration `seg_dur` so Task 1b can reuse it for a SHORT HDR10 clip. Add a SHARED independent-verification helper `_verify_frame_counts_and_keyframes(src, workdir, scenes_path, final_mkv)` implementing (per RESEARCH Pitfall 4 — do NOT trust the CLI exit code or the pipeline's own count_frames): (a) LOAD-BEARING trim correctness — parse scenes_path with read_scenes (independent re-parse), and for each chunk_{i:05d}.obu in workdir assert count_frames(chunk) == (e - s) for that scene, and assert the summed chunk frames == count_frames(final_mkv) == sum of scene spans; add a comment that per-chunk count_frames(chunk)==e-s is THE load-bearing trim-correctness invariant (a wrong seek/trim recovers the wrong span → frame-count mismatch). (b) NON-tautological keyframe alignment (opencode H2) — compute fps via probe_fps(src), call keyframe_table_ffprobe(src, fps) (ground-truth slow path, NOT keyframe_table) to build an INDEPENDENT set `kf_frames` of keyframe frame numbers; for each scene [s,e) exercise the pipeline's OWN pure decision by calling compute_chunk_seek_trim(gt_table, s, e) (from enpipe.encoding.keyframes) AND independently derive its chosen seek keyframe via kf_before(gt_table, s) -> (kf_frame, _), then assert kf_frame in kf_frames (the pipeline's kf_before decision lands on a REAL keyframe from the independent scan). DELETE the old tautological `max({f for f in kf_frames if f <= s}) exists` check — since keyframe_table_ffprobe guarantees frame 0 is a keyframe it can never fail. Then write test_sdr: generate an SDR clip via `_make_multiscene_clip` (libx264 / -pix_fmt yuv420p / bt709), assert detect_hdr(sdr) == [] (sanity), run the FULL pipeline via the enpipe CLI end-to-end using `_run_cli` — `_run_cli(["detect", str(src), "--jobs", "2"])` then `_run_cli(["encode", str(src), str(scenes), "-o", str(out), "--workdir", str(wd), "--keep", "--no-audio", "--no-metrics", "--jobs", "2"])` — the "--no-metrics" is MANDATORY (without it chunk_command appends --psnr --ssim → qsvencc rc=255, OpenCL absent; scratch/parity_encode.py:81-94), --keep so per-chunk .obu files survive, --no-audio to avoid the lavfi audio/seek artifact documented in scratch/parity_encode.py — then call `_verify_frame_counts_and_keyframes(src, wd, scenes, out)`. Keep Russian only where it matches convention; test code identifiers stay English.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "sdr and not legacy" -x -rs</automated>
  </verify>
  <done>On this Arc devcontainer test_sdr actually RUNS (not skipped) and passes: independent per-chunk + total frame counts match and every chunk seek keyframe is a member of the independent ground-truth keyframe set (non-tautological). The encode runs with --no-metrics. On non-Arc environments the module skips cleanly. The `_run_cli`, `_make_multiscene_clip(..., seg_dur=...)` and `_verify_frame_counts_and_keyframes` helpers exist and are reusable by Tasks 1b/1c/2. The test drives the enpipe CLI end-to-end (not run_encode directly) and a pipeline failure surfaces as a clean pytest failure, not an unhandled SystemExit.</done>
</task>

<task type="auto">
  <name>Task 1b: synthetic HDR10 end-to-end + STREAM-level HDR10-metadata-survival verification</name>
  <files>tests/integration/test_hardware_real_media.py</files>
  <action>Extend tests/integration/test_hardware_real_media.py with test_hdr10, reusing `_make_multiscene_clip`, `_run_cli`, and `_verify_frame_counts_and_keyframes` from Task 1a. FIRST add a `_encoder_available(name)` helper that runs `ffmpeg -hide_banner -encoders` and matches the encoder name with a word-boundary regex; at the top of test_hdr10 `pytest.skip(...)` cleanly if libx265 is absent (opencode M4 — symmetric with the hardware-gate philosophy; the self-hosted Arc runner's ffmpeg may lack it). Generate an HDR10 clip by calling `_make_multiscene_clip` with the HDR10 codec args from <interfaces> (libx265 / -pix_fmt yuv420p10le / `-preset ultrafast` + the exact bt2020/smpte2084 + master-display/max-cll x265-params) and a SHORT `seg_dur` (~10s → ~40s total, 4 segments = 3 cuts) to avoid a software-x265 timeout (qwen H2). HDR signaling is stream-level and constant across scenes per RESEARCH, so it is applied to the single encode pass over the concatenated segments. Assert detect_hdr(hdr10) classifies HDR10 (contains '--master-display') as a sanity check. Run the FULL pipeline via the enpipe CLI end-to-end exactly as test_sdr does (via `_run_cli`, with the MANDATORY --no-metrics): `_run_cli(["detect", str(src), "--jobs", "2"])` then `_run_cli(["encode", str(src), str(scenes), "-o", str(out), "--workdir", str(wd), "--keep", "--no-audio", "--no-metrics", "--jobs", "2"])`. Then call `_verify_frame_counts_and_keyframes(src, wd, scenes, out)` for the per-chunk/total frame-count + keyframe-alignment invariants, AND additionally assert HDR10 metadata SURVIVES to the final .mkv using STREAM-level side data (opencode M2): run `ffprobe -show_streams -show_entries stream=side_data_list` on the final output and assert its stream_side_data_list still reports Mastering display metadata / Content light level (mastering-display/max-cll are STREAM-level, NOT per-frame — per-frame frame=side_data_list is reserved for DV RPU in Task 2). Verify the exact side-data key empirically on the produced HDR10 output before asserting on it. Keep test code identifiers English.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "hdr10 and not hdr10plus" -x -rs</automated>
  </verify>
  <done>On this Arc devcontainer test_hdr10 actually RUNS (not skipped) and passes: independent per-chunk + total frame counts match, every chunk seek keyframe is in the independent ground-truth set, and HDR10 Mastering-display / max-CLL metadata survives to the final .mkv checked at STREAM level. Encode runs with --no-metrics; the HDR10 clip is short (~40s, ultrafast x265) so it does not time out; test_hdr10 SKIPS cleanly if libx265 is unavailable. Reuses the Task 1a helpers (no duplicated verification logic).</done>
</task>

<task type="auto">
  <name>Task 1c: legacy-oracle SDR parity (SC4 — legacy/encode_scenes.py remains the parity oracle)</name>
  <files>tests/integration/test_hardware_real_media.py</files>
  <action>Add test_sdr_legacy_oracle_parity to tests/integration/test_hardware_real_media.py (opencode M3 / SC4), reusing `_make_multiscene_clip`, `_run_cli`, and count_frames. Generate an SDR multiscene clip; run enpipe detect then enpipe encode via `_run_cli` with `--keep --no-audio --no-metrics --jobs 2` into a distinct workdir `wd_enpipe` → enpipe_out. Then run the FROZEN oracle legacy/encode_scenes.py via subprocess on the SAME sample and the SAME .scenes file the enpipe pipeline used (this isolates ENCODE parity from detect): `subprocess.run([sys.executable, str(REPO_ROOT/"legacy"/"encode_scenes.py"), str(src), str(scenes), "-o", str(legacy_out), "--workdir", str(wd_legacy), "--keep", "--no-audio", "--no-metrics", "--jobs", "2"], cwd=REPO_ROOT, capture_output=True, text=True)` and assert its returncode == 0 (both sides run --no-metrics because qsvencc --psnr/--ssim needs an OpenCL device unavailable on this devcontainer — scratch/parity_encode.py:81-94; run legacy ONLY read-only, NEVER modify anything under legacy/). PRIMARY assertion: count_frames(legacy_out) == count_frames(enpipe_out). Additionally apply scratch/parity_encode.py's determinism-aware PRE-MUX movie.obu comparison (lines 199-234): if wd_legacy/movie.obu and wd_enpipe/movie.obu are byte-identical → qsvencc is deterministic on this box (STRONG byte-parity gate holds); ELSE (HW AV1 is commonly non-deterministic) FALL BACK to asserting count_frames(wd_legacy/movie.obu) == count_frames(wd_enpipe/movie.obu). Document (comment) that a byte-difference-with-equal-frame-count is the EXPECTED non-deterministic-qsvencc case per parity_encode.py, so the fallback is correct, not a weakening. Keep test identifiers English. Zero edits under legacy/.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "legacy_oracle_parity" -x -rs</automated>
  </verify>
  <done>On this Arc devcontainer test_sdr_legacy_oracle_parity RUNS and passes: enpipe encode frame-count matches the frozen legacy/encode_scenes.py oracle on the same sample+scenes, and the pre-mux movie.obu is byte-identical (deterministic qsvencc) OR frame-count-equal (non-deterministic fallback). Both sides run --no-metrics. Legacy is executed read-only via subprocess with ZERO edits under legacy/. Skips cleanly on non-Arc. SC4's "legacy remains the parity oracle" now has an executable check, not just a --help smoke.</done>
</task>

<task type="auto">
  <name>Task 2: fixture-gated HDR10+/DV cases + ffprobe-native DV RPU SOURCE-parity + fixtures README/gitignore</name>
  <files>tests/integration/test_hardware_real_media.py, tests/fixtures/media/README.md, .gitignore</files>
  <action>Extend tests/integration/test_hardware_real_media.py with the fixture-gating layer per D-06/D-07. Define FIXTURES_DIR = Path(os.environ.get("ENPIPE_TEST_MEDIA", <repo>/tests/fixtures/media)) and a `_fixture(name)` helper returning the path if it is_file() else None. Add a DV-mechanism SELF-CHECK helper that asserts the installed ffmpeg parses AV1 DOVI OBU — run the read-only `ffmpeg -h bsf=dovi_rpu` and assert its Supported-codecs line matches AV1 with a WORD-BOUNDARY regex `\bav1\b` (opencode L2 — NOT a bare substring, which could match spuriously); if not, skip the DV case with a message that the toolchain lacks AV1 DOVI parsing (per RESEARCH Assumption A2 — fail loud, never silently under-report). Add a read-only `_dv_rpu_frame_count(path) -> (with_rpu, total)` helper that runs `ffprobe -v error -select_streams v:0 -show_frames -show_entries frame=side_data_list -of json` (NO -bsf:v) and counts frames whose side_data_list contains a "Dolby Vision RPU Data" side_data_type. Add test_hdr10plus and test_dv cases: each calls `_fixture("hdr10plus.mkv")` / `_fixture("dv.mkv")`, and if None does `pytest.skip(<explanatory message pointing at tests/fixtures/media/README.md and $ENPIPE_TEST_MEDIA, stating genuine HDR10+/DV cannot be synthesized (D-06) and must be operator-supplied — this is NOT a failure>)`. When a fixture IS present: run the same end-to-end enpipe CLI pipeline via `_run_cli` (detect → encode with `--keep --no-audio --no-metrics --jobs 2`) on it, then for DV run the AV1-DOVI self-check, then compute SOURCE-parity (opencode M1 / qwen M3): probe the SOURCE fixture's RPU frame count via `_dv_rpu_frame_count(dv_fixture)` -> (src_with_rpu, _) and the final output's via `_dv_rpu_frame_count(final_mkv)` -> (out_with_rpu, out_total), then assert out_with_rpu == src_with_rpu AND out_with_rpu > 0 (the RPU frame-count SURVIVES the splice/mux — the genuine survival invariant; asserting merely `with_rpu == total` is too strict if the source itself is partial and wrong-baselined). ALSO run the same source-parity on the pre-mux .obu chunks (RESEARCH Assumption A3 — mkvmerge's DV-side-data mux path is distinct from HDR10). CRITICAL false-confidence guard (D-07): the DV survival check MUST use ONLY read-only ffprobe side-data (side_data_list) of AV1 DOVI OBUs. Do NOT `dovi_tool extract-rpu` (broken on AV1 in 2.3.2), do NOT pass the mutating dovi_rpu bitstream filter to any verification/output command (it FABRICATES a fake DOVI record — RESEARCH Pitfalls 2/3), and do NOT transcode AV1→HEVC. Additionally, to keep the CI forbidden-usage grep gate unambiguous, do NOT write the literal tokens `-bsf:v dovi_rpu`, the stream-specifier form `-bsf:v:0 dovi_rpu`, `extract-rpu`, or any non-self-check `bsf=dovi_rpu` ANYWHERE in the file — not even inside comments; express the DO-NOT rules in prose (e.g. "the dovi_rpu bitstream filter fabricates RPU and must never be used to verify") WITHOUT the literal flag strings. The ONLY permitted occurrence of the token `bsf=dovi_rpu` in the whole file is the read-only `ffmpeg -h bsf=dovi_rpu` AV1-support self-check. Create tests/fixtures/media/README.md documenting: the expected filenames (hdr10plus.mkv, dv.mkv), that they are operator-supplied and gitignored (copyright), the $ENPIPE_TEST_MEDIA override, and why they cannot be synthesized (D-06). Add a `.gitignore` block ignoring operator media under `tests/fixtures/media/` (e.g. `tests/fixtures/media/*` + `!tests/fixtures/media/README.md`) so README.md stays tracked while *.mkv/*.obu stay untracked.</action>
  <verify>
    <automated>bash -c 'set -e
uv run pytest tests/integration/test_hardware_real_media.py -m hardware -k "hdr10plus or dv" -rs 2>&1 | grep -Eq "skipped|passed"
grep -Rq "ENPIPE_TEST_MEDIA" tests/fixtures/media/README.md
grep -q "tests/fixtures/media" .gitignore
grep -q "side_data_list" tests/integration/test_hardware_real_media.py
if grep -Eq "extract-rpu|-bsf:v(:[0-9]+)?[[:space:]]*dovi_rpu" tests/integration/test_hardware_real_media.py; then echo "GATE FAIL: forbidden mutating dovi_rpu write / extract-rpu present"; exit 1; fi
if grep -n "bsf=dovi_rpu" tests/integration/test_hardware_real_media.py | grep -vq -- "-h bsf=dovi_rpu"; then echo "GATE FAIL: non-self-check bsf=dovi_rpu present"; exit 1; fi
echo GATE_OK'</automated>
  </verify>
  <done>Absent fixtures → HDR10+/DV cases skip cleanly with an explanatory how-to-supply message (never a faked pass). The DV check uses only read-only ffprobe side-data (frame=side_data_list) of AV1 DOVI OBUs and asserts output RPU frame-count == SOURCE fixture RPU frame-count (survives splice/mux) on both the final .mkv AND the pre-mux .obu; the AV1 self-check uses a `\bav1\b` word-boundary match; the ONLY dovi_rpu token in the file is the read-only `ffmpeg -h bsf=dovi_rpu` self-check. The gate command CANNOT print GATE_OK / exit 0 if `extract-rpu`, a mutating `-bsf:v[:N] dovi_rpu` (stream-specifier form included), or any non-self-check `bsf=dovi_rpu` appears in the test file — each forbidden pattern forces `exit 1` via an explicit `if` guard (no `&&`/`||` precedence bypass). README.md and the gitignore entry exist; operator media stays untracked.</done>
</task>

<task type="auto">
  <name>Task 3: D-08 self-hosted-Arc CI stub (workflow_dispatch only)</name>
  <files>.github/workflows/hardware-integration.yml</files>
  <action>Create .github/workflows/hardware-integration.yml per D-08 — a distinct, NEVER-auto-triggered GitHub Actions job giving the `hardware` tier a named home without letting hosted-CI green be mistaken for hardware validation. Trigger ON `workflow_dispatch` ONLY (no push, no pull_request — RESEARCH Security: a self-hosted runner with /dev/dri must never execute untrusted fork PR code; Elevation-of-Privilege mitigation). Add a top comment block stating: this requires a self-hosted Intel Arc runner (labels e.g. [self-hosted, arc]); it is intentionally decoupled from ci.yml's hosted cpu-fallback tier; hosted CI green ≠ hardware validation. The job runs on the self-hosted label, does `uv sync --locked` (falling back to `uv pip install -e .` on a stale lockfile), and runs `uv run pytest -m hardware -rs` (the ONLY workflow that selects the hardware marker). Keep it a documented stub: it will only ever run when a maintainer manually dispatches it on a machine with real Arc hardware.</action>
  <verify>
    <automated>bash -c 'set -e
f=.github/workflows/hardware-integration.yml
test -f "$f"
grep -q "workflow_dispatch" "$f"
if grep -Eq "^[[:space:]]*(push|pull_request)[[:space:]]*:" "$f"; then echo "GATE FAIL: auto-trigger (push/pull_request) present"; exit 1; fi
echo DISPATCH_ONLY_OK'</automated>
  </verify>
  <done>hardware-integration.yml exists, triggers on workflow_dispatch ONLY (no push/pull_request — verified via a grep-based gate that needs no PyYAML import, opencode L3), targets a self-hosted Arc runner label, runs `pytest -m hardware`, and documents that hosted-CI green is not hardware validation. ci.yml is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real Arc GPU | The hardware test drives qsvencc/ffmpeg subprocesses on /dev/dri; correctness (not security) is the risk surface |
| CI trigger → self-hosted runner | A self-hosted runner with /dev/dri passthrough must not execute untrusted (fork PR) code |
| verification tooling → bitstream | The DV verification tool must not itself mutate/fabricate the metadata it reports on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Repudiation / false-confidence | DV RPU verification step | mitigate | Use READ-ONLY `ffprobe -show_entries frame=side_data_list` (no bitstream-filter write); NEVER the mutating `dovi_rpu` bsf (empirically fabricates a fake DOVI record on non-DV content) nor `dovi_tool extract-rpu` (fails `Invalid input file type` on AV1). Add a read-only `ffmpeg -h bsf=dovi_rpu` self-check asserting AV1 support with a `\bav1\b` word-boundary match so a toolchain downgrade fails loud, not silent. Task 2's CI gate is an explicit `if`/grep guard that exits 1 on any forbidden write-usage token (no `&&`/`||` precedence bypass); its regex `-bsf:v(:[0-9]+)?[[:space:]]*dovi_rpu` also catches the stream-specifier form `-bsf:v:0 dovi_rpu` |
| T-04-04 | Information disclosure / false-confidence | test coverage claims (HDR10+/DV) | mitigate | Fixture-gate HDR10+/DV: absent → `pytest.skip` with how-to-supply message; NEVER fake a pass; document the coverage boundary in README.md (D-06 honesty requirement) |
| T-04-05 | Elevation of Privilege | D-08 self-hosted Arc runner | mitigate | Workflow triggers on `workflow_dispatch` ONLY — no `push`/`pull_request`; never runs untrusted fork code with `/dev/dri` access |
| T-04-06 | Integrity | frame-count / keyframe / parity verification | mitigate | Verify INDEPENDENTLY (count_frames on `--keep` chunks vs independently-parsed `.scenes`; `keyframe_table_ffprobe` ground-truth path — RESEARCH Pitfall 4/6). The keyframe check is NON-tautological: the pipeline's compute_chunk_seek_trim/kf_before decision is asserted to land on a member of the independent ground-truth keyframe set (the removed `max({f<=s})`-exists check could never fail). Per-chunk count_frames(chunk)==e-s is the load-bearing trim invariant. A legacy-oracle parity check (frame-count + determinism-aware pre-mux movie.obu byte compare vs the frozen legacy/encode_scenes.py) closes the "invariants pass but bytes differ from the oracle" gap (SC4) |
| T-04-07 | Denial of Service (test flakiness) | HDR10 software-x265 clip generation | mitigate | Shorten the HDR10 clip to ~40s (4×10s segments, still 3 cuts) + `-preset ultrafast`; probe libx265 and SKIP test_hdr10 cleanly if absent — avoids a multi-minute software-x265 timeout on the self-hosted runner |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase — all tools (ffmpeg/qsvencc/mkvmerge/dovi_tool) are pre-provisioned system binaries; RESEARCH Package Legitimacy Audit: "Not applicable" |
</threat_model>

<verification>
- `uv run pytest -m "not hardware"` is UNAFFECTED (the new test is marker-excluded from the default tier).
- On this Arc devcontainer: `pytest -m hardware -k "sdr and not legacy"`, `pytest -m hardware -k "legacy_oracle_parity"`, and `pytest -m hardware -k "hdr10 and not hdr10plus"` actually run and pass — every encode invocation carries --no-metrics.
- HDR10+/DV cases skip cleanly (no fixtures) with explanatory messages, or run + pass (DV asserting SOURCE-parity of RPU frame count) when fixtures are supplied.
- test_hdr10 skips cleanly if libx265 is unavailable; the HDR10 clip is short + ultrafast (no timeout).
- No verification path invokes `dovi_tool extract-rpu` or the mutating dovi_rpu bitstream filter (stream-specifier form included); the Task 2 gate exits 1 on any such token.
- hardware-integration.yml triggers on workflow_dispatch only (grep-based gate, no PyYAML dependency).
</verification>

<success_criteria>
- TEST-04 satisfied: a hardware-gated end-to-end (detect→encode→mux via the `enpipe` CLI, all encodes --no-metrics) test independently verifies per-chunk + total frame counts, NON-tautological keyframe alignment, legacy-oracle parity, and DV RPU survival; excluded from default CI.
- SDR + synthetic HDR10 validated live on real Arc (HDR10 short/ultrafast, libx265-gated); HDR10+/DV fixture-gated with honest clean skips (D-06).
- DV RPU checked via ffprobe-native side-data only, asserting SOURCE-parity of the RPU frame count (D-07); HDR10 metadata checked at STREAM level; coverage boundary documented; no coverage theater.
- Legacy remains the executable parity oracle (SC4): enpipe encode is frame-count/movie.obu parity-checked against legacy/encode_scenes.py on the SDR sample.
- Hardware tier has a named self-hosted CI home (D-08) that can never be confused with hosted-CI green.
</success_criteria>

<output>
Create `.planning/phases/04-unified-cli-hardware-gated-real-media-validation/04-02-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

- **D-05** (hardware-marked full detect→encode→mux test verifying per-chunk/total frame counts, keyframe alignment, DV RPU survival): Task 1a (frame counts + NON-tautological keyframe alignment, live SDR, all encodes --no-metrics) + Task 1b (same invariants, live HDR10, STREAM-level metadata survival) + Task 1c (legacy-oracle parity, SC4) + Task 2 (DV RPU source-parity). All CLI calls go through the SystemExit-safe `_run_cli` wrapper.
- **D-06** (SDR/HDR10 synthesized + validated; HDR10+/DV fixture-gated, clean skip, never fake a pass, document boundary): Task 1a (SDR live) + Task 1b (HDR10 live, short/ultrafast, libx265-gated) + Task 2 (fixture-gating + README).
- **D-07** (DV RPU mechanism: read-only ffprobe side-data; NOT dovi_tool extract-rpu, NOT the fabricating dovi_rpu bsf; self-check AV1 DOVI parsing with `\bav1\b`; forbidden-usage CI grep gate incl. stream-specifier form): Task 2. Now asserts SOURCE-parity of the RPU frame count, not merely `== total`.
- **D-08** (optional self-hosted-Arc CI stub, never auto-triggered, hardware tier named home): Task 3 (grep-based dispatch-only gate, no PyYAML).
- **D-09** (legacy untouched; conventions preserved): this plan modifies only tests/CI/fixtures — zero production/legacy edits. Task 1c executes legacy/encode_scenes.py read-only via subprocess (the parity oracle) without editing it.
- **SC4** (legacy remains the parity oracle): Task 1c makes this an executable frame-count + determinism-aware movie.obu parity check, not just a --help smoke.
</output>
