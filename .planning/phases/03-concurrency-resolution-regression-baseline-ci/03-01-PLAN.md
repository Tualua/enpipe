---
phase: 03-concurrency-resolution-regression-baseline-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enpipe/detection/parallel.py
  - .devcontainer/Dockerfile
  - .devcontainer/post-create.sh
  - scratch/profiling_debt03.py
autonomous: true
requirements: [DEBT-03, DEBT-04]
must_haves:
  truths:
    - "The ThreadPool-vs-ProcessPool contradiction in parallel.py is gone: the executor actually used and the comment describing it agree, and the comment carries measured wall-clock/speedup numbers as its justification"
    - "The executor decision follows a QUANTIFIED rule (concrete speedup/ratio thresholds, not a qualitative judgement), and the SUMMARY records the measured numbers plus which branch of the rule fired"
    - "Detection output is unchanged: detect_scenes_parallel(clip, jobs=2) yields the same (start_frame, end_frame) pairs as detect_scenes(clip, jobs=1) on the profiling clip"
    - "The profiling numbers provably came from the REAL parallel path, not a silent fallback: the jobs=2 Layer-1 timing carries a call-count engagement check on _segment_worker (>1 invocation)"
    - "dovi_tool remains installed in the devcontainer with an explicit comment stating it is retained for planned Phase-4 DV RPU-fidelity work, without overclaiming the AV1 mechanism"
  artifacts:
    - path: "scratch/profiling_debt03.py"
      provides: "Reproducible Layer-1 wall-clock A/B (jobs=1 vs jobs=2) + Layer-2 CPU-isolated microbenchmark, with a call-count engagement check proving the jobs=2 numbers came from the real parallel path, that produces the numbers backing the DEBT-03 decision"
      min_lines: 40
    - path: "src/enpipe/detection/parallel.py"
      provides: "Resolved executor (ThreadPool kept OR ProcessPool swapped) with a measured-rationale comment replacing the contradictory one at lines ~97-99"
      contains: "def detect_scenes_parallel"
    - path: ".devcontainer/Dockerfile"
      provides: "dovi_tool install block annotated with retained-purpose comment"
      contains: "dovi_tool"
    - path: ".devcontainer/post-create.sh"
      provides: "dovi_tool self-check line annotated with retained-purpose comment"
      contains: "dovi_tool"
  key_links:
    - from: "src/enpipe/detection/parallel.py"
      to: "the executor comment"
      via: "measured numbers recorded inline"
      pattern: "detect_scenes_parallel"
---

<objective>
Resolve the parallel-detection concurrency inconsistency with real profiling data (DEBT-03), and document the retained `dovi_tool` devcontainer dependency (DEBT-04). Both are tech-debt closure with no change to detection output.

Purpose: The executor choice in `detect_scenes_parallel` must be evidence-backed, not a guess — the code comment at `parallel.py:97-99` claims "real parallelism bypassing the GIL needs processes" while both executors at lines 127/146 are `ThreadPoolExecutor`. This plan MEASURES which is correct on the real Arc GPU present in this sandbox, then applies a QUANTIFIED decision rule (D-01/D-02) to either swap to `ProcessPoolExecutor` or keep threads and rewrite the comment with the measured rationale. This MUST land before TEST-03's baseline (D-03, locked ordering). Separately, `dovi_tool` is kept and documented rather than removed (D-04, lower-churn).

Output: A resolved `parallel.py`, a reproducible profiling script, and annotated devcontainer files.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-concurrency-resolution-regression-baseline-ci/03-CONTEXT.md
@.planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md

@src/enpipe/detection/parallel.py
@src/enpipe/detection/detect.py
@src/enpipe/detection/config.py
@scratch/parity_detect.py

<interfaces>
<!-- Key contracts the executor needs — extracted from the codebase. Use directly; no exploration. -->

From src/enpipe/detection/detect.py:
  detect_scenes(path: PathLike, config: DetectionConfig = DetectionConfig(), jobs: int = 1) -> List[Scene]
  _min_scene_len(config: DetectionConfig, fps: float) -> int
  _detect_relative(stream: QsvPipeStream, config: DetectionConfig) -> List[Tuple[int, int]]

From src/enpipe/detection/parallel.py:
  detect_scenes_parallel(path: PathLike, config: DetectionConfig, jobs: int) -> List[Scene]
  _boundary_worker(args: tuple) -> Optional[Tuple[int, float, bool]]   # module-level, pickle-safe; called jobs-1 times
  _segment_worker(args: tuple) -> List[Tuple[int, int]]                # module-level, pickle-safe; called ONLY after the len(bnds)<3 fallback is cleared
  # TWO sequential fallbacks (both `from .detect import detect_scenes; return detect_scenes(path, config, jobs=1)`):
  #   (1) gate at parallel.py:121 — total is None OR jobs < 2 OR total < jobs*min_span
  #   (2) collapse at parallel.py:133 — `if len(bnds) < 3` after boundary-finding
  # min_span = max(2*_min_scene_len, round(60*fps)). Two `with ThreadPoolExecutor(max_workers=jobs)` blocks at lines 127 and 146.

From src/enpipe/detection/config.py:
  DetectionConfig  # frozen dataclass; min_scene_len_frames=72 default; use_qsv=True default; PICKLABLE
  Scene(index, start_frame, end_frame, start_sec, end_sec)  # frozen; PICKLABLE

Gate arithmetic (verified): default DetectionConfig at 24fps -> min_span = max(144, 1440) = 1440 frames = 60s.
  jobs=2 => profiling clip must be >= 2*60s = 120s of real frames for the parallel path to engage (use ~150s+ for margin).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile the executor (Layer 1 wall-clock A/B + Layer 2 CPU-isolated microbenchmark)</name>
  <files>scratch/profiling_debt03.py</files>
  <read_first>
    scratch/parity_detect.py (lavfi-concat clip recipe + use_qsv probe pattern),
    src/enpipe/detection/parallel.py (the gate at 120-123, the second fallback at 133, and the two executors),
    .planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md
      ("DEBT-03: Profiling Methodology" — Layer 1 and Layer 2 code, and the decision table)
  </read_first>
  <action>
    Write a throwaway (NOT packaged) profiling script following RESEARCH's Layer-1 and Layer-2 methodology.

    (a) Generate ONE synthetic profiling clip at scratch/profiling_debt03_sample.mkv via ffmpeg lavfi
    concat of two alternating distinct visual segments (color=red / smptebars), 24fps, 320x180, each segment
    ~78s so total >= 156s (comfortable headroom over the 120s jobs=2 gate). Profiling at jobs=2 (not jobs=4)
    is deliberate: the GIL-vs-I/O signal is binary and does not need jobs=4, and a >=288s jobs=4 clip roughly
    doubles the A/B-matrix wall-clock for no diagnostic gain (qwen H-1). This satisfies the fallback gate
    total_frames >= jobs*min_span (=120s for jobs=2 at 24fps, verified) so the parallel path actually engages —
    reuse the color-vs-smptebars visual content proven to produce real cuts in parity_detect.py.

    (b) Layer 1 — real-path wall-clock A/B: for use_qsv in (True, False), build DetectionConfig(use_qsv=...)
    and time detect_scenes(clip, cfg, jobs=1) vs detect_scenes(clip, cfg, jobs=2); print jobs=1 seconds,
    jobs=2 seconds, and speedup ratio (jobs=1 / jobs=2) for each use_qsv. Comparing use_qsv=True (GPU decode)
    vs use_qsv=False (CPU decode) isolates whether GPU-decode I/O overlap is masking GIL serialization
    (Pitfall 5). Use the repo convention: frozen DetectionConfig, no --no-qsv flag.

    (c) ENGAGEMENT CHECK (closes the same vacuous-run hole opencode flagged for TEST-03, applied here so the
    DECISION NUMBERS cannot silently come from a fallback run): before/around the jobs=2 timing, install an
    in-process call-counter on `enpipe.detection.parallel._segment_worker` (save the original, replace the
    module attribute with a wrapper that increments a counter and calls through, restore after). `_segment_worker`
    is invoked ONLY after BOTH fallbacks (line 121 gate AND line 133 len(bnds)<3 collapse) are cleared, so a
    non-zero count proves the real parallel+stitch branch ran. Assert the counter is > 1 for the jobs=2 run and
    print it. (At this point parallel.py still uses ThreadPoolExecutor — in-process, so the wrapper is honored;
    the swap, if any, happens in Task 2.)

    (d) Layer 2 — CPU-isolated microbenchmark (per RESEARCH): decode ~300 frames ONCE into memory via
    QsvPipeStream, then time a 2-way split of a module-level _score_frames(frames) (constructs its own
    AdaptiveDetector and calls process_frame — pure CPU, no subprocess I/O) under ThreadPoolExecutor vs
    ProcessPoolExecutor, plus a max_workers=1 baseline for each, to compute the ProcessPool/ThreadPool ratio.
    _score_frames MUST be module-level (pickle-safe) per the codebase's existing worker pattern.

    (e) Print all raw numbers to stdout in a stable, greppable form. Keep the script in scratch/ for
    reproducibility (it is not held to lint/style bars). Russian or English prose is fine in scratch/.

    Do NOT edit parallel.py in this task — this task only MEASURES (D-01: profile first, decide second).
  </action>
  <verify>
    <automated>python3 scratch/profiling_debt03.py 2>&1 | tee scratch/profiling_debt03_out.txt; grep -Eiq 'speedup|thread|process' scratch/profiling_debt03_out.txt && grep -Eiq '_segment_worker|engag|call.?count' scratch/profiling_debt03_out.txt</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scratch/profiling_debt03.py` runs to completion and prints, for both use_qsv=True and
      use_qsv=False: jobs=1 seconds, jobs=2 seconds, and the speedup ratio (Layer 1).
    - It prints ThreadPoolExecutor vs ProcessPoolExecutor 2-way split times and single-worker baselines,
      and the ProcessPool/ThreadPool ratio (Layer 2).
    - The jobs=2 run carries a `_segment_worker` call-count engagement check that asserts >1 invocation and
      prints the count, proving the parallel path (not either fallback) produced the decision numbers.
    - The profiling clip is >= 156s so detect_scenes(jobs=2) exercises the parallel path, not the fallback.
  </acceptance_criteria>
  <done>Raw wall-clock + microbenchmark numbers exist, are proven to come from the real parallel path, and map onto RESEARCH's decision table.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve parallel.py per the QUANTIFIED decision rule (measured rationale, output unchanged)</name>
  <files>src/enpipe/detection/parallel.py</files>
  <read_first>
    scratch/profiling_debt03_out.txt (Task 1 numbers),
    src/enpipe/detection/parallel.py (comment lines 97-99; executors at 127 and 146; imports at 17-18),
    .planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md ("Decision rule" table + "ProcessPoolExecutor caveats")
  </read_first>
  <action>
    Apply the locked D-02 decision rule to the Task-1 numbers using CONCRETE thresholds (no qualitative
    "materially capture" wording — opencode M / qwen):

    DECISION RULE: SWITCH to ProcessPoolExecutor IF AND ONLY IF
      (Layer-1 ThreadPool speedup on the use_qsv=False CPU-decode arm, = jobs=1 wall / jobs=2 wall) < 1.5x
      AND (Layer-2 ProcessPool/ThreadPool CPU-microbenchmark ratio) > 2x.
    OTHERWISE keep ThreadPoolExecutor.

    - IF the rule fires SWITCH: BEFORE committing the swap, run two pre-commit guards (opencode Layer-1.5 +
      qwen H-2 pickling smoke-check):
        (i) Layer-1.5 real-path: temporarily swap BOTH `with ThreadPoolExecutor(max_workers=jobs)` blocks to
        `ProcessPoolExecutor` and measure detect_scenes_parallel(clip, cfg, jobs=2) end-to-end wall-clock.
        Confirm the microbenchmark win SURVIVES process-startup + per-call scenedetect/numpy/cv2 re-import
        overhead. If the real-path wall-clock is NOT faster than the ThreadPool real-path, the win did not
        materialize -> DO NOT SWITCH; keep threads and record why.
        (ii) Pickling smoke-check: run the (start_frame, end_frame) equality smoke-check on a SECOND, distinct
        >=120s clip (different visual content so it ALSO engages the jobs=2 parallel gate — a short clip would
        fall back to sequential and start no processes, proving nothing) to catch scenedetect/opencv pickling
        or module-state issues that only surface across process boundaries (would fail in CI, not just here).
      If BOTH guards pass, commit the switch: change the import at line 18 to
      `from concurrent.futures import ProcessPoolExecutor` and both executor blocks (127, 146). The workers
      `_boundary_worker`/`_segment_worker` are already module-level and all args (PathLike, DetectionConfig,
      ints/floats) are picklable — no other change needed. In the resolution comment, ALSO note the two-`with`-
      block double process-startup + import cost per call as a legitimate reason the real win may be smaller
      than the microbenchmark alone.

    - ELSE (threads kept): KEEP `ThreadPoolExecutor` unchanged and REWRITE the comment at lines 97-99 so it no
      longer contradicts the code. State precisely, with the measured numbers, WHY threads are acceptable here
      (e.g. "the CPU-bound detector serializes under the GIL in threads, but it is a small fraction of total
      wall time dominated by subprocess/decode I/O which threads already overlap — measured: jobs=1 Xs vs
      jobs=2 Ys, speedup Zx"). Do NOT leave the old "нужны процессы" claim. The Layer-1.5 and pickling
      smoke-check guards are MOOT in this branch — note that explicitly in the SUMMARY.

    Either branch: the resolved comment MUST carry the measured wall-clock seconds and speedup ratio so the
    decision is auditable. Record in the SUMMARY: both use_qsv Layer-1 speedups, the Layer-2 ratio, which branch
    of the rule fired, and (if switched) the Layer-1.5 real-path numbers + second-clip smoke-check result.
    Preserve conventions verbatim (Russian prose, typing generics, banner style — D-10).
    Do NOT change any detection algorithm, argv, or the gate arithmetic — output must be byte-for-byte identical.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
from enpipe.detection.config import DetectionConfig
from enpipe.detection.detect import detect_scenes
from enpipe.detection.parallel import detect_scenes_parallel
clip='scratch/profiling_debt03_sample.mkv'
cfg=DetectionConfig(use_qsv=Path('/dev/dri/renderD128').exists())
seq=[(s.start_frame,s.end_frame) for s in detect_scenes(clip,cfg,jobs=1)]
par=[(s.start_frame,s.end_frame) for s in detect_scenes_parallel(clip,cfg,jobs=2)]
assert len(seq)>=2, f'clip trivial: {len(seq)} scenes'
assert seq==par, f'OUTPUT CHANGED\nseq={seq}\npar={par}'
print(f'OK: {len(seq)} scenes, parallel==sequential')
"</automated>
  </verify>
  <acceptance_criteria>
    - The executor used in `detect_scenes_parallel` and the comment describing it AGREE (no contradiction).
    - `grep -nE 'jobs=2|speedup|[0-9]+(\.[0-9]+)?\s*(s|x)' src/enpipe/detection/parallel.py` shows measured
      numbers embedded in the resolution comment.
    - The decision followed the concrete rule (speedup<1.5x AND ratio>2x -> switch; else keep); the SUMMARY
      records both use_qsv speedups, the Layer-2 ratio, and the fired branch (and, if switched, the Layer-1.5
      real-path numbers + second-clip pickling smoke-check result).
    - The inline smoke check above proves `detect_scenes_parallel(jobs=2) == detect_scenes(jobs=1)` by
      (start_frame, end_frame) pairs on the >=156s profiling clip (>=2 scenes, so non-trivial).
    - Russian docstrings/comments, `typing.List/Optional/Tuple` generics, and banner style preserved.
  </acceptance_criteria>
  <done>parallel.py is internally consistent and evidence-backed via a quantified rule; detection output is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Document retained dovi_tool in the devcontainer (DEBT-04)</name>
  <files>.devcontainer/Dockerfile, .devcontainer/post-create.sh</files>
  <read_first>
    .devcontainer/Dockerfile (dovi_tool RUN block, lines 67-76),
    .devcontainer/post-create.sh (dovi_tool self-check, line 53),
    legacy/encode_scenes.py:15-16 (the existing HEVC-only note),
    .planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md (Pitfall 3 — do not overclaim the AV1 mechanism)
  </read_first>
  <action>
    Keep `dovi_tool` installed (do NOT remove it) and add an explicit retained-purpose comment at BOTH
    references, satisfying DEBT-04's "removed OR documented reason for keeping" with the lower-churn option
    (D-04). In the Dockerfile, above/within the `dovi_tool` RUN block (line ~67), add a Russian comment
    stating it is retained for the PLANNED Phase-4 Dolby Vision RPU-fidelity verification (TEST-04) — the
    pipeline currently only guards aggregate video frame count, so an RPU frame-count/profile check is a
    Phase-4 goal. CRITICAL (Pitfall 3): do NOT claim `dovi_tool extract-rpu` will work on the pipeline's AV1
    output — `extract-rpu` is documented HEVC-only (see legacy/encode_scenes.py:15-16); word it as retained
    pending Phase-4 investigation of the correct AV1 DV-verification approach (may need dovi_tool's C-API/OBU
    support, a different tool, or a custom RPU-presence check). Add a shorter matching comment at the
    post-create.sh self-check line (~53). The current DV path (`qsvencc --dolby-vision-rpu copy`) is unchanged.
    Preserve Russian prose (D-10). No install commands change — comments only.
  </action>
  <verify>
    <automated>grep -nEi 'phase.?4|фаз[аеы].?4|TEST-04|RPU' .devcontainer/Dockerfile .devcontainer/post-create.sh && grep -c 'dovi_tool' .devcontainer/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - `.devcontainer/Dockerfile` still installs `dovi_tool` (the RUN block is intact) AND carries a comment
      tying its retention to planned Phase-4 TEST-04 RPU-fidelity work.
    - `.devcontainer/post-create.sh` self-check line carries a matching short comment.
    - Neither comment asserts `dovi_tool extract-rpu` works on AV1 output (no overclaim of the mechanism).
  </acceptance_criteria>
  <done>dovi_tool is documented-and-kept; DEBT-04 satisfied without churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process executor swap (Thread→Process) | A concurrency change can silently alter detection output — the load-bearing correctness invariant |
| profiling run → decision | Decision numbers sourced from a silent fallback run would justify the wrong executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `detect_scenes_parallel` executor resolution | mitigate | Task 2 inline smoke check asserts `parallel(jobs=2) == sequential(jobs=1)` by (start,end) pairs on the >=156s clip; if ProcessPool is chosen, a pre-commit pickling smoke-check on a 2nd distinct clip runs first; TEST-03 (Plan 03-02) formalizes this as a committed regression test before CI |
| T-03-02 | Repudiation | executor decision provenance | mitigate | Measured wall-clock/speedup numbers (both use_qsv arms) + the Layer-2 ratio are embedded in the parallel.py resolution comment and recorded in the SUMMARY with the fired decision branch; reproducible via scratch/profiling_debt03.py |
| T-03-09 | Spoofing (false-provenance) | profiling decision numbers from a silent fallback | mitigate | Task 1 installs a `_segment_worker` call-count engagement check around the jobs=2 timing and asserts >1 invocation, so the decision numbers provably came from the real parallel path (not the line-121 gate or line-133 collapse fallback) |
| T-03-03 | Information Disclosure | dovi_tool doc comment | accept | Doc-only change; no runtime code path touches dovi_tool this phase; comment avoids overclaiming the unverified AV1 mechanism (Pitfall 3) |
</threat_model>

<verification>
- `python3 scratch/profiling_debt03.py` produces the decision-backing numbers with the `_segment_worker` engagement check passing.
- Executor and comment in parallel.py agree; comment carries measured numbers; decision followed the quantified rule.
- `detect_scenes_parallel(jobs=2) == detect_scenes(jobs=1)` on the >=156s profiling clip (>=2 scenes).
- `dovi_tool` retained + documented in both devcontainer files without overclaiming AV1 RPU extraction.
</verification>

<success_criteria>
DEBT-03 resolved with evidence (executor/comment consistent, quantified rule applied, measured numbers recorded,
provenance proven via the engagement check), detection output unchanged, and DEBT-04 satisfied (dovi_tool
documented-and-kept). Lands before TEST-03 (D-03).
</success_criteria>

## Truths — Decision Traceability

- **D-01** (profile first): Task 1 runs Layer-1 wall-clock A/B (use_qsv True vs False, jobs=1 vs jobs=2) + Layer-2 CPU-isolated microbenchmark and records raw numbers, with an engagement check proving the jobs=2 numbers came from the real parallel path.
- **D-02** (decide from data): Task 2 applies the QUANTIFIED locked decision rule (speedup<1.5x AND ratio>2x -> ProcessPool, else keep ThreadPool + rewrite comment) with the measured rationale embedded; a ProcessPool choice is gated by a Layer-1.5 real-path check + a second-clip pickling smoke-check.
- **D-03** (locked ordering): This plan is Wave 1; TEST-03 (Plan 03-02) depends on it, so resolution lands before the baseline.
- **D-04** (dovi_tool kept + documented): Task 3 keeps the install and adds a Phase-4-TEST-04 retained-purpose comment, without overclaiming the AV1 mechanism.
- **D-10** (conventions preserved): Russian prose, typing generics, banners kept; `legacy/` untouched; no detection output change.

<output>
Create `.planning/phases/03-concurrency-resolution-regression-baseline-ci/03-01-SUMMARY.md` when done.
</output>
