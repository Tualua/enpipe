---
phase: 03-concurrency-resolution-regression-baseline-ci
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - pyproject.toml
  - uv.lock
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "`ruff` is a pinned dev dependency in pyproject.toml/uv.lock, and a minimal `[tool.ruff]` config (select = ['F','E9']) makes `ruff check src tests` pass clean on the current code without fighting the preserved legacy style"
    - "A GitHub Actions workflow runs on push and PR against the pinned lockfile: setup-uv (SHA-pinned), apt ffmpeg+mkvtoolnix, uv sync --locked, ruff check, pytest -m 'not hardware' — covering lint + unit + mocked + the software-fallback regression test"
    - "The hardware-gated tier is excluded (`-m 'not hardware'`) and the CI job is named/commented distinctly so a green hosted check is never mistaken for real-Arc-hardware validation"
  artifacts:
    - path: "pyproject.toml"
      provides: "ruff dev dependency + [tool.ruff] lint config"
      contains: "[tool.ruff"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline (lint + non-hardware tests) with SHA-pinned setup-uv and distinct hardware-exclusion naming"
      contains: "astral-sh/setup-uv"
    - path: "uv.lock"
      provides: "ruff resolved into the committed lockfile so `uv sync --locked` installs it in CI"
      contains: "ruff"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "uv.lock"
      via: "uv sync --locked"
      pattern: "uv sync --locked"
    - from: ".github/workflows/ci.yml"
      to: "tests (incl. TEST-03 regression)"
      via: "uv run pytest -m \"not hardware\""
      pattern: "not hardware"
user_setup:
  - service: github-actions
    why: "CI runs automatically once ci.yml is pushed; no dashboard config needed, but the first run must be observed on GitHub"
    dashboard_config:
      - task: "Confirm the CI workflow appears and passes under the Actions tab after push"
        location: "GitHub repo → Actions"
---

<objective>
Wire lint + non-hardware tests into GitHub Actions against the pinned lockfile (CI-01), with the hardware tier visibly excluded.

Purpose: Every push must be automatically verified — ruff lint, pure-logic unit tests, subprocess-mocked tests, and the Plan-03-02 software-fallback regression test — installed from the committed `uv.lock` (not ad hoc). The lint config is deliberately minimal (`select = ["F","E9"]`, D-09): pyflakes real-defect detection + syntax errors only, verified clean on current code, NOT the fuller `E` set which fires 15× E702 on the intentionally dense `mkv/ebml.py` parser (Pitfall 2). The hosted runner has no GPU, so the job is named/commented distinctly to never be confused with hardware validation (D-08, Pitfall 3).

Output: `[tool.ruff]` + ruff dev dep in pyproject.toml/uv.lock, and `.github/workflows/ci.yml`.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-concurrency-resolution-regression-baseline-ci/03-CONTEXT.md
@.planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md

@pyproject.toml

<interfaces>
<!-- Verified facts from RESEARCH (run directly this session). -->

Ruff config (verified `ruff check --select F,E9 src/ tests/` -> "All checks passed!"):
  [tool.ruff]
  line-length = 100
  target-version = "py312"
  [tool.ruff.lint]
  select = ["F", "E9"]   # pyflakes + syntax errors only; NOT E/W (E702 fires 15x on mkv/ebml.py)

setup-uv pin (docs.astral.sh best practice, fetched 2026-07-08):
  astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0

Existing pyproject: [dependency-groups] dev already has pytest / pytest-subprocess / pytest-mock;
  [tool.pytest.ini_options] addopts already = "-m \"not hardware\" --import-mode=importlib".

Package Legitimacy Audit (RESEARCH): ruff — PyPI, ~4yr, tens of millions/wk, github.com/astral-sh/ruff,
  slopcheck OK -> APPROVED as dev dependency. No legitimacy checkpoint required.

CI invocation scope (anti-pattern to avoid: never lint legacy/ or scratch/):
  uv run ruff check src tests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ruff dev dependency + minimal [tool.ruff] config</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    pyproject.toml (dependency-groups dev; pytest ini),
    .planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md
      (Code Examples "[tool.ruff] recommended config"; Pitfall 1 & 2; Package Legitimacy Audit)
  </read_first>
  <action>
    Add `ruff` as a dev dependency via `uv add --dev ruff` (this updates both pyproject.toml
    [dependency-groups] dev AND uv.lock — ruff is APPROVED per the RESEARCH Package Legitimacy Audit, no
    checkpoint needed). Add a `[tool.ruff]` block: `line-length = 100`, `target-version = "py312"`, and
    `[tool.ruff.lint] select = ["F", "E9"]` — pyflakes (F401 unused imports, F821 undefined names, F841
    unused locals) + syntax errors only. Do NOT use the `E`/`W` pycodestyle set (Pitfall 2: E702 fires 15×
    on the deliberately dense mkv/ebml.py). Add a short comment on the select line noting F+E9 only, to
    protect the preserved legacy style (Russian comments, typing.List, banners — D-09/D-10). If you add any
    justification comment referencing why ruff is used, do NOT repeat the inaccurate "would have caught the
    JOBS finding" claim (Pitfall 1 — pyflakes does not flag unused module-level constants); phrase it as
    catching real F401/F821/F841 defects. Verify `ruff check src tests` passes clean.
  </action>
  <verify>
    <automated>uv run ruff check src tests && grep -q 'ruff' uv.lock && grep -q 'select = \["F", "E9"\]' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check src tests` exits 0 ("All checks passed!").
    - `pyproject.toml` has a `[tool.ruff]` block with `[tool.ruff.lint] select = ["F", "E9"]` and ruff in
      `[dependency-groups] dev`.
    - `uv.lock` contains a `ruff` entry (so `uv sync --locked` installs it in CI).
    - No `E`/`W` pycodestyle rules selected; no inaccurate JOBS-dead-code justification comment.
  </acceptance_criteria>
  <done>ruff is a locked dev dependency with a minimal, legacy-style-safe config that passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/ci.yml (lint + non-hardware tests, hardware tier excluded distinctly)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .planning/phases/03-concurrency-resolution-regression-baseline-ci/03-RESEARCH.md
      (Code Examples ".github/workflows/ci.yml" — complete verified template; D-07/D-08 notes),
    pyproject.toml (addopts default, dependency groups — confirm nothing extra needed for uv sync)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per the RESEARCH verified template. Trigger on `push` and
    `pull_request`. Single job on `ubuntu-latest`, named distinctly to signal it is the CPU/software-fallback
    tier with NO GPU (e.g. job key `cpu-fallback`, `name: "ci / cpu-fallback (lint + unit + mocked +
    regression, no GPU)"`). Steps in order: (1) `actions/checkout@v7`; (2)
    `astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0` (SHA-pinned per Astral best
    practice — a floating tag is a supply-chain risk); (3) `sudo apt-get update && sudo apt-get install -y
    --no-install-recommends ffmpeg mkvtoolnix` (real non-QSV binaries — the software-fallback regression +
    EBML cross-validation tests need real ffmpeg; only qsvencc is truly hardware-gated); (4)
    `uv sync --locked` (refuses to proceed on a stale lockfile — this is the load-bearing pinned-install
    flag); (5) `uv run ruff check src tests` (lint, scoped to src+tests only — never legacy/ or scratch/);
    (6) `uv run pytest -m "not hardware"` (unit + mocked + regression; explicit even though it is the
    pyproject addopts default, to self-document CI-01 intent). Add a top-of-job comment (D-08 / Pitfall 3)
    stating this runs on hosted ubuntu-latest with NO GPU, validates only the software-fallback path, does
    NOT validate real QSV/qsvencc/Arc behavior, and that hardware-gated tests (`hardware` marker, first added
    in Phase 4 / TEST-04) require a self-hosted Arc runner and are intentionally NOT wired here. Comment-only
    exclusion (no stub job) is the chosen discretion option (RESEARCH Open Question 3).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'push' in d[True] and 'pull_request' in d[True]; j=next(iter(d['jobs'].values())); steps=' '.join(str(s) for s in j['steps']); assert '08807647e7069bb48b6ef5acd8ec9567f424441b' in steps, 'setup-uv not SHA-pinned'; assert 'uv sync --locked' in steps; assert 'ruff check src tests' in steps; assert 'not hardware' in steps; print('ci.yml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML, triggers on `push` and `pull_request`.
    - `astral-sh/setup-uv` is pinned to commit SHA `08807647e7069bb48b6ef5acd8ec9567f424441b` (not a floating tag).
    - Steps include `uv sync --locked`, `uv run ruff check src tests`, and `uv run pytest -m "not hardware"`,
      plus apt install of `ffmpeg` + `mkvtoolnix`.
    - The job name and a comment distinctly mark this as the no-GPU / software-fallback tier and state the
      hardware tier is excluded and requires a self-hosted Arc runner (Phase 4 / TEST-04).
  </acceptance_criteria>
  <done>CI runs lint + non-hardware tests on every push/PR from the pinned lockfile, hardware tier named-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → upstream actions/packages | CI pulls a third-party action (setup-uv) and apt/PyPI packages; a repointed tag or unpinned install is a supply-chain vector |
| green hosted CI → correctness claim | A passing hosted check must not be read as real-Arc-hardware validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering (supply chain) | `astral-sh/setup-uv` action + ruff/pip/apt installs | mitigate | setup-uv pinned by commit SHA (not tag); `uv sync --locked` installs only from the committed lockfile; ruff is slopcheck-APPROVED per RESEARCH Package Legitimacy Audit (no [ASSUMED]/[SUS] package -> no legitimacy checkpoint required) |
| T-03-07 | Spoofing (false validation) | green hosted CI vs hardware validation | mitigate | Job named `cpu-fallback (... no GPU)` + top-of-job comment stating no QSV/Arc validation; `-m "not hardware"` excludes the (Phase-4) hardware tier by design (D-08, Pitfall 3) |
| T-03-08 | Tampering | apt package pull (ffmpeg/mkvtoolnix) | accept | Standard ubuntu-latest apt repos; non-QSV binaries only; no code executed from them beyond media generation — proportionate for a local toolchain's hosted CI |
</threat_model>

<verification>
- `uv run ruff check src tests` passes clean; ruff in pyproject dev + uv.lock.
- `.github/workflows/ci.yml` valid YAML, push+PR, SHA-pinned setup-uv, uv sync --locked, ruff, pytest -m "not hardware", apt ffmpeg+mkvtoolnix.
- Job named/commented distinctly from the hardware tier.
</verification>

<success_criteria>
CI-01 satisfied: a push/PR pipeline runs lint + pure-logic unit + subprocess-mocked + software-fallback
regression tests against the pinned lockfile, with the hardware tier excluded by design and named distinctly.
</success_criteria>

## Truths — Decision Traceability

- **D-07** (workflow shape): Task 2 creates ci.yml on ubuntu-latest, push+PR, with SHA-pinned setup-uv, apt ffmpeg+mkvtoolnix, `uv sync --locked`, `uv run ruff check`, `uv run pytest -m "not hardware"`.
- **D-08** (hardware excluded + named distinctly): the job name and comment mark the no-GPU tier and defer the self-hosted Arc runner to Phase 4 (comment-only exclusion, discretion per RESEARCH OQ3).
- **D-09** (minimal ruff): Task 1 adds `[tool.ruff] select = ["F","E9"]` only, avoiding the E-set that fights mkv/ebml.py; pyright deferred to v2.
- **D-10** (conventions/style preserved): lint scoped to `src tests`, never `legacy/`/`scratch/`; the rule set does not restyle Russian comments / typing generics / banners.

<output>
Create `.planning/phases/03-concurrency-resolution-regression-baseline-ci/03-03-SUMMARY.md` when done.
</output>
