---
phase: 01-package-foundation-migration-fast-test-tier
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/enpipe/encoding/scenes_io.py
  - src/enpipe/encoding/keyframes.py
  - src/enpipe/encoding/hdr.py
  - src/enpipe/encoding/chunk.py
  - src/enpipe/encoding/audio.py
  - src/enpipe/encoding/metrics.py
  - src/enpipe/encoding/pipeline.py
  - tests/unit/encoding/test_scenes_io.py
  - tests/unit/encoding/test_keyframes.py
  - tests/unit/encoding/test_chunk.py
  - tests/subprocess/encoding/test_hdr.py
  - tests/subprocess/encoding/test_chunk.py
  - tests/subprocess/encoding/test_audio.py
  - tests/subprocess/encoding/test_keyframes.py
  - scratch/parity_encode.py
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Encoding code lives in src/enpipe/encoding/{scenes_io,keyframes,hdr,chunk,audio,metrics,pipeline}.py, routes every subprocess through enpipe.shared.proc, and calls die() from enpipe.shared.logging"
    - "The hand-rolled EBML/Cues parser stays inline in keyframes.py (NOT split into mkv/ebml.py — that is Phase 2)"
    - "run_encode(args) accepts an argparse.Namespace-shaped object with the legacy attribute names, RETAINS the shutil.which tool-availability preflight, and produces byte-identical encoded output to legacy/encode_scenes.py on a sample run"
    - "`pytest -m \"not hardware\"` runs pure-logic tests (read_scenes, kf_before, fmt_seek, EBML helpers, parse_metrics, chunk_command) and mocked argv/error-path tests (detect_hdr, encode_chunk, encode_audio, keyframe_table_ffprobe); the encode-parity gate is hardware-gated and excluded from the fast tier"
  artifacts:
    - path: "src/enpipe/encoding/keyframes.py"
      provides: "_ebml_num/_eid/_esz, keyframe_table_cues/_ffprobe/_table, kf_before, fmt_seek (EBML inline per D-07)"
      contains: "def kf_before"
    - path: "src/enpipe/encoding/chunk.py"
      provides: "ICQ/QPMAX/GOP_LEN env consts, chunk_command (pure argv builder), parse_metrics, count_frames, encode_chunk"
      exports: ["chunk_command", "encode_chunk"]
    - path: "src/enpipe/encoding/pipeline.py"
      provides: "JOBS, probe_fps, run_encode(args) orchestration (main() minus argparse, preflight retained)"
      exports: ["run_encode"]
    - path: "tests/subprocess/encoding/test_hdr.py"
      provides: "mocked ffprobe argv + HDR flag-selection test for detect_hdr"
      contains: "def test_"
  key_links:
    - from: "src/enpipe/encoding/keyframes.py"
      to: "enpipe.shared.proc"
      via: "proc.run"
      pattern: "proc\\.run"
    - from: "src/enpipe/encoding/pipeline.py"
      to: "enpipe.shared.logging"
      via: "die import"
      pattern: "from enpipe\\.shared\\.logging import"
    - from: "src/enpipe/encoding/chunk.py"
      to: "run_encode consumers"
      via: "chunk_command argv (--icq/--seek/--trim)"
      pattern: "--seek"
---

<objective>
Mechanically migrate `legacy/encode_scenes.py` into `src/enpipe/encoding/{scenes_io,keyframes,hdr,chunk,audio,metrics,pipeline}.py` behind the `shared.proc` seam and `shared.logging.die`, add the encoding slice of the fast test tier, and verify byte-identical encoded output against the legacy oracle on real Arc hardware.

Purpose: TEST-01/TEST-02 for the encoding stage and the encoding half of the D-14 parity gate. Encoding migrates AFTER detection per D-13 (this plan depends on 01-02, wave 3): detection must be migrated and parity-verified before encoding migration begins. The EBML parser and seek/trim math move as-is (isolation is Phase 2).
Output: seven encoding modules, seven test files, and a passing byte-identical encode parity run.

Note on file count (14 files): this is expected and inherent to migrating the 7 encoding modules plus their 7 tests as one behavior-preserving unit (D-13 mechanical whole-stage migration). No horizontal split is warranted — the Task boundaries keep the module-move (Task 1) and test-write (Task 2) separable, and the parity gate (Task 3) is isolated in `scratch/`.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-CONTEXT.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Line-level source→module map (verified in RESEARCH Mechanical Migration Map). Copy-paste, do not re-derive. -->
scenes_io.py <- legacy/encode_scenes.py:94-107   (_SCENE_RE, read_scenes — move `import re` to TOP of file, not mid-file)
keyframes.py <- legacy/encode_scenes.py:130-326   (_ebml_num/_eid/_esz, keyframe_table_cues/_ffprobe/_table, kf_before, fmt_seek — EBML stays inline per D-07)
hdr.py       <- legacy/encode_scenes.py:55,332-348 (DV_PROFILE env const; detect_hdr)
chunk.py     <- legacy/encode_scenes.py:52-54,354-417 (ICQ/QPMAX/GOP_LEN; chunk_command PURE; _SSIM_RE/_PSNR_RE/parse_metrics; count_frames; encode_chunk)
audio.py     <- legacy/encode_scenes.py:57,59,423-478 (FLAC_LEVEL, LOSSLESS; encode_audio — returns (bool, Optional[str]), NEVER die())
metrics.py   <- legacy/encode_scenes.py:481-509   (write_metrics_csv, ИТОГО totals row)
pipeline.py  <- legacy/encode_scenes.py:56,110-122,515-724 (JOBS; probe_fps; main() body minus argparse → run_encode(args))
die (legacy :62-63) and run (legacy :66-67) do NOT migrate here — die comes from enpipe.shared.logging, run from enpipe.shared.proc (Plan 01-01).
__main__ guard (:727-728) is NOT migrated (Phase 4).

Preflight block to RETAIN inside run_encode — legacy/encode_scenes.py:533-537:
  for tool in ("qsvencc","ffprobe","ffmpeg","mkvmerge"): if not shutil.which(tool): die(f"не найден {tool}")
  if not args.video.is_file(): die(f"нет файла: {args.video}")
The high-water-mark append writes the concatenated AV1 stream to `<workdir>/movie.obu` (legacy :599-616); it is DELETED unless `--keep`/`args.keep` is set (legacy :715). The final `mkvmerge` mux wraps movie.obu into `<out>.mkv` (legacy :688-696).

run_encode(args) attribute names (argparse.Namespace-shaped, unchanged for Phase 4 CLI):
  video, scenes, out, frm, to, workdir, keep, jobs, no_audio, no_metrics, csv

Env-const test rule (Pattern 4): override via monkeypatch.setattr(module, "ICQ", 30), NOT monkeypatch.setenv after import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate encoding into seven modules behind the seam and shared die() (preflight retained)</name>
  <files>src/enpipe/encoding/scenes_io.py, src/enpipe/encoding/keyframes.py, src/enpipe/encoding/hdr.py, src/enpipe/encoding/chunk.py, src/enpipe/encoding/audio.py, src/enpipe/encoding/metrics.py, src/enpipe/encoding/pipeline.py</files>
  <read_first>
    - legacy/encode_scenes.py (full — the parity oracle; migrate the exact line ranges above)
    - legacy/encode_scenes.py:515-537 (the argparse block to STRIP vs the preflight to KEEP — the boundary matters)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md (encoding sections for each module, plus the Shared Patterns: proc seam, dual error regime, worker-thread tuple return, env-var constants, die relocation)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md (Pattern 3 die relocation, Pattern 4 env consts, Pitfall 3/4)
    - .planning/codebase/CONVENTIONS.md (Russian prose, typing generics, frozen dataclasses, banners, collect-then-report, high-water-mark)
  </read_first>
  <action>
    Copy each symbol into its target module per the line map with ZERO logic changes (D-13/D-15). Replace the local `run(...)` calls with `proc.run(...)` (`from enpipe.shared import proc`) in `keyframes.keyframe_table_ffprobe`, `hdr.detect_hdr`, `chunk.count_frames`/`encode_chunk`, `audio.encode_audio`, and `pipeline` (incl. the final `mkvmerge` mux call); import `die`/`log`/`step` from `enpipe.shared.logging`. In `scenes_io.py` put `import re` at the TOP of the file (do NOT repeat legacy's mid-file import) and preserve the two-level failure exactly (per-line silent skip; whole-file zero-match → `die()`, Pitfall 4). Keep the EBML/Cues walk inline in `keyframes.py` — do NOT create `mkv/ebml.py` (D-07; that split is Phase 2). Copy env-var constants character-for-character (`ICQ`/`QPMAX`/`GOP_LEN` in chunk.py, `DV_PROFILE` in hdr.py, `FLAC_LEVEL`/`LOSSLESS` in audio.py, `JOBS` in pipeline.py) — Pitfall 3. Keep `chunk_command` a pure argv builder (do NOT split it). Keep `encode_audio`/`encode_chunk` returning `(bool, Optional[str])` and NEVER calling `die()` (worker-thread regime).
    PREFLIGHT RETENTION (review finding — opencode HIGH #2, consensus #6): in `pipeline.py`, rename `def main()` to `def run_encode(args) -> None`. Strip ONLY the `argparse.ArgumentParser`/`add_argument`/`parse_args` lines (that is genuine Phase-4 CLI glue). RETAIN the `shutil.which` tool-availability preflight loop AND the `args.video.is_file()` check (legacy :533-537) as the FIRST statements inside `run_encode` — keeping them preserves the early, clear `die("не найден <tool>")` behavior and is the most parity-preserving choice. Add `import shutil` at the top of pipeline.py. Do NOT drop preflight silently (that would be an undocumented behavior change contradicting D-13). Keep the remaining `args`-attribute reads unchanged (video/scenes/out/frm/to/workdir/keep/jobs/no_audio/no_metrics/csv) and preserve the high-water-mark `flush_appends`/`movie.obu` append and collect-then-report error list verbatim. Do NOT migrate the `__main__` guard.
    SANCTIONED DEVIATION NOTE (review finding — consensus #7): removing ONLY the argparse parsing while KEEPING the preflight inside `run_encode` is the sanctioned minimal structural change for the module/CLI split — argv, tool checks, and output bytes are unchanged. Record this preflight-retention decision in the SUMMARY so the "ZERO logic changes" contract (D-13) is explicit, not silently contradicted.
  </action>
  <verify>
    <automated>uv run python -c "import enpipe.encoding.scenes_io, enpipe.encoding.keyframes, enpipe.encoding.hdr, enpipe.encoding.chunk, enpipe.encoding.audio, enpipe.encoding.metrics, enpipe.encoding.pipeline as p; import inspect; from enpipe.encoding.chunk import chunk_command, ICQ; from enpipe.encoding.hdr import DV_PROFILE; assert ICQ==23 and DV_PROFILE=='10.1'; assert hasattr(p,'run_encode'); src=inspect.getsource(p.run_encode); assert 'shutil.which' in src, 'preflight dropped'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE "subprocess\\.(run|Popen|call|check_output)" src/enpipe/encoding/` returns no matches, and there is no local `def run(` in `src/enpipe/encoding/` (all subprocess via `proc.run`).
    - `grep -Rn "mkv/ebml" src/enpipe/encoding/` returns nothing and the EBML helpers `_ebml_num/_eid/_esz` live in `keyframes.py` (D-07 respected).
    - Env-const defaults intact with a clean environment: `chunk.ICQ==23`, `chunk.QPMAX==100`, `chunk.GOP_LEN==300`, `hdr.DV_PROFILE=="10.1"` (Pitfall 3 tripwire).
    - `pipeline.run_encode` exists, reads the legacy `args` attribute names, and RETAINS the `shutil.which` tool preflight + `args.video.is_file()` check as its first statements (`inspect.getsource(run_encode)` contains `shutil.which`); `scenes_io.py` has `import re` at the top (grep line number < first function def).
    - `encode_audio`/`encode_chunk` contain no `die(`/`sys.exit(` call (worker-thread tuple regime preserved).
    - The preflight-retention decision is recorded in the SUMMARY as a sanctioned (non-logic) structural note.
  </acceptance_criteria>
  <done>Encoding is fully migrated behind the seam + shared die(), EBML stays inline, preflight is retained, and run_encode has the Phase-4-ready shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encoding fast test tier (TEST-01 pure + TEST-02 mocked)</name>
  <files>tests/unit/encoding/test_scenes_io.py, tests/unit/encoding/test_keyframes.py, tests/unit/encoding/test_chunk.py, tests/subprocess/encoding/test_hdr.py, tests/subprocess/encoding/test_chunk.py, tests/subprocess/encoding/test_audio.py, tests/subprocess/encoding/test_keyframes.py</files>
  <read_first>
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md (Code Examples: TEST-01 keyframes/scenes_io/chunk; TEST-02 hdr with `fp` fixture; Pattern 4 monkeypatch.setattr)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md (test-tier sections; chunk_command needs NO fp fixture)
    - src/enpipe/encoding/*.py (migrated signatures under test)
  </read_first>
  <behavior>
    - read_scenes: parses `frames [N, M)` ranges; silently skips non-matching lines; raises SystemExit (die) on a zero-match file.
    - kf_before: returns the last keyframe ≤ frame (exact-match, between-keyframes, first-frame cases) on a synthetic table.
    - fmt_seek: floors to the millisecond (2.0009 → "00:00:02.000") and handles h:m:s rollover.
    - _ebml_num/_eid/_esz: decode synthetic EBML byte inputs to the expected (value, next_pos) pairs.
    - parse_metrics: extracts ssim_all and psnr_avg from a synthetic qsvencc stderr block.
    - chunk_command: pure — returns argv containing exact --icq/--seek/--trim values; --psnr absent when metrics=False (direct call, no fp). Env-const reads (ICQ/QPMAX/GOP_LEN) are overridden via monkeypatch.setattr on the chunk module (Pattern 4), NOT setenv.
    - detect_hdr: builds exact ffprobe argv and returns the correct flag list per color_transfer/side-data (fp fixture).
    - encode_chunk / encode_audio / keyframe_table_ffprobe: exercise argv + failure paths (tuple return / die) via fp fixture.
  </behavior>
  <action>
    Write TEST-01 pure tests under `tests/unit/encoding/` (read_scenes incl. silent-skip and die-on-zero per Pitfall 4; kf_before; fmt_seek; EBML helpers; parse_metrics; chunk_command called directly — no fp). Because `chunk_command` reads module-level env-consts (`ICQ`/`QPMAX`/`GOP_LEN` set at import), any test asserting non-default values MUST use `monkeypatch.setattr(chunk, "ICQ", ...)` on the imported module (Pattern 4), never `setenv` after import. Write TEST-02 mocked tests under `tests/subprocess/encoding/` using the pytest-subprocess `fp` fixture for `detect_hdr` (exact ffprobe argv + flag selection), `encode_chunk`, `encode_audio` (assert the `(False, msg)` tuple on non-zero returncode, never a raise), and `keyframe_table_ffprobe` (assert the `die()`/SystemExit path). Keep fixtures inline/local — do not create or edit a shared root `tests/conftest.py` (Plan 01-02 owns the disjoint detection subtree; keeping the subtrees independent keeps the two test plans conflict-free regardless of execution order).
  </action>
  <verify>
    <automated>uv run pytest -m "not hardware" tests/unit/encoding tests/subprocess/encoding -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -m "not hardware"` passes the encoding unit + subprocess tests with no real media invoked.
    - `chunk_command` test asserts the exact `--icq`/`--seek`/`--trim` argv positions (using `monkeypatch.setattr` for any non-default env-const); `detect_hdr` test asserts the exact returned flag list.
    - read_scenes test proves silent per-line skip AND SystemExit on a zero-match file; encode_audio test proves the `(False, msg)` tuple (no exception) on failure.
  </acceptance_criteria>
  <done>Encoding pure-logic and subprocess-boundary behavior is covered by the fast, hardware-free tier.</done>
</task>

<task type="auto">
  <name>Task 3: Hardware-gated encode parity vs legacy — determinism pre-check + raw .obu compare (D-14)</name>
  <files>scratch/parity_encode.py</files>
  <read_first>
    - legacy/encode_scenes.py:515-724 (the main() the migrated run_encode mirrors; use it to shape the Namespace args, note movie.obu at :599-616 and --keep at :715)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md (Open Question 2 synthetic-clip parity; Environment Availability confirms ffmpeg/qsvencc/mkvmerge/`/dev/dri` present)
  </read_first>
  <action>
    Write a throwaway (NOT packaged, place under `scratch/`) parity script. It is HARDWARE-GATED (review finding — both reviewers MEDIUM, consensus #2): the FIRST thing it does is check `Path("/dev/dri/renderD128").exists()` and `shutil.which("qsvencc")`; if either is absent it prints `SKIP: no Arc hardware` and exits 0 (a clean skip, NOT a failure). This gate is NOT part of the default `pytest -m "not hardware"` fast tier — it lives in `scratch/`, is never collected by pytest, and requires a real encode. State this explicitly in a header comment so TEST-01/TEST-02's fast-tier promise stays honest.

    When hardware IS present, the script:
    (1) Generates a short synthetic source ONCE via `ffmpeg -f lavfi -i testsrc=duration=10:size=640x360:rate=24 -f lavfi -i sine=duration=10` into `scratch/parity_encode_sample.mkv` (DISTINCT filename — must never collide with Plan 01-02's `scratch/parity_detect_sample.mkv`).
    (2) Runs `python3 legacy/scene_detection.py scratch/parity_encode_sample.mkv` to produce `scratch/parity_encode_sample.mkv.scenes` (oracle input, kept independent so this gate is self-contained).
    (3) DETERMINISM PRE-CHECK (review finding — opencode HIGH #1, consensus #5): run `legacy/encode_scenes.py <sample> <scenes> -o scratch/legacy1.mkv --workdir scratch/wd_legacy1 --keep` TWICE (second into `scratch/wd_legacy2`, `-o scratch/legacy2.mkv`). Both `--keep` runs preserve `<workdir>/movie.obu`. `cmp scratch/wd_legacy1/movie.obu scratch/wd_legacy2/movie.obu`.
        - If the two legacy `movie.obu` are BYTE-IDENTICAL → qsvencc is deterministic on this box → PRIMARY GATE = byte-identical `cmp` of the pre-mux `.obu`.
        - If they DIFFER → qsvencc is non-deterministic (common for hardware AV1) → the byte-identical criterion is UNSAFE and would block a correct migration. FALL BACK to: exact per-chunk frame counts AND exact total frame count match (via the migrated `count_frames`/`ffprobe -count_packets`) PLUS SSIM/PSNR within a documented epsilon (e.g. |ΔSSIM| ≤ 1e-4, |ΔPSNR| ≤ 0.05 dB — record the exact epsilon used in the SUMMARY). The autonomous executor MUST NOT block on `cmp` failure when the pre-check proved non-determinism.
    (4) Builds an `argparse.Namespace`-shaped object with the legacy attribute names (video/scenes/out/frm/to/workdir/keep/jobs/no_audio/no_metrics/csv), sets `keep=True` and `workdir=scratch/wd_new`, and calls `enpipe.encoding.pipeline.run_encode(args)` → `scratch/wd_new/movie.obu` + `scratch/new.mkv`.
    (5) PRIMARY GATE — compare the RAW pre-mux `.obu`, NOT the final `.mkv` (review finding — both reviewers MEDIUM, consensus #1): `cmp scratch/wd_legacy1/movie.obu scratch/wd_new/movie.obu`. This eliminates mkvmerge mux-metadata variance (timestamps/track headers) that can differ even with identical video payloads. Apply the deterministic-vs-fallback criterion from step (3).
    (6) SECONDARY GATE (always, on the final `.mkv`): compare per-chunk and total frame counts via `ffprobe -count_packets` / the migrated `count_frames` on `scratch/legacy1.mkv` vs `scratch/new.mkv`.
    Exit non-zero on any mismatch under whichever criterion is in force.

    HDR GAP NOTE (review finding — opencode MEDIUM): the `testsrc` clip is SDR, so this gate does NOT exercise HDR10/HDR10+/Dolby Vision `--dolby-vision-rpu copy` paths end-to-end. That is acceptable for Phase 1 (the mocked `detect_hdr` TEST-02 covers the flag logic); real HDR-media parity is Phase 4 / TEST-04. State this gap explicitly in the script header and the SUMMARY. Note `scratch/*.obu`/`*.mkv` are gitignored by Plan 01-01.
  </action>
  <verify>
    <automated>uv run python scratch/parity_encode.py && echo "PARITY_OK_OR_SKIPPED"</automated>
  </verify>
  <acceptance_criteria>
    - The script is hardware-gated: it probes `/dev/dri/renderD128` + `qsvencc` and cleanly SKIPs (exit 0, "SKIP: no Arc hardware") when absent; a header comment states it is excluded from the `pytest -m "not hardware"` fast tier.
    - A determinism PRE-CHECK runs `legacy/encode_scenes.py` twice (both `--keep`) and `cmp`s the two `movie.obu`; the chosen parity criterion (byte-identical vs frame-count+SSIM/PSNR-epsilon fallback) is selected from that result and documented in the SUMMARY (including the epsilon if the fallback is used).
    - The PRIMARY byte-identical gate compares the PRE-MUX `<workdir>/movie.obu` files (produced with `--keep`/`keep=True`), NOT the final `.mkv`; frame-count/ffprobe checks on the final `.mkv` are the SECONDARY gate.
    - On real hardware, the migrated `run_encode` output matches legacy under the in-force criterion (byte-identical `.obu` when deterministic; frame counts + SSIM/PSNR within the documented epsilon otherwise).
    - The parity script lives outside `src/enpipe/`, adds no `[project.scripts]` entry, and writes to `scratch/parity_encode_sample.mkv` (distinct from Plan 01-02's `scratch/parity_detect_sample.mkv`).
    - The HDR-parity gap (SDR clip only; HDR/DV end-to-end deferred to Phase 4/TEST-04) is documented in the script header and SUMMARY.
  </acceptance_criteria>
  <done>Encoding migration is proven behavior-preserving on real hardware via a raw-.obu gate with a determinism pre-check and a documented non-deterministic fallback; the phase cannot block spuriously on a correct migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source filename/path ↔ subprocess argv | path values flow into ffprobe/qsvencc/mkvmerge argv via `proc.run` |
| env vars ↔ encode parameters | ICQ/QPMAX/GOP_LEN/DV_PROFILE/FLAC_LEVEL read at import into module constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-E1 | Tampering | encoding qsvencc/ffprobe/mkvmerge argv construction (chunk.py, hdr.py, keyframes.py, audio.py, pipeline.py) | mitigate | Migration preserves argv-list construction only; no `shell=True`, no string-interpolated commands (RESEARCH Security Domain V5). TEST-02 `fp`-fixture tests pin exact argv, catching injection-surface drift. |
| T-01-E2 | Tampering | mechanical copy silently shifting seek/trim frames (kf_before/fmt_seek) or drifting env-var defaults | mitigate | D-13 zero-logic-change discipline; TEST-01 covers the floor-to-ms/keyframe edge cases (Pitfall 1); env-const default tripwire (Pitfall 3); Task 3 raw-.obu parity gate with determinism pre-check. |
| T-01-E3 | Info Disclosure | error messages surfacing stderr/paths via die()/tuple text | accept | Local single-user batch CLI, no network sink; message text preserved verbatim from legacy for parity (no new exposure introduced). |
</threat_model>

<verification>
- `pytest -m "not hardware"` green for the encoding tier (parity gate excluded — it is hardware-gated in scratch/).
- Encode parity: raw `movie.obu` byte-identical to legacy when qsvencc is deterministic; otherwise frame counts match exactly + SSIM/PSNR within the documented epsilon. Final `.mkv` frame counts match as the secondary gate.
- No direct `subprocess.*` and no `mkv/ebml.py` in `src/enpipe/encoding/`; `run_encode` retains the `shutil.which` preflight.
</verification>

<success_criteria>
- ROADMAP Phase 1 criteria #2 (encoding half: mechanical migration behind `shared.proc`, behavior-preserving sample run), #3 (pure-logic encoding tests), and #4 (mocked argv/error-path for ffmpeg/ffprobe/qsvencc/mkvmerge call sites) satisfied for the encoding stage.
</success_criteria>

<output>
Create `.planning/phases/01-package-foundation-migration-fast-test-tier/01-03-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

Locked CONTEXT.md decisions implemented by this plan:
- **D-05**: encoding lands under `src/enpipe/encoding/`, coupled to detection only via the `.scenes` file (no cross-import).
- **D-06**: split encoding into `scenes_io`/`keyframes`/`hdr`/`chunk`/`audio`/`metrics`/`pipeline` submodules.
- **D-07**: the hand-rolled EBML/Cues parser moves inline into `encoding/` (isolation deferred to Phase 2).
- **D-08**: all encoding subprocess calls route through the `shared.proc` seam; `die()` consumed from `shared/logging.py`.
- **D-09**: pytest + pytest-subprocess for the encoding tests.
- **D-10**: `hardware` marker excluded by default (`pytest -m "not hardware"`); the hardware-gated encode-parity script sits in `scratch/`, outside the collected test tier.
- **D-11**: TEST-01 pure-logic targets (`kf_before`, `fmt_seek`, `read_scenes`, `_ebml_num`/`_eid`/`_esz`, metrics parsing) + TEST-02 subprocess-boundary targets (`detect_hdr`, `chunk_command`, `encode_chunk`, `encode_audio`, `keyframe_table_ffprobe`).
- **D-12**: no `main()`/CLI-glue coverage chase; no Hypothesis.
- **D-13**: encoding migrated after detection (wave 3, depends on 01-02). Preflight retention inside run_encode is the sanctioned minimal structural change (non-logic).
- **D-14**: behavior-preserving encoded-output parity vs `legacy/encode_scenes.py` — byte-identical raw `.obu` when qsvencc is deterministic, frame-count + SSIM/PSNR-epsilon fallback when it is not.
- **D-15**: conventions preserved verbatim during the mechanical move.
</output>
