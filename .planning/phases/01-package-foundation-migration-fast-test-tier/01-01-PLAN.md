---
phase: 01-package-foundation-migration-fast-test-tier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - src/enpipe/__init__.py
  - src/enpipe/detection/__init__.py
  - src/enpipe/encoding/__init__.py
  - src/enpipe/shared/__init__.py
  - src/enpipe/shared/proc.py
  - src/enpipe/shared/logging.py
  - .devcontainer/post-create.sh
autonomous: true
requirements: [PKG-02]

must_haves:
  truths:
    - "`uv sync --locked` installs runtime + dev deps from a committed uv.lock lockfile (no ad hoc pip install)"
    - "`import enpipe` works after an editable install and resolves from the uv-managed .venv, not system site-packages"
    - "`enpipe.shared.proc.run` and `.popen` are the sole subprocess call-through seam (wrap subprocess.run/Popen, never shell=True)"
    - "`enpipe.shared.logging` exposes die/log/step with the verbatim `encode_scenes: ` die prefix preserved"
    - "`.gitignore` excludes scratch-generated media (`*.mkv`/`*.scenes`/`*.obu`) so parity artifacts are never committed"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv_build backend + exact-pinned deps + hardware marker + pytest config"
      contains: "uv_build"
    - path: "uv.lock"
      provides: "committed cross-platform lockfile pinning scenedetect==0.7 and numpy==2.5.1 plus transitives"
      contains: "scenedetect"
    - path: ".gitignore"
      provides: "ignore rules for scratch/ generated media (mkv/scenes/obu) in addition to the existing Python artifact rules"
      contains: "scratch/"
    - path: "src/enpipe/shared/proc.py"
      provides: "run()/popen() subprocess seam"
      exports: ["run", "popen"]
    - path: "src/enpipe/shared/logging.py"
      provides: "die()/log()/step()/_START relocated leaf logging module"
      exports: ["die", "log", "step"]
    - path: ".devcontainer/post-create.sh"
      provides: "uv-bootstrap + uv sync --locked provisioning (replaces unpinned pip install)"
      contains: "uv sync --locked"
  key_links:
    - from: ".devcontainer/post-create.sh"
      to: "uv.lock"
      via: "uv sync --locked"
      pattern: "uv sync --locked"
    - from: "pyproject.toml"
      to: "uv_build backend"
      via: "[build-system] build-backend"
      pattern: "build-backend = \"uv_build\""
---

<objective>
Scaffold `enpipe` as an installable, dependency-pinned `src/`-layout Python package and stand up the two shared leaf modules (`shared/proc.py` subprocess seam, `shared/logging.py`) that both migration stages depend on.

Purpose: PKG-02 — retire the unpinned ad hoc `pip install` with a `uv`/`uv_build`/`uv.lock` toolchain, and establish the `shared.proc` choke point (D-08) and relocated `die()` (Pattern 3) before any migration touches call sites.
Output: `pyproject.toml`, committed `uv.lock`, package `__init__` tree, `src/enpipe/shared/{proc,logging}.py`, `.gitignore` scratch-media rules, and an updated `.devcontainer/post-create.sh`.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-CONTEXT.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md

<interfaces>
<!-- The exact seed for shared/proc.py and shared/logging.py, extracted from legacy. Copy verbatim, do not retype from memory. -->

Existing local run() wrapper to generalize — legacy/encode_scenes.py:66-67:
  def run(cmd: List[str], **kw) -> subprocess.CompletedProcess: return subprocess.run(cmd, **kw)

Popen call site needing a popen() sibling — legacy/scene_detection.py:266-270 (inside QsvPipeStream._start_process): subprocess.Popen(self._build_command(), stdout=subprocess.PIPE, stderr=self._stderr)

die/_START/log/step to relocate — legacy/encode_scenes.py:62-63 (die), :73-88 (_START, log, step).
  die MUST keep the literal prefix: sys.exit(f"encode_scenes: {msg}")  (part of the D-14 parity surface — do NOT rename to "enpipe:").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package, pin+lock deps (fail-fast), gitignore scratch media, rewrite provisioning</name>
  <files>pyproject.toml, uv.lock, .gitignore, src/enpipe/__init__.py, src/enpipe/detection/__init__.py, src/enpipe/encoding/__init__.py, src/enpipe/shared/__init__.py, .devcontainer/post-create.sh</files>
  <read_first>
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md ("Concrete `pyproject.toml`" section — the byte-for-byte manifest block; and the `.devcontainer/post-create.sh` change block)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md (`pyproject.toml` and `.devcontainer/post-create.sh` sections)
    - .devcontainer/post-create.sh (line 37 is the only line to replace; steps 1, 2, 2b, 4 stay untouched)
    - .gitignore (already covers `__pycache__/`, `*.egg-info/`, `.venv`, build artifacts, `.pytest_cache/`, `.ruff_cache/`, and leaves `uv.lock` UN-ignored — only scratch media rules are missing)
    - .planning/codebase/CONVENTIONS.md (no manifest exists yet — this is net-new config)
  </read_first>
  <action>
    Bootstrap `uv` if absent (official installer `curl -LsSf https://astral.sh/uv/install.sh | sh`, then export PATH="$HOME/.local/bin:$PATH"). Create `pyproject.toml` verbatim from RESEARCH "Concrete `pyproject.toml`": `[build-system]` requires `uv_build>=0.11.28,<0.12` and `build-backend = "uv_build"`; `[project]` name `enpipe`, version `0.1.0`, `requires-python >=3.12`, dependencies exactly `scenedetect[opencv-headless]==0.7` and `numpy==2.5.1` (per D-02); `[dependency-groups]` dev = `pytest==9.1.1`, `pytest-subprocess==1.6.0`, `pytest-mock==3.15.1` (per D-09, note the 1.6.0 pin — NOT "2.x"); `[tool.pytest.ini_options]` registering the `hardware` marker with `addopts = "-m \"not hardware\""` and `testpaths = ["tests"]` (per D-10). Do NOT add `[project.scripts]` (Phase 4 / PKG-01). Do NOT add `[tool.ruff]`/`[tool.pyright]` (RESEARCH Open Question 3 — deferred, not forbidden; may return in Phase 3 / v2 QUAL-01).
    PIN SEMANTICS (review finding — opencode MEDIUM): `scenedetect==0.7` under PEP 440 matches EXACTLY `0.7.0` (not `0.7.1`). This exact-`0.7.0` behavior is the intent per D-02 ("whatever currently works, documented"). Before committing the pin, confirm which `0.7.x` actually resolves/installs and works — if the working version is a `0.7.x` other than `0.7.0`, write that exact version (e.g. `==0.7.1`) instead of the bare `==0.7`. Document the chosen version in a comment or the SUMMARY.
    FAIL-FAST LOCK (review finding — opencode MEDIUM): run `uv lock` IMMEDIATELY after writing `[project.dependencies]`/`[dependency-groups]` and BEFORE anything else, so a non-existent pinned version (`scenedetect`, `numpy==2.5.1`, `pytest==9.1.1`, `pytest-subprocess==1.6.0`, `pytest-mock==3.15.1`, `uv_build>=0.11.28,<0.12`) surfaces with a clear resolver diagnostic instead of blocking a later step. Then `uv sync --locked` to materialize the committed `uv.lock`.
    Create `src/enpipe/__init__.py` holding only `__version__ = "0.1.0"`; create empty `src/enpipe/{detection,encoding,shared}/__init__.py`.
    GITIGNORE (review finding — both reviewers MEDIUM/suggestion): the existing `.gitignore` already ignores `__pycache__/`, `*.egg-info/`, `.venv`, `build/`, and `.pytest_cache/`, and correctly leaves `uv.lock` UN-ignored (so it commits). It does NOT cover the scratch-generated media the parity scripts (Plans 01-02/01-03) write. Append a small `# enpipe scratch/parity artifacts` block ignoring `scratch/*.mkv`, `scratch/*.scenes`, `scratch/*.obu` (or `scratch/` wholesale if no scratch file needs committing). Do not remove or reorder existing entries.
    In `.devcontainer/post-create.sh`, replace ONLY the `python3 -m pip install ... "scenedetect[opencv-headless]" numpy` line (line 37) with the uv self-bootstrap + `uv sync --locked` block per D-03/RESEARCH (uv is confirmed absent in this image); leave the GPU, npm, claude-plugin, and self-check sections unchanged.
  </action>
  <verify>
    <automated>uv sync --locked && uv run python -c "import enpipe, scenedetect; assert enpipe.__version__ == '0.1.0'; assert '.venv' in scenedetect.__file__, scenedetect.__file__; print('ok')" && grep -qE "scratch/(\*\.(mkv|scenes|obu)|$)" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock` was run early and succeeded (no unresolved version); `uv.lock` exists, is committed, and pins the chosen `scenedetect` `0.7.x` and `numpy==2.5.1` (grep the lockfile).
    - `pyproject.toml` `[build-system] build-backend` equals `uv_build`; `[tool.pytest.ini_options]` registers the `hardware` marker and sets `addopts = "-m \"not hardware\""`.
    - The `scenedetect` pin intent is documented (exact `0.7.0` vs a specific `0.7.x`), not left ambiguous.
    - `import enpipe` succeeds and `scenedetect.__file__` resolves inside `.venv/`, not `/usr/local/lib/python3.12/` (Pitfall 5 tripwire).
    - `.gitignore` ignores `scratch/*.mkv`, `scratch/*.scenes`, `scratch/*.obu` (or `scratch/`); pre-existing rules for `__pycache__/`/`*.egg-info/`/`.venv` are unchanged and `uv.lock` remains committable (not ignored).
    - `.devcontainer/post-create.sh` contains `uv sync --locked` and no longer contains a `pip install ... scenedetect` line; sections 1/2/2b/4 are byte-unchanged.
    - No `[project.scripts]` and no `[tool.ruff]` block present in `pyproject.toml`.
  </acceptance_criteria>
  <done>Package installs from the lockfile with an early fail-fast lock, scratch media is gitignored, `import enpipe` works from the .venv, and provisioning no longer uses unpinned pip.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared subprocess seam and logging leaf module</name>
  <files>src/enpipe/shared/proc.py, src/enpipe/shared/logging.py</files>
  <read_first>
    - legacy/encode_scenes.py:62-63 (die), :66-67 (local run wrapper), :73-88 (_START, log, step)
    - legacy/scene_detection.py:266-270 (the one Popen call site the popen() sibling serves)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md (`src/enpipe/shared/proc.py` and `src/enpipe/shared/logging.py` sections — verbatim target bodies)
    - .planning/codebase/CONVENTIONS.md (Russian docstrings, `from __future__ import annotations`, `typing.List` not `list[...]`)
  </read_first>
  <action>
    Create `shared/proc.py` with the Russian module docstring from PATTERNS, `from __future__ import annotations`, `import subprocess`, `from typing import List`, and two functions: `run(cmd: List[str], **kw) -> subprocess.CompletedProcess` returning `subprocess.run(cmd, **kw)` and `popen(cmd: List[str], **kw) -> subprocess.Popen` returning `subprocess.Popen(cmd, **kw)`. Neither may set `shell=True` and neither adds a `runner`/`CommandRunner` parameter (D-08 anti-pattern). Keep both functions STATELESS (no module-level mutable state, no globals mutated) — they are reached from `ProcessPoolExecutor` boundary-worker functions in detection, so any state would be a pickle-safety hazard (review finding — qwen LOW). Create `shared/logging.py` copying `_START`, `log()`, and `step()` verbatim from legacy lines 73-88 (imports `sys`, `time`, `from contextlib import contextmanager`) and relocating `die()` here (Pattern 3 — keeps `keyframes.py`↔`pipeline.py` acyclic). Preserve `die`'s exact body `sys.exit(f"encode_scenes: {msg}")` — the `encode_scenes: ` prefix is part of the D-14 byte-identical parity surface (D-15); do not modernize it to `enpipe:`.
    IMPORT-SHADOW NOTE (review finding — both reviewers LOW): `shared/logging.py` shadows the stdlib `logging` module name. Downstream migrated modules MUST import it qualified — `from enpipe.shared.logging import die` or `from enpipe.shared import logging` — and MUST NOT write a bare `import logging` expecting the stdlib within a module that also references `enpipe.shared.logging`. Note this in the module docstring so migration executors do not conflate the two. `_START = time.monotonic()` is set at THIS module's import time, which shifts elapsed-time log prefixes vs legacy — this is cosmetic only (log text is not part of the parity surface) and must not be "fixed".
  </action>
  <verify>
    <automated>uv run python -c "from enpipe.shared import proc, logging; import inspect; assert 'shell' not in inspect.getsource(proc.run); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `enpipe.shared.proc` exports `run` and `popen`, both delegating to `subprocess` with no `shell=True`, no extra runner parameter, and no module-level mutable state (stateless / pickle-safe).
    - `enpipe.shared.logging` exports `die`, `log`, `step`; `grep "encode_scenes: " src/enpipe/shared/logging.py` matches (prefix preserved verbatim).
    - `log`/`step` bodies are a verbatim copy of legacy lines 73-88 (elapsed-time prefix and ▶/✔ markers intact).
    - The module docstring records the qualified-import requirement (never bare `import logging` in modules referencing `enpipe.shared.logging`).
  </acceptance_criteria>
  <done>Both shared leaf modules import cleanly and expose the seam + logging helpers every migrated module will consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev/CI ↔ PyPI registry | dependency artifacts (`scenedetect`, `numpy`, `pytest*`, `uv_build`) are downloaded and installed here |
| filename/path ↔ subprocess argv | values later passed to `proc.run`/`proc.popen` argv lists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | uv/PyPI dependency installs (scenedetect, numpy, pytest-subprocess, pytest-mock, uv_build) | mitigate | Exact `==` pins in `[project.dependencies]` + committed `uv.lock` (PKG-02). All packages returned `slopcheck OK` with no `[ASSUMED]`/`[SUS]`/`[SLOP]` in RESEARCH Package Legitimacy Audit — no blocking human-verify checkpoint required. |
| T-01-01 | Tampering | `shared/proc.py` run()/popen() | mitigate | Seam wraps argv-list `subprocess.run`/`Popen` only; never defaults `shell=True`; no string-interpolated commands (preserves the legacy positive pattern per RESEARCH Security Domain V5). |
| T-01-02 | Tampering | `uv` self-bootstrap in post-create.sh (curl installer) | accept | Official `astral.sh` installer over HTTPS; local single-user devcontainer, no elevation beyond user PATH. Documented in RESEARCH Assumption A2 as an accepted install path. |
</threat_model>

<verification>
- `uv sync --locked` reproduces the environment from the committed lockfile.
- `import enpipe` resolves from `.venv/` (Pitfall 5 guard).
- `enpipe.shared.proc.{run,popen}` and `enpipe.shared.logging.{die,log,step}` import without error.
- `.gitignore` excludes scratch parity media.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion #1 satisfied: `pip install -e .`/`uv`-equivalent editable install succeeds, `import enpipe` works, deps resolve from a checked-in `uv.lock`.
- The `shared.proc` seam and relocated `die()` exist for waves-2 migration to consume.
</success_criteria>

<output>
Create `.planning/phases/01-package-foundation-migration-fast-test-tier/01-01-SUMMARY.md` when done.
</output>

## Truths — Decision Traceability

Locked CONTEXT.md decisions implemented by this plan:
- **D-01**: `uv` + `uv_build` + `pyproject.toml` + committed `uv.lock` as the sole packaging/lock/build tool.
- **D-02**: pin `scenedetect[opencv-headless]` + `numpy` to exact working versions in `[project.dependencies]`.
- **D-03**: `.devcontainer/post-create.sh` installs from the lockfile (`uv sync`) instead of ad hoc `pip install`.
- **D-04**: editable install + `import enpipe` are the acceptance gate.
- **D-05**: create the `src/enpipe/{detection,encoding,shared}` skeleton (shared layer populated here).
- **D-08**: create `src/enpipe/shared/proc.py` seam (`run()`/`popen()`) and relocate `die()` to `shared/logging.py`.
- **D-15**: preserve conventions verbatim in the new shared modules (Russian docstrings, typing style, banners, log()/step()).
</output>
