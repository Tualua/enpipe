---
phase: 01-package-foundation-migration-fast-test-tier
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/enpipe/detection/config.py
  - src/enpipe/detection/stream.py
  - src/enpipe/detection/detect.py
  - src/enpipe/detection/parallel.py
  - tests/unit/detection/test_detect.py
  - tests/subprocess/detection/test_stream.py
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Detection code lives in src/enpipe/detection/{config,stream,detect,parallel}.py and routes every subprocess through enpipe.shared.proc (no direct subprocess.* calls)"
    - "`import enpipe.detection.detect` and `import enpipe.detection.parallel` both succeed with no circular-import error (deferred function-body imports)"
    - "detect_scenes on a synthetic clip produces a byte-identical .scenes output to legacy/scene_detection.py"
    - "`pytest -m \"not hardware\"` runs pure-logic detection tests (_min_scene_len, _build_scenes) and a mocked probe_source argv/error-path test, with no real media"
  artifacts:
    - path: "src/enpipe/detection/config.py"
      provides: "PathLike, SceneDetectionError, DetectionConfig, SourceInfo, Scene (frozen dataclasses)"
      contains: "class DetectionConfig"
    - path: "src/enpipe/detection/detect.py"
      provides: "_min_scene_len, _detect_relative, _build_scenes, detect_scenes"
      exports: ["detect_scenes"]
    - path: "src/enpipe/detection/parallel.py"
      provides: "keyframes_in_window, find_boundary, _sanitize_boundaries, workers, detect_scenes_parallel"
      exports: ["detect_scenes_parallel"]
    - path: "tests/subprocess/detection/test_stream.py"
      provides: "mocked ffprobe argv + SceneDetectionError error-path test for probe_source"
      contains: "def test_"
  key_links:
    - from: "src/enpipe/detection/stream.py"
      to: "enpipe.shared.proc"
      via: "proc.run / proc.popen"
      pattern: "proc\\.(run|popen)"
    - from: "src/enpipe/detection/detect.py"
      to: "src/enpipe/detection/parallel.py"
      via: "deferred function-body import"
      pattern: "from \\.parallel import detect_scenes_parallel"
---

<objective>
Mechanically migrate `legacy/scene_detection.py` into `src/enpipe/detection/{config,stream,detect,parallel}.py` behind the `shared.proc` seam with zero behavior change, then add the detection slice of the fast test tier and verify byte-identical `.scenes` output against the legacy oracle.

Purpose: TEST-01/TEST-02 for the detection stage, and the detection half of the D-14 parity gate. Detection migrates first per D-13 (smaller, no dependents).
Output: four detection modules, two test files, and a passing byte-identical parity run.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-CONTEXT.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md
@.planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Line-level source→module map (verified in RESEARCH Mechanical Migration Map). Copy-paste, do not re-derive. -->
config.py    <- legacy/scene_detection.py:50-107  (PathLike, SceneDetectionError, DetectionConfig, SourceInfo, Scene)
stream.py    <- legacy/scene_detection.py:115-422 (probe_source; QsvPipeStream — Popen at :266 → proc.popen)
detect.py    <- legacy/scene_detection.py:430-485 (_min_scene_len, _detect_relative, _build_scenes, detect_scenes)
parallel.py  <- legacy/scene_detection.py:498-644 (keyframes_in_window, find_boundary, _sanitize_boundaries, _boundary_worker, _segment_worker, detect_scenes_parallel)
__main__ block (:647-692) is NOT migrated this phase (Phase 4 / PKG-01).

Consumed from Plan 01-01: enpipe.shared.proc.run / enpipe.shared.proc.popen.
Seam substitution in tests: monkeypatch enpipe.shared.proc.run, or use pytest-subprocess `fp` fixture (hooks Popen).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate detection into four modules behind the proc seam</name>
  <files>src/enpipe/detection/config.py, src/enpipe/detection/stream.py, src/enpipe/detection/detect.py, src/enpipe/detection/parallel.py</files>
  <read_first>
    - legacy/scene_detection.py (full — the parity oracle; migrate the exact line ranges above)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md (detection sections: config/stream/detect/parallel — include the deferred-import and Popen→proc.popen excerpts)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md (Pattern 2 circular-import fix)
    - .planning/codebase/CONVENTIONS.md (preserve Russian prose, typing.List/Optional/Tuple generics, frozen dataclasses, banners, dual error regime)
  </read_first>
  <action>
    Copy each symbol into its target module per the line map above with ZERO logic changes (D-13/D-15). In `stream.py`, change `probe_source`'s `subprocess.run(...)` to `proc.run(...)` and `QsvPipeStream._start_process`'s `subprocess.Popen(...)` (legacy :266-270) to `proc.popen(...)`, keeping the exact `raise SceneDetectionError(...) from exc` chain and messages, and both `close()`/`finish()` methods. In `parallel.py`, route `keyframes_in_window`/`find_boundary` subprocess calls through `proc.run`, and keep `_boundary_worker`/`_segment_worker` as bare module-level functions (pickle-safety). Break the `detect.py`↔`parallel.py` circular import with deferred function-body imports in BOTH directions (Pattern 2): `from .parallel import detect_scenes_parallel` inside `detect_scenes`, and `from .detect import detect_scenes` inside `detect_scenes_parallel` (its two fallback call sites). Import the seam as `from enpipe.shared import proc`. Do NOT migrate the `__main__` block. Do NOT add any `runner`/`CommandRunner` parameter. Do NOT modernize style.
  </action>
  <verify>
    <automated>uv run python -c "import enpipe.detection.config, enpipe.detection.stream, enpipe.detection.detect, enpipe.detection.parallel; from enpipe.detection.detect import detect_scenes; from enpipe.detection.parallel import detect_scenes_parallel; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE "subprocess\\.(run|Popen|call|check_output)" src/enpipe/detection/` returns no matches (every call goes through `proc.`).
    - `grep -n "from \\.parallel import detect_scenes_parallel" src/enpipe/detection/detect.py` and `grep -n "from \\.detect import detect_scenes" src/enpipe/detection/parallel.py` both match (deferred imports present).
    - Importing `enpipe.detection.detect` and `enpipe.detection.parallel` in either order raises no ImportError (circular-import proven broken).
    - `config.py` defines `DetectionConfig`, `SourceInfo`, `Scene` as `@dataclass(frozen=True)` with `Scene.frame_count` property intact; Russian docstrings preserved.
  </acceptance_criteria>
  <done>Detection is fully migrated behind the seam with the circular import resolved and no direct subprocess usage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detection fast test tier (TEST-01 pure + TEST-02 mocked)</name>
  <files>tests/unit/detection/test_detect.py, tests/subprocess/detection/test_stream.py</files>
  <read_first>
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md (Code Examples: TEST-01 and TEST-02 with pytest-subprocess `fp` fixture; test_stream.py example)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-PATTERNS.md (test-tier sections)
    - src/enpipe/detection/detect.py and src/enpipe/detection/stream.py (the migrated signatures under test)
  </read_first>
  <behavior>
    - `_min_scene_len` returns the expected frame count for frames-configured and seconds-configured DetectionConfig inputs (synthetic values, no subprocess).
    - `_build_scenes` maps relative cut lists + fps into the expected `Scene` boundary tuples.
    - `probe_source` builds the exact ffprobe argv and parses width/height/fps/duration from registered JSON (pytest-subprocess `fp` fixture).
    - `probe_source` raises `SceneDetectionError` on the ffprobe failure path (non-zero returncode / FileNotFoundError), never `die()` (detection uses the typed-exception regime).
  </behavior>
  <action>
    Under `tests/unit/detection/test_detect.py` write TEST-01 pure-logic tests for `_min_scene_len` and `_build_scenes` using synthetic inputs only (no mocking, no subprocess), constructing `DetectionConfig()` directly. Under `tests/subprocess/detection/test_stream.py` write TEST-02 tests for `probe_source` using the `fp` fixture: register the exact ffprobe argv with a JSON stdout payload and assert the parsed `SourceInfo` fields, plus a failure-path test asserting `SceneDetectionError` is raised (per D-11 error-path requirement). Keep any needed fixtures inline/local to these files (do not create or edit a shared root `tests/conftest.py` — Plan 01-03 owns disjoint test subtrees, this keeps the two test plans conflict-free).
  </action>
  <verify>
    <automated>uv run pytest -m "not hardware" tests/unit/detection tests/subprocess/detection -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -m "not hardware"` collects and passes the detection unit + subprocess tests with zero real-media invocation.
    - The `probe_source` test asserts the exact ffprobe argv list (flags order preserved), not a loose substring.
    - A test asserts `SceneDetectionError` (not `SystemExit`) on the ffprobe failure path.
  </acceptance_criteria>
  <done>Detection pure-logic and subprocess-boundary behavior is covered by the fast, hardware-free tier.</done>
</task>

<task type="auto">
  <name>Task 3: Byte-identical detection parity vs legacy (D-14)</name>
  <files>scratch/parity_detect.py</files>
  <read_first>
    - legacy/scene_detection.py:647-692 (the __main__ `.scenes` writer format — replicate it verbatim in the throwaway script so the diff is byte-level)
    - .planning/phases/01-package-foundation-migration-fast-test-tier/01-RESEARCH.md (Open Question 2 — synthetic-clip parity recommendation; Environment Availability confirms ffmpeg present)
  </read_first>
  <action>
    Write a throwaway (NOT packaged, NOT a committed CLI, place under `scratch/`) parity script that: (1) generates a short synthetic source once via `ffmpeg -f lavfi -i testsrc=duration=10:size=640x360:rate=24 -f lavfi -i sine=duration=10` into an mkv; (2) runs `python3 legacy/scene_detection.py <clip>` as a subprocess to write `<clip>.scenes` (the oracle); (3) calls `enpipe.detection.detect.detect_scenes(<clip>)` and writes a second `.scenes` file using the byte-identical format string copied from the legacy `__main__` writer; (4) `diff`s the two `.scenes` files and exits non-zero on any difference. Use `--no-qsv`/software decode if QSV is unavailable, but the map notes hardware is present. This confirms the mechanical migration is behavior-preserving for detection (the phase's key correctness guard, D-14).
  </action>
  <verify>
    <automated>uv run python scratch/parity_detect.py && echo "PARITY_OK"</automated>
  </verify>
  <acceptance_criteria>
    - The migrated `detect_scenes` output `.scenes` file is byte-identical to the `legacy/scene_detection.py` output on the same synthetic clip (diff reports zero differences).
    - The parity script lives outside `src/enpipe/` and adds no `[project.scripts]` entry.
  </acceptance_criteria>
  <done>Detection migration is proven byte-identical to the legacy oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source filename/path ↔ subprocess argv | user-supplied path values flow into ffmpeg/ffprobe argv via `proc.run`/`proc.popen` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-D1 | Tampering | detection ffmpeg/ffprobe argv construction (stream.py, parallel.py) | mitigate | Migration preserves argv-list construction only; never introduce `shell=True` or string-formatted commands during the move (RESEARCH Security Domain V5). The `fp`-fixture TEST-02 test pins the exact argv, catching accidental injection-surface drift. |
| T-01-D2 | Tampering | mechanical copy silently altering detection logic | mitigate | D-13 zero-logic-change discipline + the Task 3 byte-identical parity gate against the legacy oracle. |
</threat_model>

<verification>
- `pytest -m "not hardware"` green for the detection tier.
- Detection `.scenes` output byte-identical to legacy on the synthetic clip.
- No direct `subprocess.*` in `src/enpipe/detection/`.
</verification>

<success_criteria>
- ROADMAP Phase 1 criteria #2 (detection half: mechanical migration behind `shared.proc`, byte-identical sample run), #3 (pure-logic detection tests), and #4 (mocked probe_source argv/error-path) satisfied for the detection stage.
</success_criteria>

<output>
Create `.planning/phases/01-package-foundation-migration-fast-test-tier/01-02-SUMMARY.md` when done.
</output>
