---
phase: quick-260709-711
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enpipe/detection/stream.py
  - src/enpipe/detection/parallel.py
  - tests/unit/detection/test_show_progress.py
  - tests/unit/detection/test_stream_progress.py
autonomous: true
requirements: [QUICK-260709-711]

must_haves:
  truths:
    - "На 2160p-файле в параллельном режиме (jobs=4) агрегированный tqdm-бар детекции сдвигается покадрово из всех сегмент-потоков, а не рывками по завершению целого сегмента"
    - "detect_scenes_parallel(show_progress=True) даёт БАЙТ-ИДЕНТИЧНЫЕ Scene-границы, что и show_progress=False (порядок results строго по futures[i], cut-математика не тронута)"
    - "Ветка show_progress=False (ex.map, 4-кортежи) остаётся неизменной"
    - "QsvPipeStream с progress_cb=None ведёт себя ровно как раньше (нулевое изменение поведения по умолчанию)"
    - "uv run ruff check src tests чисто; uv run pytest -m 'not hardware' -q зелёный, тестов >= 112"
  artifacts:
    - path: "src/enpipe/detection/stream.py"
      provides: "QsvPipeStream.__init__ принимает progress_cb; read() дёргает его на каждый прочитанный кадр"
      contains: "progress_cb"
    - path: "src/enpipe/detection/parallel.py"
      provides: "Покадровый агрегированный бар в show_progress-ветке; _segment_worker принимает 4- и 5-кортеж"
      contains: "progress_cb"
    - path: "tests/unit/detection/test_stream_progress.py"
      provides: "Hardware-free юнит: read() вызывает progress_cb ровно N раз на N кадров; None-дефолт не падает"
    - path: "tests/unit/detection/test_show_progress.py"
      provides: "Обновлённый параллельный тест под покадровую модель (5-кортеж + cb на кадр, без as_completed)"
  key_links:
    - from: "src/enpipe/detection/stream.py::read"
      to: "self._progress_cb"
      via: "вызов progress_cb(1) сразу после self._frame_num += 1"
      pattern: "self\\._progress_cb"
    - from: "src/enpipe/detection/parallel.py::detect_scenes_parallel"
      to: "_segment_worker"
      via: "seg_args_p = [a + (cb,) for a in seg_args]; cb = bar.update"
      pattern: "bar\\.update"
---

<objective>
Сделать агрегированный tqdm-бар детекции сцен в ПАРАЛЛЕЛЬНОМ режиме
(detect_scenes_parallel, show_progress=True) плавным ПОКАДРОВЫМ. Сейчас бар
двигается через as_completed — по завершению ЦЕЛОГО сегмента, поэтому на
длинном 2160p-файле (дефолт jobs=4) висит на 0% минутами и выглядит зависшим.

Purpose: убрать ложное ощущение зависания на длинных 4K-источниках. Изменение
СТРОГО аддитивное — не трогает cut-математику, порядок results, ветку
show_progress=False, сигнатуры CLI; новых зависимостей нет (tqdm уже
импортируется в parallel.py).

Output: progress_cb-хук в QsvPipeStream + покадровое обновление общего бара из
всех сегмент-потоков в parallel.py + hardware-free тесты.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Ключевые сигнатуры из кодовой базы. Использовать напрямую — без разведки. -->

src/enpipe/detection/stream.py:
- typing import (строка 12): `from typing import List, Optional, Union` — добавить `Callable`
- `class QsvPipeStream(VideoStream)`
- `def __init__(self, path, config, seek_sec=None, to_sec=None, max_frames=None)` (~97) — сюда добавить `progress_cb`
- `def read(self, decode=True)` (~251) — инкремент `self._frame_num += 1` на строке ~263, сразу после него вставить вызов callback
- read() читает кадр через `self._read_exact(self._frame_bytes)` из `self._proc.stdout`; поля: `_eof, _proc, _max_frames, _frame_num, _frame_bytes, _out_w, _out_h`

src/enpipe/detection/parallel.py:
- import (строка 18): `from concurrent.futures import ThreadPoolExecutor, as_completed` — `as_completed` станет неиспользуемым после правки (ruff F401), убрать его из импорта
- `def _segment_worker(args: tuple)` (~142): сейчас `path, config, seek_sec, to_sec = args` — расширить до приёма 4- и 5-кортежа
- `def detect_scenes_parallel(path, config, jobs, show_progress=False)` (~148): show_progress-ветка (~186-211) с as_completed/span_by_future — переписать на покадровый cb
- `tqdm` уже импортирован: `from scenedetect.platform import tqdm`
- НЕ ТРОГАТЬ: шаги 3-4 (накопленная сумма offset, non_cut_offsets merge, строки ~213-240) — корректностно-чувствительная cut-математика

tests/unit/detection/test_show_progress.py (существующий, обновить):
- `_SyncExecutor` имеет и `.map` (False-путь) и `.submit` (True-путь → `_FakeFuture(fn(arg))`)
- `_fake_segment_worker(args)` сейчас распаковывает 4-кортеж `_path, _config, _seek_sec, _to_sec = args`
- есть `_fake_as_completed` + `monkeypatch.setattr(parallel_module, "as_completed", ...)` — символ будет удалён, эти строки убрать
- `test_parallel_progress_bar_used` ассертит `sum(tqdm_sink["updates"]) == _TOTAL_FRAMES`
- `_FakeTqdm.update(n)` → `sink["updates"].append(n)`; `_SEG_LEN = 1680`, `_TOTAL_FRAMES = 5040` (3 сегмента)

tests/unit/detection/test_parallel_merge.py (НЕ ТРОГАТЬ): `_SyncExecutor` только с `.map`, вызывает jobs=3 БЕЗ show_progress → идёт через ex.map 4-кортежным путём, остаётся зелёным без правок.

tests/subprocess/detection/test_stream.py (НЕ ТРОГАТЬ): probe_source через pytest-subprocess `fp`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: progress_cb-хук в QsvPipeStream</name>
  <files>src/enpipe/detection/stream.py, tests/unit/detection/test_stream_progress.py</files>
  <behavior>
    - read() при заданном progress_cb вызывает его РОВНО один раз на каждый успешно прочитанный кадр (N кадров -> N вызовов progress_cb(1))
    - callback вызывается ТОЛЬКО для полностью прочитанного кадра: при обрыве потока (данных меньше frame_bytes -> EOF) вызова нет
    - progress_cb=None (дефолт) -> read() не падает и ведёт себя ровно как раньше
    - callback вызывается и при decode=False (кадр посчитан до ветки decode)
  </behavior>
  <action>
Сначала написать тест (RED), затем реализацию (GREEN).

Реализация в stream.py:
- На строке 12 добавить `Callable` в typing-импорт (по стилю проекта — typing-generics, НЕ PEP 604): `from typing import Callable, List, Optional, Union`.
- В `QsvPipeStream.__init__` добавить НОВЫЙ последний опциональный параметр `progress_cb: Optional[Callable[[int], None]] = None` (после `max_frames`). Сохранить `self._progress_cb = progress_cb` рядом с прочими `self._...` присваиваниями до `self._start_process()`. Инлайн-комментарий на русском: покадровый хук для агрегированного прогресс-бара параллельного детекта; дефолт None -> нулевое изменение поведения.
- В `read()` СРАЗУ ПОСЛЕ `self._frame_num += 1` (строка ~263, кадр уже успешно прочитан, ДО ветки `if not decode` — чтобы считались все кадры, включая decode=False) вставить: `if self._progress_cb is not None: self._progress_cb(1)`. Инлайн-комментарий (русский): дёргаем хук на КАЖДЫЙ прочитанный кадр — так общий бар в параллельном режиме двигается покадрово, а не рывками по сегментам.
- Больше в stream.py ничего не менять: сигнатуры свойств, close/finish, _build_command — как есть.

Тест tests/unit/detection/test_stream_progress.py (hardware-free, БЕЗ subprocess/ffmpeg). Модульный docstring на русском в стиле проекта. Проверить read()->progress_cb, НЕ запуская реальный ffmpeg: сконструировать экземпляр через `object.__new__(QsvPipeStream)` и вручную выставить минимальный набор полей, которые читает read(): `_eof=False`, `_proc=<фейк с .stdout>`, `_max_frames=None`, `_frame_num=0`, `_frame_bytes=<малое, напр. 6>`, `_out_w`/`_out_h` (под frame_bytes, напр. 1x2x3=6), `_progress_cb=<счётчик>`. Фейковый `_proc.stdout` — объект с методом `read(n)`, отдающий ровно N*frame_bytes байт, затем b"". Вызвать `stream.read(decode=False)` в цикле до False; заассертить, что счётчик == N. Отдельный кейс: `_progress_cb=None` -> цикл отрабатывает без исключений и возвращает False на EOF. (Если `object.__new__`-подход окажется хрупким — альтернатива: monkeypatch `stream_module.probe_source` (вернуть SourceInfo с валидными width/height) и `stream_module.proc.popen` (фейковый Popen c нужным stdout) и пройти реальный __init__.)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/detection/test_stream_progress.py -q && uv run ruff check src/enpipe/detection/stream.py tests/unit/detection/test_stream_progress.py</automated>
  </verify>
  <done>progress_cb добавлен в __init__ и вызывается в read() на каждый кадр; None-дефолт не меняет поведения; новый юнит-тест зелёный без hardware/subprocess; ruff чист.</done>
</task>

<task type="auto">
  <name>Task 2: покадровый агрегированный бар в detect_scenes_parallel</name>
  <files>src/enpipe/detection/parallel.py</files>
  <action>
Убрать `as_completed` из импорта строки 18 -> `from concurrent.futures import ThreadPoolExecutor` (он станет неиспользуемым после правки; иначе ruff F401).

`_segment_worker` (~142): расширить распаковку, чтобы принимать И 4-кортеж (старый путь ex.map + тест test_parallel_merge), И 5-кортеж с progress_cb:
  path, config, seek_sec, to_sec = args[0], args[1], args[2], args[3]
  progress_cb = args[4] if len(args) > 4 else None
  stream = QsvPipeStream(path, config, seek_sec=seek_sec, to_sec=to_sec, progress_cb=progress_cb)
  return _detect_relative(stream, config)
Инлайн-комментарий (русский): 5-й элемент опционален — 4-кортежные вызовы (ветка show_progress=False через ex.map + тесты) продолжают работать без правок.

show_progress-ветка (~186-211, блок `else:` под `if not show_progress`): УБРАТЬ as_completed/span_by_future-цикл. Вместо него:
  bar = tqdm(total=total, unit="frame", desc=f"Детект сцен (jobs={jobs})")
  cb = bar.update  # каждый прочитанный кадр ЛЮБОГО сегмента двигает общий бар (tqdm.update потокобезопасен)
  seg_args_p = [a + (cb,) for a in seg_args]  # progress_cb пятым элементом кортежа
  try:
      futures = [ex.submit(_segment_worker, a) for a in seg_args_p]
      results = [f.result() for f in futures]  # СТРОГО в исходном порядке futures[i]
  finally:
      bar.close()
Сохранить/обновить ведущий комментарий ветки, объясняющий КРИТИЧНОСТЬ: `results = [f.result() for f in futures]` собирается в исходном порядке futures[i], НЕ по as_completed — поэтому cut-математика шагов 3-4 ниже байт-идентична последовательной. Добавить замечание про total: сумма покадровых update ≈ total (ведущие -ss кадры отбрасываются select в _build_command и в read() не попадают), бар может закрыться на ~99% — косметически ок, tqdm терпит недо/переполнение.

НЕ ТРОГАТЬ: ветку `if not show_progress:` (`results = list(ex.map(_segment_worker, seg_args))`), шаги 3-4 (offset-накопление, non_cut_offsets merge), сигнатуру detect_scenes_parallel, _boundary_worker, _sanitize_boundaries.
  </action>
  <verify>
    <automated>uv run pytest -m "not hardware" -k parallel -q && uv run ruff check src/enpipe/detection/parallel.py</automated>
  </verify>
  <done>as_completed/span_by_future удалены; бар обновляется покадрово через cb=bar.update, проброшенный 5-м элементом seg_args; results собираются строго по futures[i]; _segment_worker принимает 4- и 5-кортеж; ветка False и cut-математика не тронуты; ruff чист; параллельные тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: обновить test_show_progress.py под покадровую модель + финальные гейты</name>
  <files>tests/unit/detection/test_show_progress.py</files>
  <action>
Обновить существующий tests/unit/detection/test_show_progress.py под новую покадровую модель (as_completed удалён из parallel.py, бар двигают progress_cb-вызовы из сегмент-воркеров):

1. Удалить `_fake_as_completed` и строку `monkeypatch.setattr(parallel_module, "as_completed", _fake_as_completed)` в `_patch_media_layer` — символа `as_completed` в parallel_module больше нет, monkeypatch с raising=True упадёт AttributeError.

2. `_fake_segment_worker`: принимать 5-кортеж и симулировать покадровое чтение, дёргая cb ровно _SEG_LEN раз (эмулирует read()->progress_cb реального QsvPipeStream, который в тесте не запускается):
     path = args[0]; ...; cb = args[4] if len(args) > 4 else None
     if cb is not None:
         for _ in range(_SEG_LEN):
             cb(1)
     return [(0, _SEG_LEN)]
   Так `test_parallel_progress_bar_used` сохраняет валидную ассерцию `sum(updates) == _TOTAL_FRAMES` (3 сегмента * _SEG_LEN), а False-путь (ex.map, 4-кортеж, cb=None) даёт 0 вызовов — ok. Обновить docstring теста, отразив, что бар теперь двигают покадровые cb, не span-по-сегменту.

3. `test_parallel_progress_preserves_order` — оставить проверку `pairs_true == pairs_false` и точных границ (регрессия порядка сохранена). Убедиться, что `_SyncExecutor.submit` (уже есть) отрабатывает 5-кортежи через обновлённый `_fake_segment_worker`.

Обновить/дополнить docstring модуля под новую модель. Не добавлять hardware/subprocess.

Затем прогнать ПОЛНЫЕ гейты (см. verify): ruff по src+tests чист; вся не-hardware сюита зелёная и тестов >= 112 (baseline 111 + новый test_stream_progress); страж корректности параллельного пути (-k parallel) зелёный.
  </action>
  <verify>
    <automated>uv run ruff check src tests && uv run pytest -m "not hardware" -k parallel -q && uv run pytest -m "not hardware" -q</automated>
  </verify>
  <done>test_show_progress.py обновлён (5-кортеж + cb-на-кадр, as_completed-мок удалён); test_parallel_merge.py НЕ тронут и зелёный; ruff check src tests чист; `uv run pytest -m "not hardware" -q` зелёный с числом тестов >= 112; -k parallel зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (нет новых) | Изменение чисто внутреннее: progress-callback между read() и tqdm-баром, никакого нового ввода/сети/парсинга. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-711-01 | Tampering | cut-математика offset/merge (parallel.py шаги 3-4) | mitigate | results строго по futures[i] (не as_completed) — байт-идентичность ветке False; регрессия -k parallel + test_parallel_merge доказывают неизменность границ |
| T-711-02 | Denial | tqdm.update из нескольких потоков | accept | tqdm.update потокобезопасен; недо/переполнение total косметическое, бар просто закрывается на ~99% |
| T-711-SC | Tampering | npm/pip/cargo installs | accept | новых зависимостей нет — tqdm уже импортируется в parallel.py, никаких install-шагов |
</threat_model>

<verification>
- `uv run ruff check src tests` — без ошибок.
- `uv run pytest -m "not hardware" -q` — все зелёные, число тестов >= 112 (baseline 111 + test_stream_progress).
- `uv run pytest -m "not hardware" -k parallel -q` — страж корректности параллельного пути зелёный (границы show_progress=True == False; non_cut merge сохранён).
- Ветка show_progress=False и legacy/ не тронуты; cut-математика и порядок results неизменны.
</verification>

<success_criteria>
- QsvPipeStream.__init__ принимает progress_cb (typing-generics стиль), read() дёргает его на каждый прочитанный кадр; progress_cb=None не меняет поведения.
- detect_scenes_parallel(show_progress=True) обновляет ОДИН агрегированный бар покадрово из всех сегмент-потоков; results собираются строго по futures[i].
- Границы Scene при show_progress=True байт-идентичны show_progress=False.
- Ветка show_progress=False (ex.map, 4-кортежи), сигнатуры CLI, cut-математика, legacy/ — не тронуты.
- Все гейты зелёные; тестов >= 112.
</success_criteria>

<output>
Create `.planning/quick/260709-711-smooth-per-frame-progress-bar/260709-711-SUMMARY.md` when done
</output>
