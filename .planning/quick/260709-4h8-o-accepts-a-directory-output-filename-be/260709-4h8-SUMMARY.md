---
quick_id: 260709-4h8
subsystem: encoding
tags: [pathlib, argparse, cli]

key-files:
  created:
    - tests/unit/encoding/test_resolve_output_path.py
  modified:
    - src/enpipe/encoding/pipeline.py
    - src/enpipe/cli/main.py

key-decisions:
  - "Directory detection uses Path.is_dir() (existing directory only) — trailing-slash intent on a non-existent path is not detectable through argparse type=Path"
  - "Zero behavior change preserved for -o <file> and omitted -o, keeping the hardware parity test byte-identical"

duration: 12min
completed: 2026-07-09
---

# Quick Task 260709-4h8: `-o` accepts a directory Summary

**Added `resolve_output_path()` helper so `enpipe encode`/`enpipe run -o <dir>` writes `<stem>.Encoded<suffix>` inside the directory instead of crashing with a PermissionError.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-09T03:09:00Z
- **Completed:** 2026-07-09T03:21:19Z
- **Tasks:** 2 completed
- **Files modified:** 3 (2 modified, 1 created)

## Accomplishments
- New pure helper `resolve_output_path(video, out)` in `src/enpipe/encoding/pipeline.py`, co-located with `run_encode`, implementing the locked three-branch rule (`None` → default, existing dir → `<stem>.Encoded<suffix>` inside it, else → unchanged file path)
- `run_encode` rewired to call the helper; `workdir` derivation line left untouched (now correctly lands inside the target directory when `out` was a directory)
- `-o/--out` help text updated on both `enpipe encode` and `enpipe run` subparsers (Russian, matching existing style) to document the new directory behavior
- New fast unit test module covering all three branches plus a multi-dot filename case (`A.B.C.mkv` → `A.B.C.Encoded.mkv`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract resolve_output_path() with directory support + rewire run_encode + update help** - `cb49cc7` (feat)
2. **Task 2: Fast unit test for resolve_output_path** - `6dda32f` (test), amended by `d566320` (fix — Rule 1 auto-fix, see Deviations)

**Plan metadata:** pending (orchestrator commits SUMMARY.md/STATE.md separately)

## Files Created/Modified
- `src/enpipe/encoding/pipeline.py` - added `resolve_output_path(video, out)` helper + rewired `run_encode`'s `out` derivation to call it; added `Optional` to the `typing` import
- `src/enpipe/cli/main.py` - updated `-o/--out` help text on the `encode` and `run` subparsers
- `tests/unit/encoding/test_resolve_output_path.py` - new fast unit test covering directory, explicit-file, `None`-default, and multi-dot-name branches

## Decisions Made
- Followed the plan's locked decisions exactly: `Path.is_dir()` for directory detection, zero behavior change for `-o <file>`/omitted `-o`, Russian docstrings/help text, `typing`-module generics (not PEP 604), banner-style module conventions preserved.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed unused `Path` import from new test file**
- **Found during:** Task 2 verification (full-suite ruff check after committing)
- **Issue:** `tests/unit/encoding/test_resolve_output_path.py` imported `pathlib.Path` but never referenced it directly (all paths came from the `tmp_path` fixture), tripping ruff's `F401` unused-import check
- **Fix:** Removed the unused `from pathlib import Path` import line
- **Files modified:** `tests/unit/encoding/test_resolve_output_path.py`
- **Verification:** `uv run ruff check src tests` now passes clean; `uv run pytest -m "not hardware" tests/unit/encoding/test_resolve_output_path.py -q` still 4/4 passing
- **Committed in:** `d566320` (follow-up commit on the same file)

---

**Total deviations:** 1 auto-fixed (1 bug/lint fix)
**Impact on plan:** Trivial lint cleanup, no scope creep, no behavior change.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Verification Results
- `uv run pytest -m "not hardware"` — 106 passed, 6 deselected (hardware tier correctly not run)
- `uv run ruff check src tests` — all checks passed
- `uv run enpipe encode --help` and `uv run enpipe run --help` — both show the updated `-o` help text describing the existing-directory `.Encoded` naming rule
- `resolve_output_path` inline sanity check (dir → `.Encoded`, explicit file unchanged, `None` → `.av1.mkv` default) — all assertions passed

## Next Phase Readiness
No blockers. This was a standalone quick task on top of the completed v1.1 milestone (Phase 5); no downstream phase depends on it.

---
*Quick task: 260709-4h8*
*Completed: 2026-07-09*

## Self-Check: PASSED

All created/modified files and all task commit hashes verified present on disk / in git log.
