---
phase: 260709-6jo-live-tqdm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/enpipe/detection/detect.py
  - src/enpipe/detection/parallel.py
  - src/enpipe/detection/pipeline.py
  - tests/unit/cli/test_run_detect_roundtrip.py
  - tests/unit/detection/test_show_progress.py
autonomous: true
requirements: [QUICK-TQDM-01]
must_haves:
  truths:
    - "Запуск `enpipe detect` печатает строку СТАРТ (файл, jobs, режим) в stderr до детекции"
    - "Во время детекции виден ЖИВОЙ tqdm-прогресс-бар (последовательный путь — штатный бар PySceneDetect; параллельный — один агрегированный бар)"
    - "После детекции печатается строка ФИНИШ (N сцен, прошедшее время) в stderr"
    - "Существующая финальная строка `{N} сцен -> {out_path}` в stdout НЕ изменилась"
    - "show_progress=False (дефолт) сохраняет байт-идентичное поведение и порядок результатов на обоих путях"
  artifacts:
    - path: "src/enpipe/detection/detect.py"
      provides: "show_progress проброшен в _detect_relative и detect_scenes"
      contains: "show_progress"
    - path: "src/enpipe/detection/parallel.py"
      provides: "агрегированный tqdm-бар в detect_scenes_parallel; ex.map-путь сохранён при show_progress=False"
      contains: "from scenedetect.platform import tqdm"
    - path: "src/enpipe/detection/pipeline.py"
      provides: "СТАРТ/ФИНИШ строки в stderr; detect_scenes(..., show_progress=True)"
      contains: "show_progress=True"
    - path: "tests/unit/detection/test_show_progress.py"
      provides: "юнит-тесты проброса show_progress и сохранения порядка/поведения при False"
  key_links:
    - from: "src/enpipe/detection/detect.py::_detect_relative"
      to: "SceneManager.detect_scenes"
      via: "именованный аргумент show_progress"
      pattern: "detect_scenes\\(.*show_progress"
    - from: "src/enpipe/detection/parallel.py::detect_scenes_parallel"
      to: "результаты сегментов В ИСХОДНОМ ПОРЯДКЕ"
      via: "results = [f.result() for f in futures] (не по завершению)"
      pattern: "f\\.result\\(\\) for f in futures"
---

<objective>
Добавить видимую обратную связь хода детекции сцен в CLI: строка СТАРТ, ЖИВОЙ
прогресс-бар (tqdm), строка ФИНИШ. Дизайн низкорисковый — cut-математика и
порядок результатов НЕ меняются, новая зависимость НЕ добавляется (tqdm берётся
из транзитивного `scenedetect`).

Purpose: Долгая детекция сейчас «немая» — пользователь не видит прогресса до
самой финальной строки. Живой бар и старт/финиш-строки закрывают этот UX-пробел.
Output: Обновлённые detect.py / parallel.py / pipeline.py + новый юнит-тест;
существующие 107 non-hardware тестов остаются зелёными.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Ключевые контракты, извлечённые из кодовой базы. Использовать напрямую. -->

Стиль CLAUDE.md (СОБЛЮДАТЬ): русская in-code проза; typing-generics `Optional/List/Tuple`
(НЕ `X | None` PEP 604); `from __future__ import annotations` первой строкой; баннеры
`# --------- ... --------- #`; импорты сгруппированы вверху файла.

src/enpipe/detection/detect.py:
```python
def _detect_relative(stream: QsvPipeStream,
                     config: DetectionConfig) -> List[Tuple[int, int]]:
    ...
    manager.detect_scenes(video=stream, show_progress=False)   # <- целевая точка

def detect_scenes(
    path: PathLike, config: DetectionConfig = DetectionConfig(), jobs: int = 1
) -> List[Scene]:
    if jobs and jobs > 1:
        from .parallel import detect_scenes_parallel
        return detect_scenes_parallel(path, config, jobs)
    stream = QsvPipeStream(path, config)
    rel = _detect_relative(stream, config)
    ...
```

src/enpipe/detection/parallel.py (сегмент-шаг, шаг 2 в detect_scenes_parallel):
```python
from concurrent.futures import ThreadPoolExecutor    # <- добавить as_completed
...
with ThreadPoolExecutor(max_workers=jobs) as ex:
    results = list(ex.map(_segment_worker, seg_args))  # <- ветвление по show_progress
```
Ранние возвраты-fallback (total is None / len(bnds) < 3):
`return detect_scenes(path, config, jobs=1)` — оба ДОЛЖНЫ пробрасывать show_progress.
`bnds` — список (frame, time, is_cut); размах сегмента i = `bnds[i+1][0] - bnds[i][0]`.

src/enpipe/detection/pipeline.py::run_detect(args):
```python
scenes = detect_scenes(args.input, cfg, jobs=args.jobs)      # <- +show_progress=True
...
print(f"{len(scenes)} сцен -> {out_path}")                    # <- stdout, ОСТАВИТЬ как есть
```
args имеет поля: input, output, width, threshold, min_scene_len_frames,
min_scene_len, no_qsv, qsv_device, jobs.

scenedetect (проверено в среде): `from scenedetect.platform import tqdm` — настоящий
класс tqdm 4.68.x, транзитивная зависимость. `SceneManager.detect_scenes(video, show_progress=True)`
берёт total из `stream.duration`. Пишет в stderr.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Проброс show_progress через слой детекции (detect.py + parallel.py)</name>
  <files>src/enpipe/detection/detect.py, src/enpipe/detection/parallel.py</files>
  <behavior>
    - detect_scenes(path, cfg, jobs=1, show_progress=True) при jobs=1 передаёт show_progress=True в SceneManager.detect_scenes (проверяется в Task 3 моком).
    - detect_scenes(..., jobs>1, show_progress=True) вызывает detect_scenes_parallel(..., show_progress=True).
    - detect_scenes_parallel с show_progress=False возвращает СТРОГО ТЕ ЖЕ Scene (порядок и границы байт-идентичны прежним) — ветка ex.map не тронута.
    - detect_scenes_parallel с show_progress=True возвращает те же Scene, что и с False (порядок результатов не зависит от порядка завершения futures).
    - Fallback-ветки (total is None, len(bnds) < 3) при show_progress=True пробрасывают его в detect_scenes(jobs=1).
  </behavior>
  <action>
    В detect.py: добавить параметр `show_progress: bool = False` (typing-стиль, дефолт False —
    обратная совместимость) в сигнатуру `_detect_relative` и передать его в
    `manager.detect_scenes(video=stream, show_progress=show_progress)` вместо жёсткого False.
    Добавить `show_progress: bool = False` в сигнатуру `detect_scenes`; при jobs=1 вызвать
    `_detect_relative(stream, config, show_progress=show_progress)`; при jobs>1 —
    `detect_scenes_parallel(path, config, jobs, show_progress=show_progress)`.

    В parallel.py: в импорт `from concurrent.futures import ...` добавить `as_completed`.
    Добавить импорт `from scenedetect.platform import tqdm` в верхний блок импортов (баннерный
    стиль, НЕ mid-file). Добавить `show_progress: bool = False` в сигнатуру
    `detect_scenes_parallel`. Оба ранних fallback-return'а
    (`return detect_scenes(path, config, jobs=1)`) заменить на
    `return detect_scenes(path, config, jobs=1, show_progress=show_progress)`.

    Сегмент-шаг (шаг 2) сделать ВЕТВЛЕНИЕМ, НЕ трогая cut-математику ниже:
    - `if not show_progress:` — оставить БЕЗ изменений: `results = list(ex.map(_segment_worker, seg_args))`
      (сохраняет байт-идентичный дефолт и совместимость с `_SyncExecutor.map` в существующих тестах).
    - `else:` — сабмитить в исходном порядке: `futures = [ex.submit(_segment_worker, a) for a in seg_args]`;
      сопоставить каждому future кадровый размах его сегмента через dict
      `span_by_future = {futures[i]: bnds[i + 1][0] - bnds[i][0] for i in range(len(futures))}`;
      создать `bar = tqdm(total=total, unit="frame", desc=f"Детект сцен (jobs={jobs})")`;
      в `try/finally` крутить `for f in as_completed(span_by_future): bar.update(span_by_future[f])`,
      в finally `bar.close()`; затем КРИТИЧНО собрать
      `results = [f.result() for f in futures]` (В ИСХОДНОМ ПОРЯДКЕ, НЕ по as_completed) —
      cut-математика шагов 3-4 остаётся байт-идентичной.
    Сегмент-воркеры (`_segment_worker`) НЕ получают show_progress (агрегируем один бар снаружи).
    Комментарии — на русском, объяснять «почему» (порядок futures ради корректности склейки).
  </action>
  <verify>
    <automated>uv run ruff check src/enpipe/detection/detect.py src/enpipe/detection/parallel.py && uv run pytest -m "not hardware" -k "parallel or detect" -q</automated>
  </verify>
  <done>Сигнатуры несут show_progress=False по умолчанию; ruff чист; существующие
  detect/parallel/merge/gate тесты зелёные (ветка ex.map при False не тронута).</done>
</task>

<task type="auto">
  <name>Task 2: СТАРТ/ФИНИШ строки + включение прогресса в run_detect (+ фикс стабов round-trip)</name>
  <files>src/enpipe/detection/pipeline.py, tests/unit/cli/test_run_detect_roundtrip.py</files>
  <action>
    В pipeline.py: добавить в верхний блок импортов `import sys` и `import time` (баннерный/
    группировочный стиль). Перед вызовом detect_scenes: определить строковый режим
    `"параллельный" if args.jobs and args.jobs > 1 else "последовательный"` и напечатать строку
    СТАРТ в stderr (русская, напр.: `f"Детекция сцен: {args.input} (jobs={args.jobs}, {режим})"`)
    через `print(..., file=sys.stderr, flush=True)`. Замерить `t0 = time.monotonic()`.
    Вызвать `detect_scenes(args.input, cfg, jobs=args.jobs, show_progress=True)`.
    После — строку ФИНИШ в stderr: `f"Готово: {len(scenes)} сцен за {time.monotonic() - t0:.1f}с"`.
    Существующую строку `print(f"{len(scenes)} сцен -> {out_path}")` (stdout) ОСТАВИТЬ как есть —
    она парсибельна, новые строки идут в stderr, чтобы не смешиваться. НЕ дублировать одну и ту
    же информацию — СТАРТ/ФИНИШ (stderr) и итог-путь (stdout) несут разное.

    В tests/unit/cli/test_run_detect_roundtrip.py: run_detect теперь зовёт detect_scenes с
    именованным `show_progress=True`, поэтому существующие стабы должны его принимать. Обновить
    ВСЕ стабы/лямбды `detect_scenes` в файле, добавив параметр `show_progress=False` (или `**kwargs`),
    чтобы новый kwarg не вызывал TypeError. Логика ассертов (round-trip, cfg, jobs, output) —
    без изменений. Число тестов в файле не меняется.
  </action>
  <verify>
    <automated>uv run ruff check src/enpipe/detection/pipeline.py tests/unit/cli/test_run_detect_roundtrip.py && uv run pytest -m "not hardware" tests/unit/cli/test_run_detect_roundtrip.py -q</automated>
  </verify>
  <done>run_detect печатает СТАРТ/ФИНИШ в stderr, зовёт detect_scenes(show_progress=True),
  сохраняет stdout-строку; все round-trip тесты зелёные с обновлёнными стабами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Юнит-тесты проброса show_progress и сохранения порядка/поведения</name>
  <files>tests/unit/detection/test_show_progress.py</files>
  <behavior>
    - test_sequential_passes_show_progress_to_scene_manager: detect_scenes(jobs=1, show_progress=True)
      приводит к вызову SceneManager.detect_scenes с show_progress=True (мок пишет переданное значение).
    - test_sequential_default_is_false: detect_scenes(jobs=1) (дефолт) даёт show_progress=False.
    - test_parallel_progress_preserves_order: detect_scenes_parallel(show_progress=True) возвращает
      те же Scene, что и с show_progress=False, на синтетических (замоканных) сегментах — порядок
      результатов не зависит от порядка завершения futures.
    - test_parallel_progress_bar_used: при show_progress=True создаётся tqdm с total=total_frames,
      вызываются update и close (мок tqdm фиксирует вызовы).
  </behavior>
  <action>
    Создать tests/unit/detection/test_show_progress.py (стиль TEST-01/TEST-03: без subprocess/GPU,
    русская докстрока-«почему», `from __future__ import annotations`). Переиспользовать паттерны из
    tests/unit/detection/test_parallel_merge.py (SourceInfo-фейк, sync-executor shim), расширив shim
    методом `.submit` (возвращает объект-обёртку с `.result()`) для show_progress-ветки, и
    замокав `enpipe.detection.parallel.as_completed` (вернуть futures как есть) и
    `enpipe.detection.parallel.tqdm` (записывающий стаб с total/update/close).

    Последовательный путь: замокать `enpipe.detection.detect.QsvPipeStream` (фейк с frame_rate,
    минимальным интерфейсом) и `enpipe.detection.detect.SceneManager` так, чтобы
    `detect_scenes(...)` фиксировал переданный `show_progress` и вернул синтетический scene_list;
    проверить True при show_progress=True и False по умолчанию.

    Параллельный путь: как в test_parallel_merge — замокать probe_source, _boundary_worker,
    _segment_worker, ThreadPoolExecutor(_SyncExecutor c .map И .submit); прогнать
    detect_scenes_parallel(jobs=3) с show_progress=False и =True, сравнить списки Scene на равенство
    (порядок и границы идентичны); при True проверить, что tqdm-стаб получил total=_TOTAL_FRAMES и
    что update/close вызывались.
  </action>
  <verify>
    <automated>uv run ruff check tests/unit/detection/test_show_progress.py && uv run pytest -m "not hardware" -q</automated>
  </verify>
  <done>Новый файл добавляет тесты (общее число non-hardware тестов >= 107 + новые);
  вся сюита зелёная; регресс параллель==последовательно подтверждён при обоих show_progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (без изменений) | Чисто внутреннее изменение UX-вывода; новых входов извне, сети или пакетов не появляется |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6jo-01 | Tampering | detect_scenes_parallel cut-математика | mitigate | ex.map-ветка при show_progress=False не тронута; при True results собираются В ИСХОДНОМ ПОРЯДКЕ (`[f.result() for f in futures]`); Task 3 сравнивает результат True==False |
| T-6jo-SC | Tampering | новые пакеты | accept | Новых пакетов нет — tqdm берётся из уже установленного транзитивного scenedetect; аудит легитимности не требуется |
</threat_model>

<verification>
- `uv run ruff check src tests` — чисто (без F/E9-нарушений).
- `uv run pytest -m "not hardware" -q` — все зелёные, число тестов >= 107 (базовые) + новые из Task 3.
- `uv run pytest -m "not hardware" -k parallel -q` — регресс-тесты параллельного пути зелёные
  (правился parallel.py; порядок результатов сохранён).
- Ручная проверка (опц., требует медиа/GPU — вне автоверификации): `enpipe detect <clip>` показывает
  СТАРТ-строку, живой бар, ФИНИШ-строку в stderr; stdout-строка `{N} сцен -> ...` без изменений.
</verification>

<success_criteria>
- show_progress проброшен через _detect_relative / detect_scenes / detect_scenes_parallel с дефолтом False.
- Последовательный путь при show_progress=True даёт штатный tqdm-бар PySceneDetect.
- Параллельный путь при show_progress=True даёт ОДИН агрегированный tqdm-бар, cut-математика и
  порядок результатов байт-идентичны прежним.
- run_detect печатает СТАРТ/ФИНИШ в stderr, включает show_progress=True, сохраняет stdout-строку.
- Новая прямая зависимость НЕ добавлена; legacy/ и CLI-флаги не тронуты.
- Вся non-hardware сюита зелёная (>= 107 тестов), ruff чист.
</success_criteria>

<output>
Create `.planning/quick/260709-6jo-live-tqdm/260709-6jo-SUMMARY.md` when done
</output>
