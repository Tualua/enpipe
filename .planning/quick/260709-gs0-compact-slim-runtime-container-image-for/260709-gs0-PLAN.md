---
phase: quick-260709-gs0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - docker/README.md
autonomous: true
requirements: [QUICK-260709-gs0]
must_haves:
  truths:
    - "Корневой Dockerfile существует и статически двухстадийный (builder + runtime на python:3.12-slim-trixie)"
    - "Runtime-стадия несёт медиа-стек (iHD/oneVPL/ffmpeg/mkvtoolnix/qsvencc/dovi_tool) и НЕ несёт dev-хлам (node/tmux/git/AI-CLI/GSD)"
    - "Пакет enpipe ставится в чистое НЕ-editable окружение из uv.lock, и точки входа `enpipe --help` / `enpipe run --help` дают код 0 с подкомандами"
    - "Существующий тест-тир `pytest -m 'not hardware'` остаётся зелёным (код src/ не тронут)"
    - "docker/README.md документирует build + run с --device /dev/dri и render-группой, и предупреждает про неработающие OpenCL-метрики"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage слим-рантайм-образ (builder: uv sync; runtime: медиа-стек + venv-copy)"
      contains: "FROM python:3.12-slim-trixie AS builder"
    - path: ".dockerignore"
      provides: "Минимальный build-контекст (только pyproject.toml/uv.lock/src)"
      contains: ".git"
    - path: "docker/README.md"
      provides: "Инструкция build/run для хоста с GPU"
      contains: "--device"
  key_links:
    - from: "Dockerfile (runtime stage)"
      to: "/opt/venv из builder"
      via: "COPY --from=builder /opt/venv /opt/venv"
      pattern: "COPY --from=builder /opt/venv"
    - from: "Dockerfile (builder stage)"
      to: "uv.lock (pinned deps + enpipe non-editable)"
      via: "uv sync --frozen --no-dev --no-editable"
      pattern: "uv sync --frozen --no-dev --no-editable"
---

<objective>
Создать ОТДЕЛЬНЫЙ компактный слим-рантайм-образ для запуска enpipe в проде,
не трогая dev-контейнер. Multi-stage Dockerfile в корне репозитория:
builder ставит пакет из `uv.lock` в чистый `/opt/venv` (не editable),
runtime переносит только медиа-рантайм (iHD/oneVPL/ffmpeg/mkvtoolnix/qsvencc/
dovi_tool) + этот venv. Рецепты медиа-зависимостей переиспользуются ДОСЛОВНО
из `.devcontainer/Dockerfile`.

Purpose: продовый запуск `enpipe run` на NAS/хосте с Intel Arc без dev-обвязки
(node/tmux/git/AI-CLI/GSD) и без editable-установки исходников.

Output: `Dockerfile`, `.dockerignore`, `docker/README.md`.

ГРАНИЦА СРЕДЫ (зафиксировать честно): в этой среде НЕТ docker/podman/buildah/
hadolint — образ здесь СОБРАТЬ и ЗАЛИНТИТЬ НЕЛЬЗЯ. Финальную сборку делает
пользователь на хосте с docker/podman + GPU. Локально автоматизируется ТОЛЬКО
воспроизведение builder-команды `uv sync --frozen --no-dev --no-editable` во
временный venv + проверка живости точки входа, плюс регрессионный `pytest`.
</objective>

<execution_context>
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/home/vscode/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.devcontainer/Dockerfile
@.devcontainer/devcontainer.json
@.devcontainer/post-create.sh
@pyproject.toml

<interfaces>
<!-- Контракты, извлечённые из репозитория. Использовать напрямую, без разведки. -->

Пакет (pyproject.toml):
- name = "enpipe", version = "0.1.0", requires-python = ">=3.12"
- build-backend = "uv_build" (requires "uv_build>=0.11.28,<0.12")
- [project.scripts] enpipe = "enpipe.cli.main:main"
- runtime deps: scenedetect[opencv-headless]==0.7, numpy==2.5.1 (в uv.lock, коммитнут)
- dev-группа (pytest/ruff и т.п.) — НЕ нужна в рантайме (--no-dev)

CLI-подкоманды (src/enpipe/cli/main.py):
- `enpipe detect ...`, `enpipe encode ...`, `enpipe run <video> ...`
- `enpipe run` — композитный detect->encode; поддерживает флаги --no-metrics, -o/--out

uv в этой среде: uv 0.11.28 (uv.lock 32KB, коммитнут). Команда builder-стадии
воспроизводима локально через UV_PROJECT_ENVIRONMENT в /tmp.

Медиа-рецепты (ДОСЛОВНО из .devcontainer/Dockerfile — переиспользовать, НЕ переписывать):
- deb822-включение contrib/non-free/non-free-firmware в /etc/apt/sources.list.d/debian.sources (строки 17-23)
- apt-блок медиа-рантайма + перебор libmfx-gen1.2|libmfxgen1|libmfx-gen1 (строки 32-42) — НО без tmux
- qsvencc: скачать latest _amd64.deb, dpkg-deb -R, awk-вырезание Depends intel-opencl-icd/libmfx1, dpkg-deb -b, apt install (строки 50-65)
- dovi_tool: musl-бинарь x86_64-unknown-linux-musl.tar.gz с GitHub releases, install -m0755 (строки 81-89)
- НЕ переносить: podman/buildah-фикс apt-sandbox (строки 91-99) — он для devcontainer-фич, в слим-рантайме фич нет
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-stage Dockerfile + .dockerignore в корне</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
Создать `/workspaces/enpipe/Dockerfile` (НЕ трогать `.devcontainer/Dockerfile`).
Русские комментарии, объясняющие ПОЧЕМУ. Две стадии:

STAGE builder — `FROM python:3.12-slim-trixie AS builder`:
- Принести uv: `COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/`. В комментарии
  зафиксировать: в проде тег стоит закрепить дайджестом (не latest); latest допустим
  с этим комментарием.
- `ENV UV_PYTHON=python3.12 UV_PROJECT_ENVIRONMENT=/opt/venv UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy`.
- `WORKDIR /app`; скопировать ТОЛЬКО `pyproject.toml`, `uv.lock`, `src/` (COPY-слои так,
  чтобы кэш не инвалидировался лишним).
- `RUN uv sync --frozen --no-dev --no-editable` → пакет enpipe встаёт в `/opt/venv`
  как настоящий wheel (не editable) с pinned-зависимостями из uv.lock. Комментарий:
  --no-dev выкидывает pytest/ruff; --no-editable гарантирует, что исходники src/ в
  финальный образ НЕ нужны.

STAGE runtime — `FROM python:3.12-slim-trixie` (без AS):
- `ENV DEBIAN_FRONTEND=noninteractive`.
- Комментарий-шапка: почему trixie (glibc≥2.39 нужен prebuilt-qsvencc; bookworm glibc 2.36 < 2.39 — не годится); почему slim, а не devcontainer-база (нет python-фич/vscode-обвязки, только рантайм); venv-copy портируется, т.к. builder и runtime — ОДНА база и один python-путь. Явно пометить: OpenCL-VPP-фильтры qsvencc НЕ работают (в trixie нет intel-opencl-icd), как и в devcontainer — базовый AV1-энкод через oneVPL работает.
- Включить contrib/non-free/non-free-firmware (deb822 sed по debian.sources — ДОСЛОВНО как .devcontainer/Dockerfile:17-23).
- `apt-get install --no-install-recommends`: `ca-certificates curl gnupg jq xz-utils` + медиа: `intel-media-va-driver-non-free libva2 libva-drm2 vainfo libvpl2 ocl-icd-libopencl1 ffmpeg mkvtoolnix` + перебор `for pkg in libmfx-gen1.2 libmfxgen1 libmfx-gen1; do ... break; done` — ДОСЛОВНО как .devcontainer/Dockerfile:32-42, но БЕЗ `tmux`. Чистить `/var/lib/apt/lists/*`.
- qsvencc через тот же dpkg-repack-трюк (curl latest _amd64.deb → dpkg-deb -R → awk-вырезание Depends intel-opencl-icd/libmfx1 → dpkg-deb -b → apt install) — ДОСЛОВНО как .devcontainer/Dockerfile:50-65. В конце чистить /tmp и apt-lists.
- dovi_tool musl-бинарь — ДОСЛОВНО как .devcontainer/Dockerfile:81-89 (dovi_tool ОСТАВИТЬ по решению пользователя; сохранить пояснительный комментарий о его текущей неиспользуемости/Phase-4 назначении в сжатом виде).
- НЕ ставить: node, tmux, git, build-essential, AI-CLI, GSD. НЕ переносить podman/apt-sandbox-фикс.
- `COPY --from=builder /opt/venv /opt/venv`.
- `ENV PATH="/opt/venv/bin:$PATH" LIBVA_DRIVER_NAME=iHD` (iHD — как containerEnv в devcontainer.json).
- `ENTRYPOINT ["enpipe"]`, `CMD ["--help"]`.

Создать `/workspaces/enpipe/.dockerignore` — держать build-контекст минимальным
(нужны только pyproject.toml, uv.lock, src/): исключить `.git`, `.planning`, `tests`,
`legacy`, `.devcontainer`, `scratch`, `**/__pycache__`, `*.pyc`, `*.pyo`, `*.mkv`,
`*.scenes`, `.venv`, `.pytest_cache`, `.ruff_cache`, `dist`, `docker`, `*.md`, `LICENSE`,
`PIPELINE_DESIGN.md`. (docker/README.md и .md-доки в образ не нужны.)

НЕ добавлять зависимости в pyproject. НЕ трогать src/, legacy/, .devcontainer/.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import pathlib, sys
df = pathlib.Path("/workspaces/enpipe/Dockerfile").read_text()
code = "\n".join(l for l in df.splitlines() if not l.lstrip().startswith("#"))
checks = {
  "builder stage": "FROM python:3.12-slim-trixie AS builder" in code,
  "runtime stage (2nd FROM)": code.count("FROM python:3.12-slim-trixie") >= 2,
  "uv sync frozen non-editable": "uv sync --frozen --no-dev --no-editable" in code,
  "venv copy": "COPY --from=builder /opt/venv /opt/venv" in code,
  "entrypoint": 'ENTRYPOINT ["enpipe"]' in code,
  "libva driver": "LIBVA_DRIVER_NAME=iHD" in code,
  "qsvencc repack": "dpkg-deb -R" in code and "intel-opencl-icd" in code,
  "dovi_tool": "dovi_tool" in code,
  "no tmux": "tmux" not in code,
  "no node/git-build": all(x not in code for x in ("nodejs", "build-essential")),
}
di = pathlib.Path("/workspaces/enpipe/.dockerignore").read_text()
checks["dockerignore .git"] = ".git" in di
checks["dockerignore tests"] = "tests" in di
bad = [k for k,v in checks.items() if not v]
print("FAIL:" , bad) if bad else print("OK: all structural checks passed")
sys.exit(1 if bad else 0)
PY</automated>
  </verify>
  <done>Dockerfile двухстадийный со всеми структурными маркерами; .dockerignore исключает .git/tests/legacy и пр.; tmux/node/build-essential отсутствуют в runtime-стадии.</done>
</task>

<task type="auto">
  <name>Task 2: docker/README.md — инструкция build/run</name>
  <files>docker/README.md</files>
  <action>
Создать `/workspaces/enpipe/docker/README.md` (короткий, русский). Содержимое:

- Заголовок + одно предложение: это слим-РАНТАЙМ-образ enpipe (отдельно от dev-контейнера), собирается на ХОСТЕ с docker/podman + Intel Arc GPU.
- Раздел «Сборка»: `docker build -t enpipe:slim .` (podman — аналогично). Отметить: контекст сборки минимизирован через .dockerignore; сборке нужны только pyproject.toml/uv.lock/src/.
- Раздел «Запуск»: пример
  `docker run --rm --device /dev/dri --group-add $(stat -c '%g' /dev/dri/renderD128) -v /path/in:/data -v /path/out:/out enpipe:slim run --no-metrics -o /out /data/movie.mkv`
  Пояснить каждый флаг: `--device /dev/dri` (проброс Intel Arc для QSV/VA-API — без него энкод не работает); `--group-add $(stat -c '%g' /dev/dri/renderD128)` (GID render-группы хоста для доступа к узлу; аналог того, что делает post-create.sh в devcontainer); `-v` тома вход/выход; `ENTRYPOINT` уже `enpipe`, поэтому в команде сразу `run ...`.
- Предупреждение: метрики `--psnr`/`--ssim` требуют OpenCL-VPP, а в trixie нет intel-opencl-icd → метрики НЕ работают; для прод-энкода использовать `--no-metrics`.
- Короткая заметка про базу: `python:3.12-slim-trixie` выбран ради glibc≥2.39 (нужен prebuilt qsvencc); bookworm не подходит.
- ЧЕСТНАЯ ремарка: образ в CI/этой dev-среде не собирается (нет docker) — сборка и проверка на GPU выполняются пользователем на хосте.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import pathlib, sys
t = pathlib.Path("/workspaces/enpipe/docker/README.md").read_text()
need = ["docker build", "enpipe:slim", "--device", "/dev/dri", "renderD128", "--group-add", "--no-metrics", "slim-trixie"]
miss = [n for n in need if n not in t]
print("FAIL missing:", miss) if miss else print("OK: README covers build/run/GPU/metrics")
sys.exit(1 if miss else 0)
PY</automated>
  </verify>
  <done>docker/README.md содержит build- и run-команды, объяснение --device/render-группы, предупреждение о неработающих метриках и заметку про базу.</done>
</task>

<task type="auto">
  <name>Task 3: Локальная проверка чистой установки пакета + регрессия</name>
  <files>(без репо-файлов — временный venv в /tmp, за собой убрать)</files>
  <action>
Доказать, что builder-команда из Dockerfile реально ставит enpipe в чистое
НЕ-editable окружение и точка входа жива — БЕЗ docker (в среде его нет).

1) Воспроизвести ИМЕННО builder-команду во временный venv вне репо:
   `UV_PROJECT_ENVIRONMENT=/tmp/enpipe-gs0-venv UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy uv sync --frozen --no-dev --no-editable --project /workspaces/enpipe`
   (та же команда, что в Dockerfile builder-стадии; --no-editable ставит enpipe как
   настоящий wheel — доказывает, что пакет собирается uv_build и ставится начисто.)
2) Из этого venv запустить `/tmp/enpipe-gs0-venv/bin/enpipe --help` и
   `/tmp/enpipe-gs0-venv/bin/enpipe run --help` — оба код 0; в выводе `--help`
   присутствуют подкоманды detect/encode/run, в `run --help` присутствует `--no-metrics`.
3) Убрать за собой: `rm -rf /tmp/enpipe-gs0-venv`.
4) Регрессия (код src/ не трогали — sanity): `uv run pytest -m "not hardware" -q`
   остаётся зелёным.

НИЧЕГО не коммитить (это делает оркестратор). Не запускать docker/hardware.
В SUMMARY зафиксировать: образ в этой среде НЕ собран (нет docker); сборку
проверяет пользователь на хосте с GPU.
  </action>
  <verify>
    <automated>set -e
cd /workspaces/enpipe
rm -rf /tmp/enpipe-gs0-venv
UV_PROJECT_ENVIRONMENT=/tmp/enpipe-gs0-venv UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy \
  uv sync --frozen --no-dev --no-editable --project /workspaces/enpipe
/tmp/enpipe-gs0-venv/bin/enpipe --help  | grep -Eq 'detect|encode|run'
/tmp/enpipe-gs0-venv/bin/enpipe run --help | grep -q -- '--no-metrics'
# доказать non-editable: пакет установлен как копия в site-packages, а не .pth на src/
test ! -e /tmp/enpipe-gs0-venv/lib/python3.12/site-packages/__editable__.enpipe*.pth
rm -rf /tmp/enpipe-gs0-venv
uv run pytest -m "not hardware" -q</automated>
  </verify>
  <done>`uv sync --frozen --no-dev --no-editable` ставит enpipe в чистый /tmp-venv; `enpipe --help` и `enpipe run --help` дают код 0 с подкомандами/--no-metrics; установка не editable; `pytest -m "not hardware"` зелёный; /tmp прибран.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases → образ | qsvencc `.deb` и dovi_tool musl-бинарь скачиваются curl'ом в build-time |
| apt (Debian trixie + non-free) → образ | системные медиа-пакеты |
| uv.lock → /opt/venv | pinned Python-зависимости (scenedetect, numpy), коммитнуты |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gs0-01 | Tampering | curl-загрузка qsvencc/dovi_tool с GitHub (latest, без пина дайджеста) | accept | Рецепт переиспользуется ДОСЛОВНО из уже работающего `.devcontainer/Dockerfile` — не новый источник; сборка идёт на доверенном хосте пользователя. Комментарий в Dockerfile рекомендует закрепить версии в проде. |
| T-gs0-02 | Tampering | uv-образ `ghcr.io/astral-sh/uv:latest` | accept | latest помечен комментарием как «в проде закрепить дайджестом»; uv лишь ставит уже-pinned deps из коммитнутого uv.lock. |
| T-gs0-SC | Tampering | Python-deps (`uv sync`) | mitigate | `--frozen` требует точного совпадения с коммитнутым uv.lock (numpy==2.5.1, scenedetect==0.7) — новые пакеты НЕ добавляются, pyproject не меняется; те же зависимости уже используются в devcontainer. |
| T-gs0-03 | Elevation of Privilege | `--device /dev/dri` + `--group-add renderGID` в runtime | accept | Необходимо для QSV/VA-API; документировано в docker/README.md; тот же доступ, что и в devcontainer. |
</threat_model>

<verification>
- Task 1: статический python-чек структуры Dockerfile (обе стадии, uv sync, venv-copy, ENTRYPOINT, iHD, qsvencc-repack, dovi_tool, отсутствие tmux/node/build-essential) + .dockerignore.
- Task 2: grep-проверка docker/README.md на build/run-команды, --device /dev/dri, render-группу, --no-metrics, slim-trixie.
- Task 3: воспроизведение builder-команды `uv sync --frozen --no-dev --no-editable` в /tmp-venv; `enpipe --help` / `enpipe run --help` код 0; non-editable подтверждён; `pytest -m "not hardware"` зелёный.
- Docker-сборка НЕ выполняется (нет docker) — проверяет пользователь на хосте с GPU.
</verification>

<success_criteria>
- Корневой `Dockerfile` — валидный multi-stage (builder на python:3.12-slim-trixie + uv sync; runtime с медиа-стеком и venv-copy), `.devcontainer/Dockerfile` не тронут.
- `.dockerignore` держит контекст минимальным (только pyproject.toml/uv.lock/src нужны образу).
- `docker/README.md` даёт готовые build/run-команды с пояснением GPU-проброса и ограничения метрик.
- Чистая НЕ-editable установка enpipe из uv.lock работает; точки входа живы; регрессионные тесты зелёные.
- src/, legacy/, .devcontainer/, pyproject.toml — не изменены.
</success_criteria>

<output>
Create `.planning/quick/260709-gs0-compact-slim-runtime-container-image-for/260709-gs0-SUMMARY.md` when done.
В SUMMARY честно указать: образ в этой среде НЕ собран (нет docker/podman) — финальная сборка и GPU-прогон выполняются пользователем на хосте.
</output>
